# step5_success_tracker —— 成功判定

**设计草案已出(2026-08-14):`../../docs/SUCCESS_TRACKER_DESIGN.md`**,从倒水任务切入,
结构任务无关。核心主张:**Tracker 不是一条判据,而是"判据的质检系统"** ——
便宜的 sim 内代理(进奖励)+ 昂贵的权威判官(抽样)+ **持续测量两者一致率**;
一致率掉下去 = 策略开始刷分。依据是本项目的仪表事故史(单指蹭转刷分 / 歪着抓 /
eval 读到复位后的 0 / TB 口径混用)。**仍待用户审定,代码照旧不要先放。**

## 要解决什么

根据 **RL 的输出**判定「训练是否正确、动作是否合理」—— 不只是"仿真里跑没跑通",
而是这条修正后的轨迹到底值不值得进最终数据集。

```
Step4 RL 输出 ─▶ 判定 ─▶ D_verified ─▶ 去重 ─▶ D_high-quality
```

## 已有可复用的素材

- **Step3 的 contact 评估模式**:不强求蛮力提起,判接触点是否合理
  (持续对指接触 + 没推翻物体)。同一思路可上升到轨迹级。
- **Step4 的 RL eval**:确定性评测成功率。
- **fidelity**:与原视频意图的偏离程度(`main` README 的奖励里已有这一项)。

## 去重维度(来自 main README 第 7 步)

几何 / 接触模式 / 抓姿 / 轨迹相似度 / 结果多样性。

设计定稿前不要往这里放代码。
