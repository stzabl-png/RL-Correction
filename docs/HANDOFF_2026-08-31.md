# HANDOFF 2026-08-31 (晚) —— 重启电脑前的交接

> 续会话第一步: 读这份 + `tasks/Pour/17/A_Design/DECISIONS.md` 的 L5-31~33。
> 训练全在**远端机器**上, 本机重启**不影响**它们; 但本机的守夜/后台任务全没了(§5)。

## 1. 正在跑什么 (L5-33 批次, 全部 2026-08-31 下午发射)

| 机器 | GPU | run | 配置 | 预算 | 交接时状态 |
|---|---|---|---|---|---|
| Denso | 0 | `AB2_base_oh_s31` | **鉴别**: 原配置(place/mouth关)+新判据, 只差seed | 6M | 5M, cert **0.842↑ 活** |
| Denso | 0 | `AB3_flat_o_s44` | 拍平·纯物轨(+新奖励) | 20M | **等链条**: s31 跑满后 Denso 上的 `~/chain_s44.sh` 自动点火(盯日志"max steps achieved", 与本机无关) |
| Denso | 1 | `AB3_base_oh_s41` | 分档·带手 | 20M | **已死** (cert 1.5M 起恒0, D2pre=1.0) |
| Denso | 2 | `AB3_base_o_s42` | 分档·纯物轨 | 20M | 5M, cert **0.900**, gate3 0.053 冒头, place/mouth 棘轮开始付钱 |
| Denso | 3 | `AB3_flat_oh_s43` | 拍平·带手 | 20M | 5M, cert 0.771 ✅ |
| msc | 1 | `AB3_noise1xC_oh_s45` | 方案C噪声1× | 20M | **已死** (同款: 1.5M cert归0) |
| msc | 2 | `AB3_noise3xC_oh_s46` | 方案C噪声3× | 20M | **已死** (同款) |
| msc | 4 | (空, 用户要求保留) | | | |

AB3 全开 `POUR_PLACE_SHAPE=1 POUR_MOUTH_BONUS=1`; 判据=圆盘相交6.75cm;
digest **95b11e5d**; 指纹已逐项核过(鉴别线两旗=False)。

## 2. 最重要的两个进行中结论

**① 鉴别成立**: s31(与死掉的 s21 只差 seed)在 s21 的死亡窗口活得很好 ⟹
base_oh 之死**不是配置必死**, 是高概率训练不稳。

**② 「分档×带手」存活 1/5, 其它组合 2/2**:
```
HYB∧分档  s21死 s31活 s41死 s45死 s46死   OBJ∧分档  s22活 s42活   拍平∧HYB  s23活 s43活
```
全部死法相同: ~1.5M cert 归零, D2pre=1.000(认证前瓶子倒了)。
★ **noiseC 两条也死** ⟹ 32.21 的"噪声母带抓握段跳变致死"解释被**削弱**
(C带跳变已修到 P95 2.99° 仍同款死), 它们更像「分档×带手」现象的一部分——
**台账里此修正还没写, 续会话补 L5-33.5**。

## 3. 等用户裁定 / 建议的下一步

用户最后看到的建议(未回复):
- **b 先行**: 对死线做认证失败分解探针(`B_SmokeTest/probe_certfail.py` 现成,
  拉 s41 的 1M ckpt, 看 cert 死在 rise/slip/pads 哪项; 现在有 5 条死线不是一对样本)
- **a 后行**: Denso 腾卡后补 2 个 seed 摸 base_oh 存活率

已裁定并完成: 方案C母带 / 圆盘相交6.75 / place+mouth棘轮 / 鉴别实验。

## 4. 巡查命令 (新会话直接用)

```bash
# 训练进度 (日志名就是这些, 不是 ab2_)
ssh Denso 'for n in seedtest_s31 ab3_base_oh ab3_base_o ab3_flat_oh ab3_flat_o; do echo -n "$n: "; grep -oE "Agent Steps: [0-9]+M" ~/$n.log 2>/dev/null | tail -1; done'
ssh msc   'for n in ab3_noise1xC ab3_noise3xC; do echo -n "$n: "; grep -oE "Agent Steps: [0-9]+M" ~/$n.log | tail -1; done'
# 守夜(已从 scratchpad 抢救到 scripts/watchdog.sh; 需先改回 RUNS 表核对)
bash scripts/watchdog.sh 6 &
# TB 拉回读曲线的现成方式见 L5-32/33 台账里的 python 片段; 本地缓存 logs/_ab3_tb/
```

## 5. 重启丢了什么 / 不丢什么

- **丢**: 本机守夜循环、/tmp scratchpad(含旧夜报 html 源文件)、后台等待任务。
- **不丢**: 七条训练(远端)、Denso 上的 s44 链条(远端 nohup)、
  所有代码与台账(已 commit+push **4f2f857** → origin/Step4_RL_Correction)、
  母带(已入库, LFS)、本地数据缓存 `logs/_ab2_final/`(AB2 终局 222M) 与 `logs/_ab3_tb/`。
- 汇报 Artifact(可继续更新): https://claude.ai/code/artifact/58237534-a3cd-4ba3-be34-dc8da13b3d86

## 6. 快速纪律回顾 (本轮踩过并已写进 CLAUDE.md 的)

- `pgrep/ps|grep` 自匹配 6 次 → **按 comm 过滤** (CLAUDE.md 6b)
- 起过的 Isaac 要回头看退没退, >30min 非训练进程当僵尸清 (5b)
- 改跨文件接口必须**全树同步**并 md5 复核三台 (L5-32.15)
- 被打断的远程发射命令要当"可能已执行"核实 (L5-33.4, s45 曾双发)
- 恒 0 的诊断量先怀疑"没被写"; 判读表/预置先写死再跑

## 7. AB2 终局数据在哪 (已停机的上一轮)

`logs/_ab2_final/` — 7 条完整 run(ckpt/TB/视频/world.json) + taitan 探针日志。
终表与全部结论: 台账 32.22; base_o 是新判据下唯一非零 success(0.061)。
