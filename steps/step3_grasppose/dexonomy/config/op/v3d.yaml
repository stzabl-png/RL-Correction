n_max: 100
data: succ_grasp # Choices: [init, grasp, succ_grasp, succ_traj, init_template, new_template]
succ: null   # Choices: [True, False, None]

hand:
  mode: visual  # Choices: [visual, collision]
  init_body: False
  contact: False
  skeleton: False

object:
  contact: False

cust_log_dir: ${log_dir}