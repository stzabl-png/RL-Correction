# DECISIONS.md —— Unscrew(拧瓶盖) 实例全史权威

> 规则: 每个拍板、标定结论、事故验尸、判据改动都在此落账, 带日期;
> 结论被推翻时不删旧账, 加"★已推翻"引到新账。判据数值以 L3_Learning/progress.py
> 为准, 本文件记"为什么是这个数"。
> 前史: 旧扭盖任务 (recon_kailang 单 clip, Dyn1~23/U1~U39) 的台账在
> `tasks/recon_kailang/bottle_reconstruction/LEDGER_unscrew.md` (unscrew_bottle_newEnv
> 分支)。本实例是 V5 框架重新实例化, 移植其中已被数据验证的结论 (逐条注明 U 号)。

### T0-1 任务定义 —— ✅ 拍板 (2026-08-29)
- 成功 = G4: 拧开释放(G3) 后盖放到母带终点、瓶回位(placed), 双臂撤回站姿且
  物体不被碰倒碰歪。终态口径全在 progress.py。
- 数据引擎口径: 一个实例吃 datasets/unscrew_bottle 全部 17 条可用 clip
  (`UNSCREW_CLIP` 选条, 默认 32=README 榜首); **每次用一条数据训练, 用 RL 从
  粗糙重建轨迹恢复出一条物理可行的好轨迹, 批量导出演示数据**。
- 分工拍板 (2026-08-29 用户确认): cuRobo 只管机器段 (站姿→站位 Approach /
  收尾→站姿 Retreat, 残差冻结照谱); 从缝1到放盖收尾的**整个交互段**
  (抓稳/认证/带瓶/拧盖/放置) = RL, 参考 = 置信度物体轨迹 + 置信度人手轨迹。

### T0-2b 死通道零依赖 —— ✅ 拍板 (2026-08-30, 用户裁定"不用死的那个")
参考构建**只消费实测为活的通道**, 死的腕平移一个字节不碰:
| 通道 | 状态 | 用途 |
|---|---|---|
| 物体轨迹 + conf_pos/rot | 活 (盖 66cm/瓶 9cm) | 任务定义: 皮筋/时钟门/w_obj/成功判据 |
| 右腕四元数流 | 活 (59°) | 对握姿的 roll 种子 (U35 掌轴对准的底料) |
| 右手指流 + conf_fingers | 活 (44°) | 拧盖手法形状指引 (P-HYB, ×W_HCONF) |
| contact/affordance | 活 (2D 证据) | 场景摆放锚 (scene_layout) / 贴实奖金几何 |
| GraspPose prior (Screw27_body) | 非本数据 (Dexonomy) | **左手抓取全套**: 镜像腕位姿+指模板+squeeze |
| ~~腕平移 (双手)~~ | **死** (静态填充 ≤0.6cm) | **零依赖** |
| ~~左腕四元数流/左指流~~ | 活但弃用 | 描述的是"人的握法"且锚在死点上, 与 prior 握位几何不配 |
落地: 左腕 = Screw27_body 镜像 (q'=(w,-x,y,-z)) 锚瓶行, 绕瓶轴方位 ArmIK 可达率
扫描 (clip32: yaw*=240°, 与旧 clip grasp_prompt 接触区 150-240° 方位带吻合——
数据侧证据与 prior 侧选择互证)。手物相对位姿 by construction 恒定 (框架 v2 哲学)。
死通道若上游修复 (恢复 HaWoR 腕平移), make_reference 换回真腕轨迹是局部改动。

### T0-2 三 Prior 验收 —— ✅ (2026-08-29, 逐项核对)
- **物体轨迹+conf**: `object_valid_measured.npz` 的 conf_pos/rot_per_frame
  (0-100, 判据 ovm_v1)。clip32: 瓶 p50 88/63, 盖 p50 85/66, 拧盖窗多帧红档
  —— 三档体制正好吃这种数据。
- **人手轨迹**: ⚠ **腕平移仍是静态填充死数据** (clip32 实测全程位移 <0.6cm
  而盖走 66cm; 与旧 clip 同病, 上游未修)。腕姿态流(82°/59°)与手指流
  (44°/49°)是活的, 且带逐帧置信度 (`hand_confidence.npz` conf_pos/conf_fingers)。
  ∴ 交互段臂参考由**物体轨迹推导** (框架正统: v2 物体轨迹反解 IK);
  人手形状指引走**手指通道** (V5 文档预留的"拧瓶盖三指扭动 prior 通道"),
  权重 = W_HAND(物conf档) × W_HCONF(人手conf档)。
- **GraspPose**: 左手瓶 = `Screw27_body.npz` (含 squeeze 层) —— 本批 17/18 条
  CAD 与 water_bottle/screw27 **字节相同** (md5: 瓶 c9d18519 / 盖 c755e07c),
  prior 直接复用合法。右手盖 = 设定 B (盖 3.5cm 太小, GraspPose 处理不了):
  无 grasp prior, 指尖抓取由人手指流+affordance (`contact/expected_area_*`) 引导。

### T0-3 Gate 阶段机 —— ✅ 拍板 (2026-08-29)
- G1(+5) **只判左手** ≥3/5 垫稳 10 步: 演示时序右手在瓶拿起转平后才进场
  (clip32: 瓶 onset f22 / 盖 onset f32), 双手同判会让 G1 永不点火。
- G2(+8) 提升认证 (框架同款, 滑移只判左腕-瓶; 盖被螺旋钉在瓶上, 判双物 z 升)。
- G3(+10) = **拧开释放**: screw_assembly 的 detach 锁存 (拧满 turns 即脱开)。
  仿真螺旋角是唯一真值 —— 重建盖转角不可观 (螺轴对称, 数据集 README)。
- placed (不付奖) = 双物到**母带末交互行**位姿 (瓶3cm/15° 盖5cm/30° hold15)。
  ⚠ 不是静置位: 盖起点在瓶上、终点在桌上, rest≠end (与 Pour 的关键差异)。
- G4(+15 终局) 撤退归位, 框架同款。
- 每步件: adv/皮筋/工资/反射/斜坡/贴实(C线) 框架原样 + **r_screw = 2.0×Δθ**
  (双向计, 回拧扣分, U6 防刷分结构; 270° 总额 ≈9.4, 与 G3 的 10 同量级)。

### T0-4 参考体制选型 —— ✅ 拍板 (2026-08-29)
- 默认 **P-HYB** (V5 文档明示: 手内操作主导的拧瓶盖 → HYB), POUR_VARIANT=OBJ
  可跑消融对照。主档 tmix = **双物体短板** min(瓶,盖)×min(pos,rot)
  (README 排名同口径; Pour 只看主物体, 本任务盖是短板必须计入)。

### T0-5 螺旋抽象移植 —— ✅ (2026-08-29, 全部承旧台账实证)
走 `tasks/pregrasp/screw_assembly` 既有通路 (secondary 自动激活), 新加三个
默认关闭的钩子 (旧任务零影响):
- `screw_drive_gain` (U34+U39): 拇/食/中分级驱动 1指1/3速→3指全速, 无接触 0
  —— 无摩擦解析螺旋被亚阈值轻擦免费空转 (旧实测近随机策略 41% 假 release);
  slow-screw `max_angular_velocity=2.0 rad/s` (U34 用户裁定"不要太快的拧",
  20 rad/s 下单指戳 5 步拧完 = 拇指戳局部最优的制度性根源, pk22)。
- `screw_omega_damping=0.9` (v3 螺纹粘滞): 轻弹惯性立刻衰减。
- `screw_detach_at_full` (UnscrewRef1): 确定性策略拧满停手不该永卡在 ω>0 条件。
- `turns=0.75` (U30b): 演示实测 ~266° 分离; 2.0 圈是标准件假设 (难 2.7×)。
  逐 clip 可用 `UNSCREW_TURNS` 覆写 (本批各 clip 实际拧角未逐条测, 欠账见下)。

### T0-6 场景角色拍板 —— ✅ (2026-08-29)
`screw_primary="body"`: 瓶=env.object (listen-to-left-hand 摆放 + upright 投影),
盖=env.aux (reset_screw 合拢瓶顶) —— preengaged 的物理正确开局。若 primary=cap,
主体摆放机制会把盖摆到它自己的桌面锚点 (f32 时盖还在倾斜瓶顶的空中), 开局即错。
upright 投影 (U24a): 重建静置帧 ~20.5° FoundationPose 噪声 > 平底圆柱 18.3°
倾倒极限, 不投则瓶必自倒 (旧台账总根因, 参考侧与摆放侧同投影同口径)。
垫序与 Pour 相反: 前5=左vs瓶, 后5=右vs盖 (configure_cfg 按交互手=left 重建)。

### T1-1 β 剂量 —— ✅ 已诊断，后由 T2-4 裁定取代
初值 βL=2.0 来自 Pour17；clip32 已实测 β=1/1.5/2/3 四档。
结果与最终裁定见 T2-4。

### T1-2 母带 —— ✅ v1/v2 链路通（v2 实测见 T2-5）
- v1 (make_reference.py, 离线): clip32 全链 273 行 (app60/缝25/交互103/缝25/退60),
  自检家族五件全绿 (放音 G 链全通收入 38 / 批量逐位一致 / RSI 四点位 / 变体
  反向 / 体制)。盖脱离帧从数据测 (瓶体系相对漂移 >4cm): f35, 交互行 13。
  ⚠ 脱离检测**不能**用"投影轴×装配位"口径 —— 瓶姿态 20° 噪声投影后与原始
  盖位差 6cm, 会把脱离帧误报到首行 (自检实锤后改体系内相对量)。
  ⚠ 盖脱离后的行按**世界平移**换基 (与瓶同一平移), 不能按脱离点连续拼
  —— U24a 投影的座位高度差会把送放段整体压低 ~5cm, 盖末行破 D1 (自检实锤)。
- **已知欠账①**: 离线右臂 IK 达标率仅 2% (URDF 锚系统差 + 可能的 U35c 肘几何
  约束) —— v1 臂行是降级品。修复路径: probe_rest.py 拿实测 anchor_T 重跑
  make_reference; 仍塌则逐 clip 判 UNSCREW_HOLD_YAW (U35c 候补, 未实施);
  终解 = build_reference.py v2 在 Isaac 内站位捕获+增量空间 IK (框架正统)。
- **已知欠账②**: 机器段默认 smoothstep 占位 (无碰撞背书) —— plan_machine_segs
  产出 Approach/Retreat 后重跑 make_reference 即消。
- **已知欠账③**: v1 交互 1 行=1 重建帧, 20Hz 播放 1.33× 实时 (Pour17 v1 同
  口径); 高速窗时间扩张留给 v2。

### T1-3b cuRobo 本机自给自足 —— ✅ 收官 (2026-08-30)
"MagicSim 定制版"之谜破案: `curobo.motion_planner` 这套 API 就是 **NVlabs/curobo
新版主线** (v0.8+, warp 内核免编译) —— MagicSim 只是 vendor 了上游+加了机器人 yml。
本机配置 (全程无 MagicSim):
- 包: git clone NVlabs/curobo → ~/WorkSpace/curobo, pip -e (--no-deps) +
  cuda-core[cu12] 内核后端进 isaac env。⚠ isaacsim 钉 packaging==23.0/
  websockets==12.0, 装依赖时被拱过一次已还原; viser (websockets≥13) 只有
  RobotBuilder/Debugger 用 —— **重跑 yml 生成器时临时升 websockets, 完事还原**。
- 机器人配置: tools/make_vega1p_sharpa_curobo_yml.py 从仓库 URDF 自动生成
  `datasets/vega_urdf/vega_1p_sharpa_curobo.yml` (254 球/76 links; MorphIt)。
  臂/头网格自 github.com/luaiabuelsamen/vega_curobo (Dexmate 官方资产) 按名
  补齐 —— link 系与网格原点与我们 URDF **逐位相同** (L_arm_l2/l4 实测);
  躯干 = FK 推导手工球; 顺手修掉 meshes/vega_1p 指向他人机器的入库断符号链接。
- 排障两笔 (都进了生成器): ①猜名关节 F_wheel 不存在 → lock 按 cspace 过滤;
  ②站姿 5 对**亚毫米**伪碰撞 (肘弯 l5-l7 0.19mm + 相邻指节, 拟合球微突出) →
  IK 收敛 1e-7 但 success=False 的病根, 忽略表并入 (规划时手指锁死, 无害)。
- 冒烟 (tools/smoke_curobo_vega.py): 双臂 14 DoF / FK 合理 / 双工具位姿规划
  1.7s / 终点误差 0.0mm ★全通。worker 默认配置链: CUROBO_ROBOT_YML → MagicSim
  树 → 仓库自带生成品。
- 与 MagicSim 人工调的球存在差异: 首次真实规划若"过保守/起点碰撞", 用
  RobotBuilder.refit_link_spheres 排查 (登记为观察项, 不预修)。

### T1-3 cuRobo 机器段 —— ✅ 接线完毕 (2026-08-29; 缺包问题已由 T1-3b 解决)
- 链路: plan_machine_segs.py (Isaac 侧组 targets: 桌+双物障碍+新站姿躯干锁角
  40.52/73.66/0.39°) → curobo_plan_worker (干净子进程) → Approach.npz (双臂
  联合位姿规划到站位腕靶, obj_inflate 1cm) / Retreat.npz (cspace 直达站姿,
  物体钉在母带终位当障碍) → make_reference 自动消费, 缝1/缝2 焊接斜坡桥接
  规划解与离线 IK 解的分支差。
- ★上面“依赖 MagicSim 定制 fork / 本机未装”的初始判断已由 T1-3b **推翻**；
  当前 worker 首选本机 NVlabs/curobo 主线及仓库自带机器人 yml。
- ⚠ 旧 pregrasp_suite 的躯干锁角 45/90/0 是**旧世界值**, 本实例驱动已用新站姿;
  若复用 suite 本体, 先改这三个数。

### T2-0 Claude 交接审计 —— ✅ 代码收口；实跑见 T2-2/T2-5 (2026-08-30)
- 本地会话 `新配置搭建` (`ae387a7f-1d46-415b-94fa-b4bdc2dc634e`) 共落四个提交:
  `1de87065` 数据导入、`332e7c3e` V5 任务实例、`51b38f60` 死通道零依赖与
  cuRobo 自包含、`a0a299f0` cuRobo 机器人配置与冒烟。17 条 clip 可注册；新增
  17 份左手瓶 affordance 已逐点核对来源、归一化和有限性，属于有效未跟踪产物。
- 已交付的设计主线保持不变: cuRobo 仅负责 Approach/Retreat；缝1后的全交互由
  RL 残差处理；物体/置信度/右手指与姿态是活通道，腕平移死通道零依赖；四级
  Gate、58 动作/507 观测、三指接触分级螺旋驱动均已接线。
- 接管时发现并修正的假完成/串线风险:
  1. `probe_rest` 曾在未投影解析螺旋时直接 step，写出瓶盖同根位的假静置 JSON，
     且“预期 18cm”只打印不断言。无效文件已移到
     `/tmp/unscrew32_env_rest_invalid_20260830.json`；现在每步先投影螺旋、校验
     轴向/径向闭合 <5mm，再原子写入；并直接用基础环境启动，不依赖尚未生成的母带，
     消除 `env_rest → v1 → env_rest` 循环依赖。
  2. D6 使用一个正则源匹配多 body、过滤端却展开 5 项，触发 PhysX
     `expected 9, found 5`；改为四个显式远端 body 传感器，各过滤四个对侧远端体。
  3. 左抓 prior 曾预先镜像后又被 `make_reference` 再镜像；现只保留原始右手
     `Screw27_body.npz`，构带时恰好镜像一次。基类 approach/prior 脚手架与数据
     直立摆放冲突，已用受限 `bypass_lift_scaffold` 明确断开。
  4. 手工回放探针原先先 step 后 `apply_screw`，且从不刷新接触增益，导致螺旋
     永久零增益/释放不可能；v2 构建、β、IK、验收现与 DirectRLEnv 顺序一致。
  5. `probe_ikcheck`、`probe_acceptance` 当时改成了物理完美硬闸；
     **此项后由 T2-4 用户裁定取代**：IK 坏行只诊断，少于 3/4 数值稳定才返回非零。
     训练启动前仍硬验 v2、真实静置、双 cuRobo 段、clip/turns/β 与有限数组；
     训练稳定性通过才原子写绑定 v2 全文件 MD5 与现场完整世界指纹的
     `acceptance_v2.json`。训练入口在导入 IsaacLab 前验
     母带/凭据，建环境后再对现场世界，离线占位母带不能误发射。
  6. 世界指纹从旧 9 字段补齐为机器人/母带/IO/判据摘要/螺纹/方法参数/物性；
     关键字段缺失或不可读属于“未验”并拒绝续训、评测与录像，不再静默放行。
  7. cuRobo worker/机器段/v1/v2/静置/验收产物均改为原子落盘；机器段另写
     clip、段名、交互几何摘要和静置哈希，构带时逐项验 provenance，防止旧规划
     与新静置或新交互轨迹串线。
  8. 真正影响成败/螺旋接触的阈值集中到 `progress.py` 单一事实源，判据摘要
     schema=2；体制自检验证“阈值改动必改摘要、纯奖励改动不改摘要”。

- 离线复核: 17 clip 的注册/静态摆放/60k affordance 全部可读；静态重建单测
  11/11；五件判据自检全绿（体制件含 9 项）；clip32 离线审计母带 273 行，
  严格位置/姿态双门槛下右臂 IK 仅 2%（全行中位 14.61cm/19.7°）、左臂 56%；
  机器段为 smoothstep，文件只留 `/tmp`，未覆盖正式母带。
- 当前两张 GPU 持续满载既有训练/录像；未杀任何既有进程。真实 `probe_rest`
  及后续 T2-1 仍待卡空后按顺序执行，故此处不宣称 Isaac 验收完成。

### T2-1 冒烟/验收 —— ✅ 全序列完成 (顺序不可乱)
probe_rest → make_reference(重跑, 实测锚) → [plan_machine_segs ×2 →
make_reference(重跑)] → smoke_zero (A 静置对账 <5mm 铁则 / 机器段死线 0 误触)
→ probe_beta (βL 基线) → build_reference (v2) → probe_ikcheck (诊断) →
probe_acceptance (稳定≥3/4 后写凭据) → 训练入口预检/现场世界核对 →
训练冒烟 (双变体各短发)。

### T2-2 本机 Isaac 实跑: probe_rest→v1→cuRobo 机器段 —— ✅ (2026-08-30)
- probe_rest 实跑连闯三关: ① settle 循环每子步 `apply_screw` (缺投影时盖穿瓶
  掉桌, 假 rest 文件已在 T2-0 记案); ② 探针走基类, build_cfg 的 10 垫传感器
  撞上基类 finger_active(5) —— 探针把 `cfg.contact_sensors` 切回前 5 左垫
  (UnscrewEnv._setup_scene 同款处理); ③ 观测宽度 167 vs 占位 8 —— 实例上
  豁免 `_check_obs_dim` (探针不消费观测)。实测: 瓶倾角 0.0°, 盖-瓶轴向差
  18.0cm, anchor_T/站姿 44 指列入档。进程卡 `app.close()` 2 小时属已知病,
  kill -9 收尾。
- v1 实测锚重建: 右臂 IK 2%→13% (交互行贴人手流, v2 重铸域), 左臂 72%。
- **cuRobo Approach 首跑全灭 → 八轮二分定案** (全部离线 worker 复现, 免 Isaac):
  ① 去物体障碍仍败; ② 拆单臂: 右=位置✅姿态✅合体❌, 左=位置/姿态各自❌;
  ③ 关自碰撞 + 500 IK 种子仍败 ⟹ 纯运动学; ④ FK 交叉对账: cuRobo
  FK(ArmIK 左解) 中位 0.56cm/1.2° —— 两运动学链一致、env→base=+[0.5,0,0]
  正确、限位 0 越限; ⑤ cspace 直达已验证构型 + 空世界原地微动也被拒;
  ⑥ 自碰撞点名: 该构型无任何自碰; ⑦ 限位排查 = **真凶: ArmIK 钳限位出解**
  —— 左 j7 33 行钉 -79° 下限、右 j7 91/103 行钉 -64° 下限, "0.42cm 达标"
  是贴边换来的; cuRobo 带限位余量把贴边构型整族拒收。且左手镜像锚姿态本身
  超左腕 j7 行程 (现场 ArmIK 20 重启也只到 5.6cm/47.9°, j7 at_limit)。
- 三条修法 (均已落盘):
  1. make_reference 左抓方位扫描加 **限位内点判据** (全关节余量 >3° 才算
     达标行): yaw* 240°→270°, 内点可达率 50%。
  2. 机器段弃位姿 IK, 改 **cspace 直达内点 pregrasp 构型**: 收缩限位 3° 的
     ArmIK 解 station+净空 —— 左 = +5cm 径向 (距锚 7.82cm/53.6°, 左腕物理
     极限), 右 = +4cm 抬升 (0.37cm/2.8°; 8cm 抬升超可达域, 离线实测 ≤5cm
     才通)。构型存 `machine_pre_q_r/l` 入 v1, 纳入规划 digest。锚不可达
     部分由缝1+RL 消化 —— 数据引擎"从粗糙轨迹恢复"的正业, 不是缺陷。
  3. Retreat 起点 = machine_pre (**非**交互末行): 交互末行手贴瓶/盖/桌,
     碰撞检查器必判 start in collision (仅桌/仅瓶/仅杯微动全❌, 空世界✅);
     "松手撤离" 归缝2 数据斜坡, 与缝1 "贴近合拢" 对称 —— 缝 by design 是
     不做碰撞背书的桥, cuRobo 只认净空↔站姿。
- 结果: Approach 81 路点/终帧 0.00°/9.1s, Retreat 81 路点/0.00°/5.8s
  (均满障碍: 桌 + 充气 1cm 双物体, 物体钉各自段的母带位)。v1 全链
  273→316 行 (app 81/ret 82), 自检五件全绿。smoke_zero: A 静置对账
  瓶 0.20cm / 盖 0.26cm (<5mm 铁则过), obs (4,507), 机器段回放垫接触
  全零; ⚠ 800 步全程判定在 t=400 被人工中断 (共享卡让出), 待空卡补跑
  收尾行。
- ⚠ 遗留观察: 右臂交互行 j7 91/103 贴限 = 人手流腕姿对右臂运动学不友好的
  又一证据 (13% 达标同源), v2 Isaac 重铸 + RL 残差是既定救治路径; 若 v2 后
  仍贴限, 考虑对握锚绕螺轴 yaw 重定向 (离线实测 station 各 yaw 全可达,
  pregrasp 在 yaw+60°~180° 可达)。

### T2-3 placed 收紧: 护送判据 escort (2026-08-30 用户裁定)
- **问题** (用户看录像发现): 旧 placed 只判双物体终位姿 + hold15, "盖自己
  脱落/被甩到目标邻域" 也算成功 —— 大量假成功视频里右手根本没拿着盖放。
- **裁定**: 成功 = **右手拿着瓶盖放在桌子上**, 不是瓶盖自己落在桌子上。
- **机制** (只加一条, 一次一参数纪律; 首版"带上连击计数"被母带证伪 —— 人是
  转平低位拧开, 释放高度仅 0.92m, 带上方只剩 2cm 根本凑不齐连击):
  **过带单事件** —— 任何落到桌面的盖必恰好穿过放下带顶 (桌面+ESCORT_BAND=3cm)
  一次; 穿带那步若 [单步降>ESCORT_FALL=8mm(=16cm/s@20Hz) 且 无右垫-盖接触]
  ⇒ escort_fail 粘滞, placed 永不立。受控放下 = 带着接触穿带或慢放; 自由
  落体哪怕从带顶上 2cm 起掉, 穿带步降幅 >3cm 必抓, 与释放高度无关。
  新 step 输入 pads_r_cap (右垫-盖 ≥1 垫, env 从 _pads_f() 后 5 列取);
  None=停用 (离线探针兼容, env 必显式传)。
- **入账**: CRITERIA_SCHEMA 2→3 (ESCORT_BAND/ESCORT_FALL 进判据摘要);
  双版同步 (progress.py + progress_batch.py); diag/escort_fail 观察针;
  变体自检加 ④ 护送正路照常 G4 / ⑤ 自由落体 → escort_fail & placed 不立
  证伪对; 体制自检 schema==3 + ESCORT 阈值敏感性。
- **证伪信号**: 训练里若 sr/gate3 高而 sr/gate4≈0 且 diag/escort_fail 高
  ⇒ 策略只会拧不会放 (判据在拦真行为, 不是 bug); 若 escort_fail≈0 且
  录像仍见自由落体成功 ⇒ 判据漏(查 PAD_FTH 与盖的接触感度)。

### 观察针预登记 (防事后挑数)
- diag/screw_deg, diag/released, diag/n_triad, diag/gain, diag/cap_any (螺旋链)
- ep_rew/screw vs ep_rew/fshape vs 年金项: 引导不许压过任务核 (U4 承旧)
- sr/gate1..4 + sr_t0 口径 + n/ep_done (分母针, L5-17 纪律)
- 挤奶哨兵 (U29 系三次尸检的教训): 若 Mean Rewards ↑ 而 release/placed ↓,
  先查每步年金总量×回合长 ≈ Mean 否 —— 完成的一次性收益必须 > 放弃的剩余年金。

### T2-4 reference 是 correction 先验，不是零动作答案 (2026-08-30 用户裁定)

- **裁定**：重建轨迹存在穿模/穿孔、IK 偏差和零动作接触失败是可接受的；本任务
  本来就要用 RL correction 恢复物理可行好轨迹，不应先把 reference 修成答案。
- **实测基线**：`smoke_zero --steps 800` 完整通过（静置瓶 0.20cm/盖 0.26cm，
  obs=(4,507)，机器段接触死线 0 误触）；`probe_beta` 的 β=1/1.5/2/3
  在关键段末均为左垫 0、未持住。22 关节顺序已逐名核对且为 identity，排除映射错。
- **剂量**：βL 取 1.0，等于 Screw27_body 原始 squeeze，避免 β>1 对关节构型
  外推；βR 保持 0。探针失败入账，不阻塞 v2 或训练。
- **硬闸重定义**：`probe_ikcheck` 的 1cm/10° 坏行只作诊断；正式入口仍要求
  probe 静置、cuRobo 机器段、Isaac v2、有限数组和剂量/哈希一致。
  `probe_acceptance` 改写 `unscrew_trainability_v1` 凭据：零动作全链记录滑移、
  释放和终点误差，但只以 NaN/Inf、状态/速度/力发散为失败，要求稳定环境≥3/4。
- **最终物理正确性**：仍由 G1→G4、真实接触门控螺旋、escort 判据和独立 eval
  约束；放宽的是 reference 前置门槛，不是成功判据。

### T2-5 v2/训练/部署闭环 —— ✅ (2026-08-30)

- **v2 产物**：316 行，段长 81/25/103/25/82，MD5
  `0d78d12af9c426a629f96c570ae88983`。`meta_v2` 记录位置坏行 172、姿态坏行
  130、关键窗坏行 15；这些是 correction 基线，不是训练门禁。
- **IK 诊断**：右臂 94/103 坏行（关键窗 5），左臂 78/103（关键窗 10）；
  全数组有限，探针按裁定返回通过。
- **可训练性凭据**：4/4 环境有限且不发散；最大垫力 3.846–6.911N，最大物体
  半径 1.164m，最大关节绝对值 3.0711rad。零动作成功 0/4、滑移/终点偏差
  均完整写入 baseline，只作诊断。正式入口和现场世界指纹均通过。
- **PPO 实跑**：HYB、OBJ 都用 4 env × 32 horizon 完成 rollout 和一次优化更新，
  均产出可加载 `best.pth`；最终 checkpoint 的 342,871 个张量值全有限，
  TensorBoard 在 Isaac 关闭前 flush，world.json 与事件文件均非空。RSI 解锁
  交互冒烟另实测 HYB 的 leash/pen、OBJ 的 adv/leash/bonus/slope 非零，
  两者都有螺旋转角/释放诊断。
- **空分母修复**：首轮没有认证样本时 `sr/cert_pass` 原始值继续用 NaN 表示
  “未观测”，日志器点名跳过；课程 EMA 把未观测率保守映射为 0，避免 NaN
  永久污染解锁状态。离线回归与真实 PPO 均已证实。
- **部署**：启动器/验证器使用仓库相对路径、LFS/母带/凭据硬检查、用户独占
  Isaac 临时目录；`docs/DEPLOY_UNSCREW.md` 给出目标服务器安装、验收和
  HYB/OBJ 发射命令。

### T2-6 交接审计: 绿灯下的三层病 + 真实螺纹副落地 (2026-08-31)

**起因**: 用户要求复查上一轮交接 (`0c545894`) 的搭建情况。审计结论 = 代码
本身大多正确 (finite_rate/writer.flush/yml 去绝对路径/rest_anchor_T 逐位同),
但**判据被整体调软之后, 全链的"绿"不再含信息**: `probe_acceptance` 只查
NaN/发散, `probe_ikcheck` 只查有限 —— 这两条对"瓶已经躺在地上"的母带同样亮绿。
凭据自己记着的基线是: 左手滑移峰 43~47cm、瓶终态倾 90° (倒了)、拧角 0°、
盖终点偏 56~86cm、站位垫 L1/R0 (两只手都没抓上)。T2-4 "reference 不必零动作
成功"是用户裁定, 保留; 但**下面三层是"策略根本学不会"而不是"参考不完美"**。

#### 病一: 参考的臂行不可达 (不是噪声, 是运动学不可能)
逐行 FK 实测 (离线, ArmIK): 右臂对**自己的**腕目标中位差 44.7cm, 103 行里
100 行贴在关节限位上, 搬运段整段冻在同一构型 (ia50 与 ia102 的 FK 逐位相同)
—— 而盖参考自己飞到桌上。这与用户要的护送判据直接冲突: 照这条母带,
`placed` 永远立不了。三条根因, 全部离线可证:
1. **腕自转被过约束**。右腕绕螺轴的自转照抄人腕四元数流; 人前臂随手 270°,
   机器腕 j7 行程只有 143°。实测: 只解位置不管姿态, 搬运段 103 行**全部
   0.0cm 可达** —— 不可达的从来只有姿态。
2. **演示的自转在脱离后没有任务含义**。用户提供的数据事实: 盖的**旋转**
   置信度低 (位置高), 且脱离前盖与瓶同体。判据侧 `placed` 只看轴倾角
   (`_axis_tilt`), 绕自身轴的自转不可观也不可判 —— 它是**规范自由度**。
3. **IK 流水线三处自伤**: 失败解当下一行热启种子 (一次解崩拖垮整段) /
   "最近达标行顶替" 会跨分支跳 / 解完再 σ=1.5 高斯平滑把不同分支平均掉
   (实测左臂 0.14cm 的解平滑后差 9.3cm/62.7°)。

**修** (全部在 `make_reference.py`, 离线可复核):
- 右腕自转 = 解出来的量: 拧盖窗一个**常量抓握自转** (扫描, 刚性抓握),
  脱离后**逐行限速 6°/行 漂移**, 手与盖一起转 (不滑手);
- 盖脱离后姿态改为 **rigid ride** (跟着手走, 位置仍用重建);
- 瓶身自转在脱离后**冻结** (只保留轴的摆动, 平行输运实现 —— 逐步剔局部 z
  增量再积分会让轴跑偏, 单元测试实测轴差 0.74, 已在函数里断言拦住)。
  演示里人拧完还在继续转瓶 (末行相对脱离行 101°), 左手抓点跟着绕到瓶另一侧,
  左臂要"绕过瓶子"才跟得上 —— 这是左臂搬运段 6~21cm 不可达的真凶;
- 左抓方位扫描把**站位行可达**升为硬条件 (原来只看全程平均, 选出来的方位角
  站位行差 5~19cm —— 手根本没到瓶上, G1 三垫永远不成形, 就是凭据里
  "左垫 1 个接触" 的来源), 网格 30°→15°;
- IK 解算: 逐行热启 + 坏行**冻结**(不跳分支) + 连冻 2 行放行重新锁定 +
  20°/行限速 + σ=0.5 轻平滑后**重投影**(只在更准且不跨分支时采纳);
- `UNSCREW_HOLD_YAW` (U35c 持瓶朝向重定向) 逐 clip 标定并落进 `task_config`。

**结果 (clip32, hold_yaw=-35°)**: 右臂达标 12.6%→**77%** (全行中位
14.61cm→**0.25cm**/0.6°, 90 分位 3.18cm, 最大逐行跳变 173.8°→32.4°);
左臂 66%→**100%** (中位 0.09cm/0.2°, 0 冻结行, 0 贴限行);
左机器段 pregrasp 锚 7.82cm/53.6°→**0.37cm/0.1°**。
盖末行倾角由重建的 107° 变为 rigid ride 的 2° (自转不可判, 判据只看轴倾角)。

#### 病二: 假摩擦 —— "碰一下瓶盖就自己脱落"
本仓库的螺旋抽象仍是旧口径: 接触门 `screw_drive_gain` (有接触就白给转动)
+ `screw_omega_damping=0.9` (ω 每子步衰减 ≠ 力矩阈值)。老方法线 (worktree
`RLC-dynline`, `LEDGER_unscrew` U40) 已按用户裁定"最接近真实情况建模"修过,
**本仓库没有落实** —— 现在移植:
- 咬合期盖换**球形重惯量** I_eff=5e-3 kg·m²: 指尖→盖的可传扭矩由 PhysX
  摩擦锥真实裁决 (≤μ·N·r, 捏得紧才传得多), 脱扣即还原实物惯量;
- 解析螺纹阻力: 静锁 breakaway 0.04 N·m (已破封的松盖量级) + 库仑 0.015 +
  粘滞 0.03 (τ=0.075 → 稳态 2 rad/s ≈ 人手拧速), |ω|<0.05 且 τ<阈值 回锁;
- 力矩估计三条防幻影 (老台账三次带毒尸检的教训, 逐条移植): 只看盖侧 /
  按矢量差分再投影 / **零接触即强制归零**的真值门;
- 反作用扭矩回瓶身 (左手必须抗扭); `max_angular_velocity` 退化成 4 rad/s
  安全夹; drive_gain 仍逐步计算 (它是观测里的接触特征, obs 507 维不变),
  只是不再乘进角速度。
- 参数进 `clips.py::_unscrew_take`, `ScrewSpec` 扩字段, 体制自检 ⑦ 断言
  整条参数流进 runtime spec (缺一个就退回假摩擦口径)。
- 新增 `B_SmokeTest/probe_thread.py` (H40.0 五段物理探针: 静置不转 / 阈下
  锁死且估计器校准 / 阈上解锁且稳态转速对得上解析值 / 撤力回锁不回退 /
  持续阈上拧满脱扣) —— 训练前必过。

#### 病三: 成功判据只拦住了"甩下去", 拦不住"慢慢松手"
T2-3 的过带单事件判据只能抓"无接触快速穿过放下带顶"。老方法线 U41 的同一条
用户裁定其实是三件: 还要**真的拿过** (释放后 ≥2 垫触盖累计 ≥10 步) 和
**放得住** (释放后单步降幅峰值 <4cm/步 = 0.8m/s)。移植后 `CRITERIA_SCHEMA`
3→4, 变体自检从 5 件加到 7 件, 并逐条断言**是哪一条**拦下的:
⑤ 脱手自由落体 → 过带; ⑥ 无接触慢沉 (降幅 <ESCORT_FALL, 过带抓不到)
→ 持盖步数; ⑦ 拿着但半途 6cm/步 坠落再接住 → 降幅峰值。

#### 其它收口
- `reference_planning_digest` 收窄到规划**真正消费**的量 (machine_pre/station/
  末行障碍位置); 原实现把整段交互行一起哈希, 与自己的 docstring 矛盾, 逼出
  无谓重规划。machine_pre 一旦变 (本轮就变了) 仍照样触发重规划。
- `probe_acceptance` 保持不阻塞 (T2-4), 但凭据新增 `trainability` 台账并把
  告警显式打印: 站位垫不足 3 / 右手零接触 / 回放拧角 ~0 / 零接触却有传入
  力矩 (幻影探测器) / 母带冻结行过多。绿灯从此有信息量。
- `launch_remote.sh` 的 `RL_ISAAC_NO_GUARD=1` 改成可覆写 (共享机要排队闸);
  `wandb_writer` 的非有限标量按 tag 只报一次; cuRobo yml 相对路径按仓库根
  解析 (不再依赖 cwd)。

#### 假设与证伪信号 (DESIGN_LOOP 台账, 训练开跑前登记)
- **H6.1 (参考可达性是 G1 的前提)**: 站位垫 L≥3 且 `sr/gate1` 在 +2M 内破 20%。
  证伪 → 左抓 prior/β 还有几何错, 回头查 pregrasp 内点构型与 squeeze 剂量。
- **H6.2 (真实螺纹副下拧转必须靠捏紧)**: `diag/screw_tau_mNm` 的分布随训练
  上移, 且 `diag/cap_any`≈0 的步上 `screw_tau_mNm`≈0 (幻影探测器)。
  证伪 (零接触仍有力矩) → 又漏了一条数值通道, 停训先修。
- **H6.3 (breakaway 0.04 N·m 不是天花板)**: +8M 时 `screw_tau_mNm` 95 分位
  ≥30。证伪 → 降到 0.025 或加指压课程 (老台账 H40.1 同款处方)。
- **H6.4 (反作用扭矩不砸左手)**: `term/D4_slip`、`term/D1_drop` 不高于本轮
  基线 2×。证伪 → `react_on_bottle` 降权或左手 squeeze 加量。
- **H6.5 (U41 三件不误伤)**: `diag/carry_steps` 在 release 后能涨到 ≥10,
  `diag/fall_peak` 中位 <4cm/步。证伪 (carry 恒 0) → 说明策略拧开就撒手,
  是 r_chold 类的持盖年金缺失, 而不是判据错。

### T2-6b 机器段"张开手"纪律 (2026-08-31 冒烟实测)

**现象**: 参考可达性修好之后, 零动作冒烟第一次报 `机器段死线误触 = 1`
(铁则 0)。原来的报告只写"env 侧终止", 查不出是哪条 —— 先给 env 侧死线加了
**失败码** (10/11=D1掉瓶/掉盖, 20/21=D2倒, 30/31=D3偏, 4=D4滑移), 冒烟直接
打印死线名。再跑, 诊断是 `D2瓶倒 @row≈101` (缝1 段)。

**根因**: 手指合拢的时机。母带原来在 Approach 的**后 30%** 就把手合成抓握形,
缝1 的 25 行全程保持闭合 —— 而缝1 正是"从 pregrasp 净空横扫到抓握位"的一段。
净空从 5cm 提到 20cm+抬10cm 之后, 闭合的手一路扫过去必然把瓶碰倒。
更要命的是: **cuRobo 规划机器段时手指是锁在站姿那一组的**, 所以"approach 里
就合拢"本身就与规划的避障背书自相矛盾 —— 规划说安全的那条路, 母带并没有照着走。

**改**: 机器段全程保持站姿指形 (= 规划锁定的那组); 合拢挪到缝1 后 60%
(前 40% 先把位置靠到位, 到位了再合手)。纪律: **机器段的任何自由度都必须与
cuRobo 规划时锁定的值一致**, 否则那段的"无碰撞"背书是假的。

### T2-6c ★左抓 prior 的**物体系原点错配** —— 一直卡着 G1 的真凶 (2026-08-31)

参考可达性修完之后, 左腕 IK 100% 达标 (站位行 0.01cm), 零动作回放到站位行却
**仍然 0 垫接触**。新写 `B_SmokeTest/probe_grasp.py` 做三方对账, 一次照出根因:

- **执行侧全干净**: 关节跟踪最大差 0.19°; `arm_center` 实测与 `env_rest.json`
  记录 0.00cm/0.00°; `ArmIK.fk(实际关节)` 与仿真读数 0.00cm —— 母带被忠实执行。
- **几何侧错位**: 左垫在**瓶坐标系**里轴向 +15.3~+23.0cm, 而瓶总高 19.7cm ——
  手指落在瓶顶上方的空气里; 且左垫比左腕本身还高 3~11cm。
- **根因**: 数据集的瓶 mesh z∈[0,0.197], 刚体原点在**瓶底**; 而 prior
  (Screw27_body, 来自 Dexonomy `bottle_body/scale010`) 的物体系是**居中**的 ——
  它的抓取锚点 z=-2.0cm、接触点 -2.0~+7.1cm 只有在瓶心系里才讲得通。原样搬过去
  = 把左手锚在**桌面以下 2cm**; `_left_track` 的贴桌钳位抬到桌上 2cm 后手掌仍
  插在桌里, 物理把整条手臂顶高 10cm (v1 命令 j4=-96.1°, 实际 -109.4°) —— 于是
  在瓶顶上方抓空。旧验收凭据里"站位左垫 1 个接触"是同一个病的旧版表现。
- **修**: 镜像锚点 z 加上**本 clip 自己 mesh 的 bbox 中心偏移** (clip32 = +9.85cm),
  换 clip 自动适配。修后: 左臂 100% 可达、站位行 0.01cm、**限位余量中位
  25°→51°**, 左抓方位角也重新扫到 yaw*=0°。
- **右手同族**: `HAND_DROP`(腕→指尖垂距) 旧值 0.203 与本手指形不符 —— probe_grasp
  实测五指垫在腕下方 9.8~16.6cm (中位 15.7cm), 于是手停高 ~5cm、指尖悬在盖顶
  上方 6cm、右手全程零接触 (真实螺纹副下 = 扭矩传不进去)。改用实测 0.157,
  并把它挪进 `task_config` (换手/换指形必须用 probe_grasp 复测)。

**顺带修掉的流程漏洞**: `make_reference` 算规划摘要时读的是**磁盘上旧的 v1**,
于是"改了站位/pregrasp -> 旧规划照旧被剪进新母带"不会当场报错, 要等下一次重建
才发现。改成按**本次要写出的**几何算摘要 —— 这次修完立刻就拦住了 (Approach
摘要不匹配, 强制重规划; 重规划后候选 #2 通过, 比修正前少让一档净空)。

### T2-6d 站位抓握: 从"抓空"到"4 垫成形"的五层修 (2026-08-31)

坐标系修对之后, 站位抓握又连挖出四层, 每层都靠 `probe_grasp` 逐行盯瓶实测,
不靠猜。按发现顺序:

1. **手指合拢时机 (机器段)**: 母带在 Approach 后 30% 就把手合成抓握形, 而
   cuRobo 规划机器段时手指锁在**站姿**那一组 —— "规划说安全的那条路, 母带
   没照着走"。改成机器段全程站姿指形。(见 T2-6b)
2. **缝1 是关节空间直线**: 关节直线 ≠ 笛卡尔直线, 手在合拢途中划弧扫穿瓶身。
   改成腕位姿线性插值 + 逐行 IK 的**笛卡尔进刀** (25 行全解出, 零退回)。
3. **边走边合手**: 实测缝1 第 100 行起倾、105 行时左垫已 3 个而左指峰值 **12N**
   —— 手是抓上了, 但那是**推**不是**握**。改成"先到位, 再合手", 并把 env/探针的
   squeeze 加压曲线一起挪到到位之后 (`TC.SEAM_MOVE_FRAC`, 母带与运行时同一分点)。
4. **张开的手比合上的手大一圈**: 用站姿(全 0° 平手)当进刀手型, 右手伸直的指节
   正好压在盖顶 (row86 起倾、指垫 0 接触 = 撞的是指节), 左手则扫瓶 (row99)。
   prior 自带的 `pregrasp` 阶梯其实是"同一杯状手型沿进刀轴后退 2cm", 从不摊平。
   定稿: 缝1 三段 = **预张开 (沿合拢方向反推, 逐关节封顶 25°) → 杯状手进刀 →
   到位合拢+加压**。
5. **径向差 1~2cm**: 钉住瓶 (`--pin_bottle`) 单独量最终抓握 —— 指垫落在瓶系
   径向 4.2~5.6cm, 而 prior 记录的接触点在 3.2~3.6cm 的瓶面上。差这 1~2cm,
   手指就在瓶身**上部** (+15cm, 力臂长) 斜推, 0.53kg 的自由瓶必倒。
   加 `TC.PRIOR_RADIAL_TRIM` 径向收紧: 0.014 过头 (拇指 34~45N 顶死), **0.007
   定档**。

**定稿实测 (clip32, βL=1.0)**: 站位行 **左垫 4/5**、全程瓶不倒 (末倾角 10.6°,
D2 死线 30°)。βL 复标: 1.0 → 4 垫; 0.6 反而掉到 2 垫 —— 旧台账"β=1/1.5/2/3
全不持住"是在**锚点错 10cm** 的母带上量的, 已作废。

**已知瑕疵 (交给 RL 残差, 不再手调)**: 左拇指 45N 偏硬 (右手 prior 按名镜像到
左手时拇指最不对称); 右手站位 0 垫接触 (βR=0 是设计: 三指精捏交给人手指流+残差,
但真实螺纹副下"没接触=传不进扭矩", 训练里要盯 `diag/cap_any` 与 `screw_tau_mNm`)。

**Retreat 的兜底**: cuRobo 在 "pregrasp -> 站姿" 这个方向上正反都解不出来
(空世界也失败 ⟹ 求解问题不是碰撞; 两端微动探针都 ✅), 而 Approach 用同一对
构型一次就通。三级兜底: 正向 → 反向+时间翻转 → **复用 Approach 路径倒放**
(来源写进产物 `derived_from=approach_reversed`; 撤退世界只少了桌上那顶盖,
而撤退是向上向后离开)。

### T2-6e 铁则的**范围**: 机器段 vs 缝1 交接段 (2026-08-31 裁定)

抓握几何全部修对之后 (站位 4 垫、瓶不倒), 零动作冒烟仍报 1 次 `D2瓶倒` ——
位置固定在**缝1 的合拢段**, 且只有 t0 (从头播全链) 那个出生点会中招; 站位/g2/g3
出生的 env 全程稳定 3~4 垫。逐层试过的缓解 (径向定档 0.007 / squeeze 逐关节
封顶 20° / 合拢从 7 行拉到 16 行) 都只把它推后, 拿不掉 —— 0.53kg 的细高瓶
(19.7cm 高、6.5cm 径) 在 13~17cm 高处被合拢, 只要指力有一点不对称就会倾。

**裁定**: 铁则的范围收回它本来的定义域。
- `smoke_zero` 开头写的就是"C 机器段死线零误触 (**approach 期** D1/D2/D3/D5
  不得响)", 而实现把范围写成了 `row < IA0` —— 把缝1 也算了进去。
- 机器段 (row < APP_END) 是 cuRobo 带碰撞背书的一段, **那里碰到物体 = 规划或
  母带出了错**, 铁则 0 误触不变。
- 缝1 是"手主动去接触物体"的交接段: 有接触是设计内的, 而且**手指与臂的残差在
  缝1 对策略开放** (`gate_rows[APP_END:RETREAT0]=1`) —— 零动作把瓶碰倒正是
  correction 要学的第一课, 不是环境搭错了。
- 缝1 的碰倒**单独统计并打印** (`缝1 交接段死线 = N`), 不并进铁则、也不消失。

**证伪信号 (H6.6)**: 训练里 t0 出生的 `term/D2_tilt` 占比应随训练下降, 且
`sr_t0/gate1` 能起来 (说明策略学会了轻合拢)。若 +5M 仍 ~100% 死在缝1, 说明
残差界不够或合拢参考本身不可救, 那时再回头改 prior 手型/βL, 而不是现在瞎调。

### T2-7 本轮收官: 环境可训练, 剩下的是任务难点 (2026-08-31)

**链条实测状态 (clip32, 全部本机跑通)**
- `probe_rest` 静置 → v1 (316 行, app81/seam1 25/ia103/seam2 25/ret82)
- cuRobo 双机器段: Approach 候选 #2 通过 81 行; Retreat 走"Approach 倒放"兜底
- `smoke_zero --steps 450`: A 静置对账 瓶 0.20cm / 盖 0.25cm, obs (4,507),
  **机器段死线 0 误触 (铁则达成)**, 缝1 交接段 1 次 (记账项), ✅ 预检骨架通过
- `probe_thread` 真实螺纹副五段: **全部通过** (阈下 20mN·m 锁死且估计器读数
  20.0; 阈上稳态 ω=1.44 rad/s vs 解析 1.50; 撤力回锁不回退; 拧满 270° 脱扣)
- v2 重铸: 交互 IK 坏行 **29/206** (codex 版 172), 关键窗坏行 **2** (原 15),
  冻结 R27/L0 (原 R95/L30), 最大 13.3cm (原 52cm)
- `probe_acceptance`: 4/4 稳定 ✅, 凭据绑定 v2 md5 + 完整世界指纹, 可训练性
  告警逐条点名 (站位垫/回放拧角/幻影力矩)
- 训练冒烟 HYB: warmup 结束 → actor 解冻 → 多轮 PPO 更新 → `max steps achieved`,
  产出 best.pth 与 TB 事件; 非有限标量按 tag 只报一次的修复生效

**还没解决、但已定性的一件事**: 零动作从 t0 播全链时, 缝1 的抓握瞬态会把瓶碰倒
(约 row 95~105)。已知它**不是**环境搭错:
- 机器段 (cuRobo 背书) 零误触; 站位/g2/g3 出生点全程稳定 3~4 垫、G1 正常成形
  (冒烟里 g1 出生 env 拿到 +3.0);
- 缝1 的手指与臂残差对策略开放, 这一段本来就是 correction 的学习对象;
- 逐层试过并留档的缓解: 径向定档 0.007 / squeeze 逐关节封顶 (20°、45° 都会把
  抓握一起掐掉, 最终设 180°=不生效) / 合拢从 7 行拉到 16 行 (反而在进刀段末尾
  就碰上)。这一段的净空只有约 1cm, 参数一动就翻 —— 继续手调收益很低。
- ⚠ 一条方法论教训: 早期"SEAM1=25 不倒且 4 垫"的结论是在**占位机器段**下量的
  (那时手臂几乎已在站位, 根本没有进刀过程), 换成真 cuRobo 机器段后不成立。
  **任何抓握结论都必须在真机器段下复测**。

**下一步的两条候选 (留给下一轮, 不在本轮范围)**
1. 让 cuRobo 规划**到抓握位姿本身**而不是停在净空 pregrasp: 最后一段把瓶从
   碰撞世界里排除 (手本来就是要去碰它的), 这样进刀路径由规划器保证无碰。
2. 或者接受 t0 出生的高失败率, 用 RSI 课程从 g1 起步 (现成机制), 待策略学会
   轻合拢后再解锁 t0 —— 判读针 H6.6 已登记。

**附: Retreat 规划失败的一条待验假设 (记录, 本轮不追)**
cuRobo 在 start=machine_pre 时**正反方向、连空世界**都解不出来, 而同一个构型
作为 **goal** 时一次就通 (Approach)。这与 T2-2 的发现同族: cuRobo 对**起点**
的限位余量校验比对目标更严 —— 收缩 3° 的内点解够当 goal, 未必够当 start。
若下一轮要根治 Retreat, 先验这条 (把 machine_pre 再往内收几度当起点试)。
另: 当前 Retreat = Approach 倒放, 其碰撞世界里**没有**桌上那顶盖;
`training_reference_issues` 分辨不出这一点 (只查 "curobo:" 前缀), 产物里的
`derived_from=approach_reversed` 是唯一凭证。

### T2-8 四段结构与"最后几厘米": cuRobo 到不了抓握位形 (2026-08-31)

**用户复述的结构**: 初始位置 → cuRobo 到**操作位置** → 操作 → cuRobo 回初始位置。
照此尝试让机器段直接规划到操作位置 (交互首行的双臂关节角), 结论是**结构性的**:

- cuRobo 用保守的碰撞球模型。只要被抓物体以任何有意义的尺寸留在世界里,
  "手指贴在物面上"的目标位形必被判 goal in collision。
- 逐条试过并留档: ① 排除两个目标物体 (桌子留着) → **桌子**把目标判碰
  (操作位置离桌仅 ~8cm, 手部碰撞球比实物保守); ② 物体收缩 12mm + 排除桌子
  → 仍判碰; ③ 空世界 → 通过, 但那等于这一腿完全没有避障, 且 cspace 直线在
  关节空间会划弧, 反而比笛卡尔进刀更容易扫到瓶。
- 这就是机器人学里的常规分工: **运动规划器送到 pregrasp, 最后几厘米是
  guarded move**; 抓握位形由抓取规划器/先验负责, 不由运动规划器验证。

**落地形态 (自动降级, 换 clip 自适应)**
```
① 站姿 → ② cuRobo 满障碍 → 净空点 [有碰撞背书]
        └ 最后 16cm: 笛卡尔直线进刀 + 先到位再合手 [guarded move]
③ 交互段 103 行 (RL) → ④ cuRobo → 站姿
```
为此新增并保留的能力: worker 支持**负充气 (收缩)** 与 `--exclude_table`,
cspace 分支接上 `--exclude_objects`, 规划驱动支持**两腿拼接**。第二腿一旦
规划成功, `make_reference` 会自动认出"机器段终点 = 操作位置" (逐关节 <2°),
缝1 随即退化成"臂不动、只合手指"; 失败则原样退回 guarded move。换 clip /
换手时若几何更宽松, 这条更好的路会自己走通。

**顺带修掉的坑**: `--obj_inflate 0` 时 worker 把**相对路径**交给 cuRobo,
cuRobo 会去自己的 content 目录里找 (报 "string is not a file: .../curobo/
content/assets/<相对路径>")。改成一律传绝对路径。充气>0 走临时文件是绝对
路径, 所以这条坑只在 inflate=0 时暴露。

### T2-9 首训尸检: 课程死锁 (2026-08-31, 8.5M 步零信号)

`UnscrewHYB_20260831` 跑到 8.5M / 514 epoch 的读数: `Mean Rewards` 恒 -10.00
(死线罚), `sr/gate1..4` 与 `sr_t0/gate1..4` **全 0**, `prog/ep_t0_frac = 1.0`。

**根因 = 渐进 RSI 的解锁条件把自己锁死了**:
- 出生表默认只有 `t0` (`POUR_UNLOCK` 空);
- 解锁 g1 出生点要求 **t0 口径**的 `sr_t0/gate1` EMA ≥0.30 且持续 20 窗
  (`train_task.py`);
- 而 t0 会死在缝1 的合拢瞬态 (T2-6e) → G1 永不响 → 课程永不解锁 → 永远只有 t0。

这直接推翻 T2-6e 里"靠 RSI 从 g1 起步"的说法: **g1 起步本身被 G1 把着门**。
那条渐进解锁是 Pour17 的遗产 (那里 t0 够得着 G1, 且解锁太早会被假阳性带偏,
见 L5-8); 本任务的 t0 够不着, 于是同一条纪律变成死锁。

**修 (用现成机制, 不改判据)**: 发射时带 `POUR_UNLOCK=1,2,3` 直接开满出生点
(README 早有此用法), t0 仍占 20% (`p_t0=0.2`)。RSI 本就是标准做法; 这里只是
把"渐进解锁"换成"一开始就给全"。

**证伪信号 (H6.7)**: 开满出生点后, `sr/gate1` 应在 +2M 内破 20% (g1/g2 出生点
本来就站在已成形的抓握上)。若仍为 0, 说明问题不在出生点而在交互段本身
(判据原料/奖励结构), 那时才该回头动判据。
**配套观察针**: `sr_t0/gate1` 单独盯 —— 它衡量"策略学没学会自己合拢",
是 T2-6e 那条未解项的直接判读针。

### T2-10 右手抓握重定向 (2026-09-01 用户裁定"右手没做重定向? 必须马上做")

**病**: 左手有 GraspPose prior (Screw27_body 镜像) 把"人怎么抓"翻译成这只手的
关节角; **右手一直是空的** —— βR=0, 指形直接用人手指流。人手在拧盖窗内合拢中位
仅 +1.0°, 配人手腕位/指长够用, 搬到 SharpaWave 上就成了"手张在盖外围一圈":
probe_pinch 实测三指落在盖系径向 6.4~11.2cm (盖半径 1.75cm), 合到 45° 仍零接触,
且食/中指越合越往外。零接触 ⇒ 真实螺纹副下扭矩传不进去 ⇒ `ep_rew/screw` 恒 0
(首训 8.5M 步实测), 任务核心根本没开始。

**做了什么**
1. 接上仓库里本就有的 `Screw27_cap_candidates` (15 个, 与左手 prior 同格式)。
   ⚠ 来源是 `bottle_cap_sharpa_wave_**left**` —— 是**左手**抓法, 给右手要**镜像**
   (与左手用右手约定的 prior 时镜像方向相反); 不镜像时指垫落在盖的另一侧。
2. 物体系原点修正 (盖 mesh bbox 中心, +0.85cm) —— 与左手 T2-6c 同一个坑。
3. 新工具 `probe_capgrasp.py`: 一次会话里遍历候选, 自己解腕 IK + 套该候选指形 +
   钉住瓶, 直接**量接触** (选候选不能看 IK 可达率: 可达 ≠ 捏得住)。
   支持 `--fit` **闭环对准** (量到指垫实际位置 -> 把腕平移到"拇/食指垫中点落在
   盖心、盖半高" -> 重解 IK -> 再量) 与 `--curl` 逐级捏合。
4. 新工具 `probe_pinch.py`: 站位行逐级捏合, 报三指接触/指力/组件扰动。

**实测结论 (全部入库为定档值)**
- 15 个候选无一"既可达又位置对": `8_0_calib` 唯一 IK 0.01cm 且径向对准盖轴,
  但高 3.5cm; `47_6` 高度正好但径向 3.8~10cm; `3_1` 位置接近但 IK 差 5.3cm。
- 闭环对准两轮收敛, 累计平移 `CAP_GRASP_TRIM = (-0.3, +0.7, -5.9)cm` ——
  先验的腕高对这只手偏高约 6cm。
- 对准后跨距仍比盖径大 ~1.8cm, 捏合 25° 时**首次接触** (三指 1/3, 峰值 5.81N)。
  `CAP_PINCH_DEG = 25`; 剩下的收拢交给 RL 手指残差 (±68.8° 总量)。
- 母带站位腕位与对准目标吻合到 **0.2mm** (离线核验), 固化正确。

**未了**: 整链回放到站位时指垫读数 (r5.5~9.5) 与直接指令到位 (r2.7~4.4) 不一致
—— 差在"回放执行"而非"母带几何"。下一步先查这一段的实际到位情况
(嫌疑: 缝1 合拢瞬态的接触扰动 / 右臂分支不同)。

**顺带**: `task_env` 的右手 squeeze 前馈仍写死 `beta_r * zeros(22)`, βR 提到 1.0
也不生效 —— 要接上候选的 squeeze 增量 (下一轮一并修)。

### T2-11 拧开角 0.75 圈 -> 30° (2026-09-01 用户裁定)
"瓶盖转 30 度就能拧下来, 0.75 圈(270°) 太多了" (先说 50, 复核后定 30)。
实物是已破封的松盖。连带影响: 螺纹脱扣阈值、`assembly.turns` 进世界指纹
(CRITICAL) 故验收凭据要重出; 任务难度大降 —— 30° 在准静态稳态 1.5 rad/s 下
只需约 0.35 秒持续握持 (原 270° 要 3 秒且机器腕行程不够, 得换把)。

### T2-12 左手先横瓶、右手后找盖 (2026-09-01 用户视频裁定)

**现象/原始数据**: 当前 v48 回放里右手从第一幕就向瓶盖运动, 与示范顺序相反。
clip32 原始 15fps 数据中, 瓶身相对起转姿态的倾角为 f34=86.1°、f35=87.6°;
右掌稳健代理 (腕点坏道, 取掌根 0/5/9/13/17 均值) 到 f32 仅漂 0.82cm,
f35 才出现明确位移。`scene_layout.json` 的右侧接触起点 f34 与此相邻。因此将
**f35 / 约 87°** 定为右手明显启动的诊断锚；轨迹本身仍从 P1/f22 连续消费。

**最终结构拍板 (用户继续澄清)**: 有全任务共用初始位 `P0`，也有本条数据
轨迹的首姿 `P1`。唯一合法分段是:

1. `P0 -> P1`: cuRobo Approach，双臂一起到本条轨迹首姿。
2. `P1 -> task_end`: 一条连续的原始数据参考 + RL 残差，包括左手转瓶、
   右手前段的小幅运动、瓶水平后右手找盖、抓盖、拧开与放置。
3. `task_end -> P0`: cuRobo Retreat，从任务实际末行回全局初始位。

`P1` 后不得插入任何独立右手 cuRobo 段，也不得把右手硬冻结到 f35。
f35 只是原始数据里“右手明显启动”的诊断锚；数据本身在 f35 前近似不动，
但那些微动仍应保留。右掌从 P1 到盖的整条曲线使用端点受约束相似变换
重定向到机器人工作空间，保留弯曲和快慢，不用直线标量插值代替。旧 v48 及本轮
中间曾试的 `RightApproach` 结构都作废，不续训。

**必要的判据同义修正**: D4 左腕-瓶基线仍在 G2 建立; 右腕-盖基线不得同时建立,
而要在右手实际形成盖接触后单独建立。否则右手从远处合法接近盖时, 距离变化会被
当成“抓后滑移”终止。这不是放宽滑移阈值, 是让基线事件恢复其原本的抓后语义。

**预注册验收/证伪**:
- 离线分段: 只有 `Approach / data-RL / Retreat`; `right_approach` 不存在。Approach
  首末分别精确是 P0/P1，Retreat 首末分别精确是 task_end/P0。
- 离线轨迹: `right_start_src == 35`、启动时瓶倾角 `>=85°`。P1->到盖使用
  完整原右掌弧长/姿态增量，端点精确是 P1 右手首姿和盖 GraspPose；
  f35 前允许与原数据一致的微动，但明显找盖运动必须在左手已把瓶转平后。
- 规划: Approach/Retreat 两个 cuRobo 产物都有当前 P1、task_end、末态障碍和
  `env_rest` 指纹；任一边界短腿规划失败时产物必须 `ok=False`，不允许用手写斜坡补齐。
- 零动作回放: 视频中左手先转瓶，右手前段只呈现数据里的小幅运动，瓶近水平后
  才明显找盖并合指；合法接近期间不得触发 D4，机器段不得触发 D1/D2/D3/D5。
- 证伪: 若上述离线时序或零动作视频任一不满足, 不启动长训; 修参考/规划, 不用
  reward shaping 掩盖。只有视频、逐行 trace 与短 smoke 都通过后才能发新版本。

### T2-13 持瓶指垫摩擦 3.0 -> 6.0 (2026-09-01 用户裁定)

最终 v2 的强制时钟物理回放在 P1 有左垫 4/5，但随横瓶动作瓶身从左手滑出；用户
明确要求瓶最好不滑、可以适当提高摩擦。只把 10 个 elastomer 指垫的 SuperGrip
从 3.0 提到 6.0；瓶身材质仍为 0.5、桌面仍为 0.5、掌背仍为 LowGrip 0.2。
multiply 合成后指垫↔瓶身有效摩擦约从 1.5 提到 3.0，不把整手或桌面做成粘接。
该值进入世界指纹关键项，训练、监控探针和验收必须一致；修改后重出验收凭据。

### T2-14 v2 局部 IK 分支 + 时序化验收针 (2026-09-01)

**病因**: v1 左腕已经与瓶身刚性锚定且连续，但旧 v2 重铸会把 `q_seed`、v1 当前行
和默认姿态三个 IK 结果只按单帧几何误差取最小。同一条平滑物轨因此反复切换冗余
肘解：左臂交互段单步 `>10°` 43 次、`>15°` 32 次。20° 后处理限速只能把一次
支路跳变摊成多行甩臂，不能把它变成连续运动。

**修复**: v2 每行把“保持上一行”作为合法候选，只接纳距上一行最大关节差 `<25°`
的局部解；v1/随机解仍可作初值，但远端分支没有资格参与比较。允许几毫米连续误差
留给 residual RL，不再用甩臂换纸面 IK 精度。最终左臂 `>10°` 降到 4 次、`>15°`
降到 1 次；右臂 `>10°` 2 次。FK 诊断右臂坏行 0/307，左臂 21/307 且集中为
连续的 k110..130 修正基线，不再是全段支路振荡。

**物理结果**: 正常速度探针的持瓶峰值滑移从旧版约 16.7cm 降到 6.5cm，k40 仍有
4 垫接触；四环境全链验收稳定 4/4，峰值滑移 7.6~10.7cm（旧版 14~33cm）。
参考本身不要求零动作完成：右手在启动后接触峰为 `[1,1,2,4]`，真实螺纹传入力矩
峰 233mN·m、拧角峰约 10°；策略要学习维持左手力闭合、增加右手三指接触并完成
剩余 30° 拧开。

**监控修正**: P1 右手本来就应远离瓶盖，验收不再把“站位右垫 0”误报为失败；
改为从原始数据启动点 k39 后统计右手接触峰，并记录全程而非末帧的拧角/扭矩峰。

### T2-15 "慢放救持瓶"假设证伪 + 门控口径下零动作基线重判 (2026-09-02)

**背景**: 上一节点计划"跑完 probe_speed 选最小可用慢放倍率重铸母带"。补跑完整
四倍率后该方案**作废**，且发现此前对"横瓶丢瓶"的判读用错了口径。

**证据 1 — 慢放与丢瓶非单调**: probe_speed 终局 lost_k = 129(3×)/17(6×)/
86(9×)/134(12×)，末态垫数 3/0/0/5。四 env 无质量/摩擦随机化（物理同构），
非单调只能解释为攥紧预载下接触处于临界（肥皂效应敏感区）——回放速度不是
控制变量。**任何全局/局部纯慢放重铸都不做。**

**证据 2 — 立瓶窗的真问题是"参考与攥紧打架"**: 所有倍率在 k110~140（盖脱扣后
把瓶立回竖直的窗口）左臂跟踪误差 17~24°，12× 准静态也一样 → 非 PD 带宽问题。
三个共犯:
- v1 左腕目标在瓶系 k105~130 有 **3.2cm 侧向偏移-回归瞬态**（k115 峰值，k130
  归零；IA 其余行刚性锚 ≈0）——命令腕在死攥的瓶上侧向顶 3cm 再回来;
- v2 IK 同窗残余 ≤2.9cm（T2-14 已知，fail 行 21 全在此窗）;
- squeeze 前馈整个 IA 恒满剂量，左垫合力 53~68N（验收口径）vs 设计饱和 15N。

**证据 3 — in-env 零动作基线其实健康**（最终母带 md5=43b8633e + 摩擦 6.0，
probe_phases --zero 满 700 步）: G1✓ G2✓（含提升认证），时钟走到 **k89 停表**
（瓶下垂 79.7° vs 参考 ~95°，卡在时钟门），L5 垫 53.6N 稳定挂满全程，
零死线误触。此前"横瓶时瓶滑掉 24~43cm"来自**强制时钟**回放——那个口径把
时钟调速器（k 只在两物体贴参考时 +1，臂行=IA0+k 同步停走）短路了，in-env
并不存在该失败。停表→前进的缺口 = 压下垂 ~15°，属预定 RL 学习项（不改门）。

**证据 4 — v48 的退化证据对当前配置无效力**: v48 于 12:36 发射，母带/摩擦
21:34~21:49 才重铸换代；其负奖励（-135→-17）与 4M 探针"k11 僵持后自甩 D4"
均产生于旧配置，不能用来指控新母带。v48 已因 carb mutex 崩溃自然终结。

**裁定**: 母带侧本轮到此为止（T2-13/14 是本轮唯一变量，一次只改一个参数）。
k105~130 腕目标瞬态与 squeeze 剂量剖面**挂起为下一轮候选杠杆**，不与本轮
改动叠加。下一步 = 按交接清单跑官方冒烟 + 有界短训探针读针。

**预注册判读针 (v49 短训, 每条都有明确证伪动作)**:
- `sr_t0/gate2` 存在且 `prog/clock_frac` 随训练上升 → 停表缺口可学成立;
  若 clock_frac 长期钉在 ~0.29 (k89/307) → "压下垂超出残差权限"，杠杆=
  βL 剂量剖面（转瓶段递减），不是改门;
- unlock=3 出生点若高发 D4 且滑移集中 k105~140 → 杠杆=k105~130 腕目标瞬态
  （下一轮把该窗腕目标改为刚性瓶锚），先于 squeeze 动手;
- `diag/cap_any` 上台阶但 `ep_rew/screw` 仍 0 → 贴势刷分（T2-14 预登记的
  回退条件，回退 bonus 系数 5.0）;
- `ep_rew/leash` 若仍以 -100 量级主导单回合 → 奖励侧压制诊断成立，但修法
  留待独立轮次，不与母带变量混判。

### T2-16 换数据 clip 32 -> 17 + 对齐 checkpoint 课程分析 (2026-09-02 用户裁定)

**裁定**: 训练数据改用 unscrew clip 17（榜单第 4：瓶 87/69、盖 79/37 双可用）。
⚠ 盖 rot 置信 37 明显低于 32 的 66 —— 护送段盖姿态更不可信，T2-17 滞回修复
必须先于 clip17 母带构建（已做）。流水线 = data_engine.sh 17 全链重走，
CAP_GRASP_TRIM 等 clip32 标定值首轮沿用，由 probe_capgrasp/pinch 诊断后再定改否。

**用户课程提案（Success Checker 三段）与分析**:
1. **A 段"对齐 checkpoint"**（左手转瓶 + 右手随人手轨迹到盖 GraspPose）:
   语义上 = "时钟到达 k_sep 抓盖行"（时钟门本就要求双物体贴参考才走，臂行
   同步把右手送到 GraspPose）。已实装 `sr/align` / `sr_t0/align` 记账针
   （出生即 >=k_sep 的 RSI env 由 pre_align 排除出分子）。如需**专练**A 段，
   现成 KCAP=k_sep 截断时钟即可，零新代码。
2. **B 段"到 GraspPose 附近给大奖励"**: 已在线 —— 贴势奖金系数 5.0
   （T2-14 预登记，earn-only 棘轮，单回合上限 ~10 = G3 量级）就是这个奖励。
   不再叠加第二份，等首训判读针读数后再动（防贴近刷分，棘轮之外无保护）。
3. **C 段"冻双腕、手指按 GraspPose 学接触拧 30°"**: 物理可行（真实螺纹副
   吃垫接触力矩，指残差总量 ±68.8° 足够棘轮式拧转；强制回放已实测 233mN·m
   /10° 通道真实）。两条红线:
   - **不许顺序解锁**（"通过率高了再进 C"= T2-9 渐进解锁死锁的原方案，
     8.5M 步零信号前科）。改为 aligned@k_sep 的 RSI 出生点**并行**练 C，
     与 POUR_UNLOCK 机制同构，开训第一步就有 C 段样本。
   - **冻腕只对该出生点的 env、且自始如此**。训练中途改动作语义 = 价值
     函数作废。冻腕正面作用: 物理上排除"腕滚假拧"，与 U44/U45 反幻影一脉。

**预注册（clip17 首训判读）**: sr/align 上升 = A 可学；diag/n_triad 与
ep_rew/screw 是否涌现决定要不要实装 C（一次一个变化）。v49 (clip32, 5M 有界)
的同名针作为跨 clip 对照一并归档。

### T2-17 护送段右腕自转滞回 (2026-09-02, 用户视频反馈)

**现象**: 参考视频脱盖后手带着盖乱翻、指垫无法稳定捏住（用户逐帧指出）。
**量化**: 护送段 k150~294 盖累计转 336° = 腕累计转 336°（逐行相等 ——
盖 rigid ride 是无辜的，位置相对腕漂移仅 0.02cm/行）。
**根源**: 脱离后 roll 规范自由度逐行求解是纯 argmin，盖为旋转体、得分对
roll 平坦，数值噪声驱动随机游走（6°/行限速内走满 2.3°/行均值）。
**修复**: 候选得分加 `0.03·|d|·ROLL_STEP` 滞回罚 —— 漂移一步 6° 必须换来
≥3mm 位置误差量级的真实可达性收益；真被限位顶住时罚项远小于收益，不拦正业。
**预注册验收**: 新母带 "[v1] 右腕自转漂移 累计" 打印值应大幅下降（clip32
口径 336°→期望 <60°）；副作用监视 = 右臂可达率/坏行数不得恶化。

### T2-18 四元数序断言在 clip17 上误伤 (2026-09-02, 换 clip 第一类故障)

**现象**: clip17 首铸 make_reference 断言炸: "四元数序存疑 wxyz 0.615 vs xyzw 1.896"。
**诊断** (对照 clip32): replay_world 与 world_fused 的分歧集中在**每条 clip 的
弱重建物体**上 —— clip32 盖差 0.607/瓶 0.000, clip17 瓶差 0.615/盖 0.000
(瓶 recon 仅 11/101 帧)。实测 clip17 两份数据瓶**倾角逐帧一致** (15.5° vs
15.6°), 差全在绕瓶轴自转 —— replay 管线对弱重建旋转体做自转正则化 (与我们
对盖的处理同理), 属良性修正。旧断言只拿瓶身判序, clip32 恰好瓶身是强物体
所以从未触发; 判序应该用"对得上的那个物体"。
**修复**: per-object 求 (wxyz, xyzw) 误差, 取最优者判序; 对 >=0.02 的物体
打印"自转正则化"告知, 物体轨迹以 replay 为准 (原本就是这么消费的)。
**顺带记录**: clip17 f0 瓶倾角 15.5° (非竖直起步), 与 env 静置位 (0°) 的
对齐交给 make_reference 的 rest 锚定; 冒烟 A 静置对账会兜底核验。

### T2-19 clip17 冒烟基线 + v49 (clip32) 5M 终局归档 (2026-09-02 02:10)

**clip17 全链** (T2-18 修复后一次通过): 248 行 = app60/seam1 25/ia78/seam2 25/
ret60, k_sep=35; cuRobo Approach+Retreat 均真规划 (Retreat 首档净空失败自动
换档, 第二腿 81 行)。铁则全过: 静置 0.20/0.32cm、机器段死线 0、缝1 死线 0、
五项自测 + 放音/RSI/变体反向全绿。

**与 clip32 的基线差异 (重要)**: 零动作 **G2 提升认证不过** —— t0/g1 停 k0
只有 G1 (3 垫接触有, 无残差提不起来); g2 出生 t23 即 D4; g3 出生走到 k53。
左手抓握先验/微调全是 clip32 标定值, HOLD_YAW 对 17 还是默认 0。
**裁定 (按"参考不镀金")**: 不先重标定, 直接发首训让 RL 修 —— v49 已实证
cert 从弱基线可学 (OBJ sr_t0/gate2 90%)。
**预注册**: 首训 +2M 内 `sr_t0/gate2` 应显著 >0 且上行; 若钉 0 = 左抓几何
超出残差权限, 回头 probe_grasp/probe_pinch 重标定 PRIOR_RADIAL_TRIM /
HOLD_YAW_BY_CLIP["17"]。`sr/align` (新针, k_sep=35) 同步观察。

**v49 5M 终局 (T2-15 针下归档)**: ① clock 未破 0.29, 但确定性探针左垫力峰
156→100N 下行 (在学"轻压"), 未判死; ② HYB `diag/cap_any` 0.0006→0.0111
上台阶, `ep_rew/screw` 仍 0 (时钟未稳定过 k100, 预期内); ③ OBJ
`sr_t0/gate1/2` = 96%/90% vs HYB 73%/58%, 而 HYB cap_any 高一个量级 ——
**HYB/OBJ 在"左手抓握 vs 右手接近"上解离**, 分段用权 (左手段 OBJ 口径、
右手段保留人手指引) 进下一轮候选。无幻影力矩 (screw_tau≈0.07mNm 噪声级)。

### T2-20 熵奖励在负回报景观下吹飞 σ —— 训练先起后塌的根因 (2026-09-02 晨)

**现象** (clip17 首训 v50, HYB 16.7M/OBJ 14.3M): 2M 时一切健康 ——
`sr_t0/gate2`=62%、`sr/align`=21% (新针首秀即活)、clock=0.39; 之后单调衰减,
14M 后全部归零。确定性探针同步退化: 2M/4M ckpt 过 G2、到 k40、力峰降到 93N
(在学轻握); 8M ckpt 丢 G2、攥到 176N。**最佳策略在 2~6M 窗口。**

**根因**: `losses/entropy` 从 -51 单调涨到 -35 (σ 持续膨胀)。v49 (clip32)
同斜率 (-51.25→-47.35 @5M), 只是 5M 停得早没塌 —— 系统性, 与 clip 无关。
机理: 本任务回报被 leash 负流主导、后期优势信号弱, `entropy_coef=0.001`
的熵奖励压过任务梯度, σ 无界膨胀 → 8mm 认证精度在采样噪声下必崩,
连带 mu 被大噪声策略梯度拖坏。pregrasp 冠军同用 0.001 不出事, 因其回报
以正奖励为主、优势一直强 —— 系数没错, 是景观不匹配。

**修复 (一次一个变化)**: 本任务 `entropy_coef` 0.001 → **0.0**;
σ 下限 sigma_floor=0.02 兜底防塌缩。v50 双子已停 (ckpt 全保留)。
**预注册**: v51 重发后 (i) entropy 曲线应停涨缓降; (ii) gate2 在 2M 达到
~60% 且 **8M 时仍不低于 2M 水平** (v50 的证伪点); (iii) 若 σ 贴地且
align>10% 但 cap_any 长期 0, 说明探索死了, 回退为退火式 (0.001→0 线性 5M)。

**cap_any=0 (T2-19 预注册命中)**: 强制回放右垫 [0,0,0,0] + 训练 16M cap_any=0
→ 按预案跑 probe_capgrasp 给 clip17 重标盖抓取 (CAP_GRASP_TRIM 现为 clip32
标定值), 结果另记。

### T2-21 clip17 盖抓取候选 8_0_calib -> 3_1 (2026-09-02 上午, 实测定档)

**背景**: v50 训练 16M cap_any≡0 + 验收右垫 [0,0,0,0] (T2-19/20 预注册命中)。
probe_capgrasp 修复两处后复测 (①测量构型从"交互首行瓶竖直"改为**抓盖行
k_sep 瓶水平** —— 08-31 尸检的同款坑; ②瓶盖都要钉住, 只钉瓶会让盖掉桌,
对准环节追着掉落的盖跑, 首轮 35cm 腕差即此故障)。
**实测 (15 候选 × fit2 × curl 0/25/35, 瓶水平构型)**:
- 大多数候选 IK 不可达 (残差 8~20cm) —— "IK 可达"是在竖直构型下评的, 换构型即失效;
- **3_1**: IK 0.01cm, 25° 捏合起单三指垫接触 6.6~13.5N, 三指径向 1.3/3.3/4.4cm
  (盖半径 1.75), 轴向全进盖高范围 —— 实测最佳;
- 40_4/40_4_calib* 同族接近; **母带实际用的 8_0_calib = 零接触**;
- 深捏合 (45°+) 反而把指尖甩离 (合拢抬指尖的老几何), CAP_PINCH_DEG=25 不动;
- 仪器缺陷记账: probe_capgrasp 闭环对准无步长夹紧/无回退, 单测时可发散
  (一步过冲 21.5cm), 判读要看整轮扫描不要信单轮。
**根因**: `CAP_GRASP_PICK` 默认钉死 "8_0_calib" (clip32 时代选择), clip17 母带
的候选扫描实为 1×24 只扫了它。
**动作**: UNSCREW_CAP_GRASP=3_1 重铸 clip17 全链 (换候选改 P1 右腕等待位 ⇒
机器段必须重规划); v51 = 熵修复 (T2-20) + 本候选切换, 两变量正交、各带独立针:
- 熵: entropy 曲线停涨 + gate2 8M 不回落 (T2-20);
- 候选: 验收"右手启动后接触峰"应 >0; 训练 cap_any 应脱离 0 (v50 基线 0.0000)。

### T2-22 自制双指姿势首铸的两处穿模 (2026-09-02 下午, 用户视频反馈)

**用户裁定**: 拧盖抓姿改用自制"大拇指、食指双指"(工作台 artifact 手调,
poses/p1788377756522), 由 T2-21 的候选机制接入 (UNSCREW_CAP_GRASP=
user_thumb_index, 编辑器右手盖心系值反演回左手候选约定, 往返校验 0 误差;
构建器滚转扫描 roll=-120° 可达, IK 0.02cm/0.0°)。

**首铸视频暴露两处穿模** (用户逐帧指出, 帧提取证实):
1. **右手非任务三指卷进水平瓶颈**: 工作台只显示盖不显示瓶, 用户把中/无名/小
   指收拢"让开"的方向恰是瓶水平时瓶颈所在空间。冒烟同步印证物理伤害
   (t0/g1 在 k40/k19 env 侧终止)。修复: 三指半开 (FE .15/PIP .25/DIP .15,
   AA 保留), 拇/食不动 —— 忠于双指设计。**工作台建议**: 后续把瓶身网格加进
   编辑器场景, 这类冲突设计时就能看见。
2. **盖埋进掌心**: CAP_PINCH_DEG=25° 是给库候选(张开态)补合拢的, 叠在已
   贴面的自制姿势上=插进盖里 25°。修复: 加 UNSCREW_CAP_PINCH 环境变量,
   自制姿势用 8° (保留物理压力剂量)。
**另records**: 左手放瓶收尾段指尖蹭桌 (腕最低点距桌 6.8cm, 穿的是垂下的指尖) ——
腕必须低才能送瓶到桌面, 抬腕会破坏放置几何; 真实物理是轻微蹭桌 (D5 有容差),
按"不镀金"挂起, 只在 D5/placed 扰动出现时回头处理。
**范围**: 规划摘要只绑臂关节+物体位姿, 手指/捏合修复**无须重规划** (实测跳过)。
**预注册**: r2 视频右手不再穿瓶; 冒烟 t0 不再在 k19-40 死; 验收右手接触峰 >0。

### T2-23 滚转扫描加入桌面净空评分 (2026-09-02, 用户视频反馈第三处穿模)

**现象**: r2 视频右手下方三指强穿桌面。**机理**: 抓盖行右腕目标 z 恒为
TABLE_Z+0.02 —— 即选中滚转角把腕位甩到盖下方、被 _cap_track 的钳位硬拉到
桌面上 2cm; 腕贴桌、指散下垂, 下方手指必然插桌。旧滚转评分只看 IK 可达 +
限位余量, 对桌面净空全盲; 而盖为旋转体, 存在"手在盖上方、指尖朝下捏"的
等价滚转方位 (腕 z≈盖+横向偏移, 指尖最低点仍高于桌面)。
**修复**: 扫描 24 滚转角时新增两项净空分 (排序在可达之后、限位余量之前):
① 腕轨迹真实 z >= 桌面+4cm (不靠钳位救); ② 手内指向 (腕->盖, 滚转不变量)
在抓盖行世界系 z >= -0.35 (不朝下超 ~20°)。选中后打印未钳位最低 z 供核验。
**影响**: 滚转角变 => 右腕整条轨迹变 => P1 变 => **机器段必须重规划** (全链重走)。
**预注册**: 新母带该打印值 >= 桌面+4cm; r3 视频三指不再穿桌; 冒烟/验收随行。

### T2-24 横瓶角度上限 80° (2026-09-02 用户裁定"适度少转一点")

**数据真相**: clip17 演示把瓶转**过头**到 102.9° 峰值 (瓶口朝下 13°), 盖嘴
几乎贴桌 —— 这是 T2-23 "右手被压进盖-桌薄层、可达滚转角腕全在桌面高度"的
总根源。用户看视频直觉提出少转一点, 命中要害。
**实现**: `UNSCREW_MAX_TILT` (make_reference, 默认关): 把超限倾角绕 up×z 转回,
自转/yaw/位置不动; 右手启动判据用原始 tilt 不受影响; 盖行(脱离前=瓶推导)/
腕锚/左握位从同一 body_q 派生自动一致 (U35c 同法)。倾角烤进母带,
训练/验收侧无需知晓该变量。
**实测 (80°, clip17)**: 影响 26 帧; 抓取窗最低腕 z 0.845→0.917 (越过 0.91
净空线, 钳位不再介入); 滚转角 -120°→-105° (净空分首次真实参与选择)。
护送段最低 0.839 仍低于线 (腕在盖下的固有几何+演示携带路径低), 0.89 钳位
兜底+三指已攥拳, 待最终视频检查, 暂记账不加改。
**预注册**: 最终视频抓取段右手不贴桌; 若 80° 仍紧, 降到 75° 是下一档
(每降 5° 盖嘴约抬 1.5cm); 若左手转瓶 IK/物理因此恶化 (селftests/冒烟对照),
回退并改攻护送段。

**T2-22~24 收官 (2026-09-02 17:10)**: 用户看过四项修正齐装的交互段预览
(80° 倾角上限 + roll -105° + 三指攥拳 + 8° 捏合), 裁定"没什么问题"。
交互段几何定稿; 接近/撤退段以真 cuRobo 规划版最终视频自检为准。
**流程教训**: 规划必须在**最终** make_reference 之后跑 —— 任何改动 P1/末行
的参数 (倾角上限、滚转角、候选) 之后先出 v1 定基准, 再规划, 再剪进;
顺序反了规划指纹必过期 (今日实测两次)。

### T2-25 臂行选支加肘/前臂离桌净空 (2026-09-02 17:20, 用户视频反馈第四处)

**现象**: 交互段预览里左前臂平贴桌面 ("机械臂本身有接触")。**机理**: solve_side
选支只看腕位姿误差+连续性, 低位持瓶段选了肘下支。**修复**: 对肘/前臂连杆
(*_arm_l4/l5/l6/l7) 做 FK, 最低点 < 桌面+5cm 按 2.0/m 计罚, 首行随机种子里
选到肘上支, 后续靠 25° 邻域跟随; 双臂同法。改 P1/末行构型 ⇒ 双段重规划。
**流程修正**: Isaac 步骤改为"产物标记出现即收尸" (run_until), 不再等 40 分钟
timeout 收 app.close 卡死的进程。
**用户裁定**: 交互段其余几何 OK, "把没修完的修完" 然后跑。

**T2-25 修正归因 (2026-09-02 18:2x, 用户复核 17:57 版视频)**: 视觉接触在
**右臂/拳握指节** (护送段轻微蹭桌), 左臂视觉无问题。修法: _cap_track 腕 z
钳位 +2cm→+5cm (抓取窗最低 0.917 不受影响, 只抬护送段)。左臂 v2 的 37 行
不可达行为独立问题 (T2-24 倾角上限 ↔ 左臂可达性张力), 正做 HOLD_YAW
per-clip 标定扫描 (0/±20/±35), 按左 IK 达标率+限位余量选档。

### T2-26 clip17 定稿母带 + v51 发射 (2026-09-02 19:45)

**定稿母带** v1 md5=f4f75f4d / v2 md5=bd3f6a0d, 451 行 = app161(真cuRobo)/
seam1 25/ia 78/seam2 25/ret162(真cuRobo, 撤退腿 -2cm 收缩修复生效)。
标定全家福: 用户自制双指盖抓姿 (user_thumb_index, roll -105→随 yaw 重算) +
非任务三指攥拳 + UNSCREW_CAP_PINCH=8 + UNSCREW_MAX_TILT=80 +
HOLD_YAW_BY_CLIP["17"]=-20 (T2-25 五档扫描定档) + 右臂净空罚 + 护送腕钳位
+5cm。质量: v2 交互 IK 0/156 坏行 (最大 0.46cm/1.2°, 余量 L26.2°); 净空
0.993/0.915 双过线; 冒烟铁则 0 误触; 验收 4/4 + **右手接触峰 [2,2,2,2]**
(四环境双指真实接触, 力矩 36 mN·m; 两日轨迹从 [0,0,0,0] 修到满堂红)。
最终视频三段自检无穿模, 用户过目交互段裁定通过。

**v51 发射**: Unscrew17HYB_v51 (GPU0) / Unscrew17OBJ_v51 (GPU1),
POUR_UNLOCK=1,2,3, 双变量 = 定稿母带 + entropy_coef 0 (T2-20)。
**不加碰撞惩罚** (用户提议, 分析后缓行: 负奖励景观两次事故前科、残差毫米级、
D 线已兜底; 若针显示碰撞剥削再作为下一单变量)。监控 2/4/8/12M
(logs/Unscrew17_v51_monitor, 探针带 MONITOR_UNSCREW_CLIP=17)。
**判读针** (预注册): ① entropy 停涨 + gate2 8M 不回落 (T2-20 证伪点);
② sr/align 上行 (v50 基线: 2M 时 21%); ③ diag/cap_any 脱离 0 (v50 恒 0,
现在强制回放已有 [2,2,2,2] 接触底子); ④ ep_rew/screw 首次 >0 = 拧转涌现,
长期 0 → 上 stage C aligned 出生点课程 (T2-16)。

### T2-27 双母带 A/B (2026-09-02 20:30, 用户提议)

**用户裁定**: 修撤退段的同时, 用 preview 观感版轨迹并行训练。构成干净单变量
A/B: 变体A=定稿 (HOLD_YAW=-20, 左臂 IK 100%/余量26°) vs 变体P=preview 观感
(HOLD_YAW=0, 左臂 IK 53%/贴限) —— 其余全同 (双指候选/攥拳/8°捏合/80°上限/
各自真规划双段)。**假设对撞**: 用户押"观感顺=学得好", 我押"左臂可达性决定
学习上限" (clip32 k110-130 病灶的教训)。本轮牺牲 HYB/OBJ 消融 (v50 已证
HYB 优), 改为 A/B 双 HYB: v52 (GPU0, 变体A) vs v52p (GPU1, 变体P)。
**机制**: 共用规范路径先后加载 —— 训练进程发射即把母带读进内存; P 建带期间
A 产物存档 _variantA_park, v52p 读完母带 (以 .out 出现"手指门"为证) 后恢复。
监控/探针只挂 A (探针读规范路径); P 用 TB 针判读。
**预注册**: 同步数下比 sr_t0/gate2、sr/align、diag/cap_any、ep_rew/screw;
若 P 全面占优 → 左臂可达性假设被证伪, 观感/自然度是更好的选带标准;
若 A 占优 → 维持标定流程; 若各有胜场 → 按段混合 (yaw 也可逐段)。

### T2-28 右手侧抓 + 放置摆转 (2026-09-02 23:00, 用户两项视频复审意见)

**用户意见**: ① 右手依然穿模; ② 右臂"整条链从上到下"不自然, 应可水平自然拧。
**诊断** (扫描表 UNSCREW_SCAN_DEBUG): 两项同根因。绕盖轴 24 档扫描里, 用户要的
水平侧抓带 roll∈[-120,-45] 全部可达且余量优 (-90° 余量 32.8° 指向微朝上),
但腕距桌 3.6cm, 被 T2-23"净空≥4cm"硬门槛整带误杀 —— 该规则防的是"指尖比腕低
插桌", 侧抓指尖朝上不适用。选择器只剩唯一过线的 -165° 顶抓: 腕在盖上 3.9cm、
指向下垂 12° 罩盖 => 从上到下链形 + 指尖插盖。
**改动** (一个变量: 抓握方位方案):
- make_reference 增 UNSCREW_CAP_ROLL (钉死侧抓 -90) / UNSCREW_PLACE_ROLL (-165)
  / UNSCREW_SCAN_DEBUG。
- **放置摆转**: 侧抓腕在盖轴下 3.4cm, 放盖段刚性偏移会把腕压到桌下被 z 钳位
  截断 (盖自沉出指间 8cm)。脱盖后绕盖轴以峰值 5.6°/行 (20 行 smoothstep) 把
  抓握方位从 -90 摆到 -165 —— 即人"拧完翻腕盖口朝下再放"; 盖是旋转体, 摆转
  对盖只是自转 (本就不可判), 放置几何回到与 T2-25 定稿完全一致的已验证形态。
**scratch 实证** (v1_side md5=d966d9f2, 不动正典/不扰 v52 双训练):
拧盖位可达余量 5.2°→57.3°; 搬运/放置 IK 100% (余量中位 10.9°, 贴限 0 行,
成功穿过 -135/-150 不可达口袋); 右腕全交互段距瓶轴 ≥8.5cm (数值核查无手-瓶
穿透); 录像自检: 侧捏盖端形态正确、左手扶瓶/右手放盖收尾正常。
**预注册针** (转正条件, 全过才升级 v53 正典):
① probe_capgrasp --row 在新方位: 接触 ≥[2,2,2,2] 量级、真实力矩不低于 36 mN·m
   同量级 (换方位后重力相对手系变了, 必须重认证);
② 摆转 20 行在 v2 物理 IK 下坏行 0;
③ probe_acceptance 4/4;
④ 用户过目侧抓视频裁定通过 (logs/unscrew17_reference_sidegrasp.mp4, 0.5x)。
**候选证伪**: 若 ① 接触/力矩塌了 => 侧抓方位虽可达但捏不住, 回退顶抓并把
"指尖修形"作为下一单变量。

#### T2-28 追记 b/c: 放置段掌心必须朝下 (2026-09-02 23:50, 用户复审)

**用户裁定**: 侧抓版放盖时手心朝上 => 指背/拇指插桌; 应手心朝下(手背朝上)放。
**证据链** (三次实证, 各一个变量):
- b1 绕盖轴滚转 -230° (掌翻面需 ~180°): 搬运 IK 崩到 35%/29 行贴限 ——
  **j7 行程 143° 物理翻不了掌**, 也解释了 A 带 -165° 收尾同样掌朝上。
- b2 绕⊥盖轴水平轴上翻 +90° (盖口朝上): IK 100% 但掌法线只到 -0.13 (水平)。
  **几何硬约束**: 该双指捏法掌法线 ⊥ 盖轴 => 盖口朝上时掌永远水平;
  掌心朝下 ⟺ 盖躺倒(轴水平) —— 与重建终点盖侧躺 82° 一致 (人类示范即如此)。
- c 掌法线→正下的**最小旋转** 150° (混合轴不压 j7) + 绕竖直轴 ψ∈[-60,60]
  扫 IK 定 ψ=-30°, 27 行 8.3°/行: 末行掌法线 z=-1.00, 腕高于盖 +6.4cm,
  搬运/放置 IK 100% (余量中位 32.5°, 贴限 0 行)。掌心朝下时拇指垂下 ~5cm,
  放置窗腕钳位 +5cm→+7cm (盖从 1-2cm 高度释放, 微降更物理)。
**产物**: v1_palm2 (scratch md5=127b660c) + UNSCREW_PLACE_PALM=1 开关;
录像自检: 手背朝上、指尖离桌有净空、盖侧躺释放。b1/b2 产物弃用。
转正针不变 (T2-28 ①-④), 视频待用户裁定: unscrew17_reference_sidegrasp_v2.mp4。

#### T2-28 追记 d: 盖圆面朝下 + 手外旋收敛 (2026-09-03 00:10, 用户截图复审)

**用户裁定**: ① 盖不能侧躺, 必须圆面朝下平放 (推翻重建终点 82° 侧躺 —— 上一
追记误判"人类示范即如此"); ② 右手朝外转多了 (掌全朝下时拇指垂直指桌)。
**改动** (UNSCREW_CAP_END_FLAT=1 / UNSCREW_PLACE_PALM_DEG=35):
- 盖末态覆写: 轴竖直圆面朝下, z=桌面+1.1cm, 方位取离手中姿态最近扭转;
  释放 12 行内盖从手中姿态自然落平 (自 ride 末姿态扭转 100°)。
- 摆转沿同一测地线提前停 35° (115°+ψ=-45°): 末行掌法线 z=-0.82, 拇指收侧。
**质量**: 搬运/放置 IK 100%, 余量中位 40.8°, 0 冻结行 (三版里最好)。
录像自检: 盖落平为环形俯视 (圆面朝下✓), 手形自然。母带 v1_palm3 md5=8c25a5fe。
视频: unscrew17_reference_sidegrasp_v3.mp4 (剪辑修正: 不再混入占位撤退行 —— 
上一版视频末尾数帧是占位段, 曾放大"朝外"观感)。

#### T2-28 追记 e: 掌心轴修正 —— 根因是我标错了掌法线 (2026-09-03 00:40, 用户三度复审)

**用户裁定**: v3 手心仍朝外, "你是不是对手心朝下有什么误解, 代码里仔细看" —— 对。
**根因**: T2-28c 的腕系掌法线是间接猜的 (+Y)。FK 真值探针 (palm_fk_probe:
张开指垫平面法线 = 食指垫-小指垫 × 中指垫-掌骨) 实测: 腕系掌法线 =
**C_MC 局部 +X** [1.00,0.04,-0.01], 与 +Y 差 ~60° —— 所谓"掌心朝下 z=-1.00"
实际落在朝外偏下 25°。b2 的"掌⊥盖轴"几何结论也因错轴作废。
**修正** (UNSCREW_PALM_AXIS=1,0,0 默认; PALM_DEG 退回 0):
最小旋转 150°→**67°** (+ψ=+15°, 13 行 7.7°/行), 腕高于盖 +9.0cm;
搬运/放置 IK 100%, 余量中位 **49.4°**, 0 冻结行 (历版最佳 —— 轴对了动作
自然, IK 也顺)。FK 复验: 末行/放置后掌法线世界 [-0.02,0.03,**-1.00**]。
录像目检: 手背朝上、指尖离桌、盖圆面朝下平放 (释放扭转 145° 落平)。
**教训入账**: 涉及"哪根轴是掌心/指向"一律 FK 实测, 不做间接标定。
母带 v1_palm4 md5=4ad2111d; 视频 unscrew17_reference_sidegrasp_v4.mp4。

### T2-29 v53 发射: 侧抓+掌心朝下母带转正 (2026-09-03 00:55, 用户裁定 v4 通过)

**用户裁定**: "可以，加上靠近和撤退的过程，开始做训练"。
**编成**: 停 v52p (7M 步, best -14.96) —— 其命题 (旧顶抓的 yaw=0 观感 A/B)
已被用户亲手定稿的新抓法取代, A/B 失义, 腾 GPU1; **v52 (GPU0) 保留**作
基线 + T2-20 熵修复证伪针 (8M 判读点将至)。v53 = 正典链全新构建:
UNSCREW_CAP_ROLL=-90 / PLACE_PALM=1(轴+X) / PLACE_PALM_DEG=0 /
CAP_END_FLAT=1 / SWING_RATE=9 (构建环境存档 17/BUILD_ENV_v53.txt),
Approach/Retreat 均按新端点重规划 (撤退保留二档排障降级)。
**对照关系**: v52 与 v53 同 yaw(-20)/同左臂, 差异集中在右手抓法+放置 ——
天然构成"旧顶抓 vs 新侧抓"消融对。
**预注册针** (v53, 对齐步数比 v52):
① 验收链: probe_acceptance ≥3/4 + 右手接触峰非零 (发射由链内门把守);
② diag/cap_any 与 sr_t0/gate2: 若 v53 不劣于 v52 且 ep_rew/screw 先于
   v52 脱零 → 侧抓假设 (可达性/接触几何更好 → 拧转更易涌现) 得证;
③ 若 v53 的 screw 到 8M 仍 ≡0 且 cap_any 不升 → 抓法不是瓶颈, 启动
   stage C (aligned@k_sep RSI 出生, T2-16) 作为下一单变量;
④ T2-20 熵针照读 (entropy 停涨, gate2 8M 不塌)。

#### T2-29 追记: 转正链落账 (2026-09-03 01:55)

**产物**: 正典 v1 md5=86b34f35 (451 行, 真规划 app161/ret162) / v2 md5=f526f23f。
**v2 质量 (历史最佳)**: 交互 IK 0/156 坏行, 最大 0.41cm/1.2°, 余量中位
R43.0°/L26.2° (侧抓可达性红利兑现: R 从 v52 带的 ~36° 升到 43°)。
**验收**: 4/4 ✅ 发射; 但 T2-28 针① **部分未达**: 右手接触峰 [1,0,0,1]
(两环境 0/5, 弱于 A 带 [2,2,2,2]); 传入力矩峰 140 mN·m (远高于 A 的 36)。
判读: 侧抓"碰上时更有力、跨环境鲁棒性更差"。已按正式门 (4/4) 放行, 接触
鲁棒性交给预注册针② (训练中 diag/cap_any vs v52) 仲裁 —— 若 cap_any 持平
或更高则强制回放的接触峰差异无实义; 若显著更低则侧抓需要位姿微调再迭代。
**脚本误触发**: 撤退一档已 ✅ 仍跑了二档 (grep 少了 -E, '+' 按字面匹配),
终版 Retreat 为二档"近腿排除物体"版 —— 与 P 变体同方案, 长腿仍全障碍背书,
验收/预检均过; 下版链脚本已知修复点。
**发射确认**: Unscrew17HYB_v53 GPU1 pid 2036087, headless.kit ✓, 手指门
451 行 ✓, 首心跳 FPS 557; v52 (GPU0) 12M 步未受扰。

### T2-30 stage C 实装发射 (2026-09-03 10:40, 用户裁定"现在可以上C")

**判读前情** (T2-29 针落地): 16M 对账 v53 全面占优 (gate2_t0 70.5% vs v52
的 0、align 11~21% vs 0、gate4 11.8% vs 0、回合最优奖励首次转正 +10.96),
侧抓假设得证; **v52 判死** (gate2 自 12M 归零、回合拉长吃 leash 舒适区 ——
顺带证伪 T2-20 "熵是塌陷唯一根因": entropy_coef=0 照塌, 塌陷机制系旧带专属)。
剩余瓶颈 = cap_any ~1%、screw ≡0 (16M > 8M 判读点) → 触发针③ → 上 C。
**实装** (task_env.py, 全部由 UNSCREW_STAGE_C_FRAC 门控, 不设时语义与 v53
运行代码完全一致):
- 出生表追加 "c" 点位: 交互行 k_sep (盖拧紧、右手已在 GraspPose、预置 {1,2}),
  固定占 c_frac=25% 出生率, 其余点位概率结构不变; pre_align 自动把 C 排除出
  sr/align 分子。时钟在盖脱前无法越过 k_sep → C 回合被构造性钉在拧盖子问题上。
- C 回合**双臂残差冻结** (delta[:, :14] 置零): 臂放参考音、只手指学 —— 物理
  排除腕滚假拧 (U44/U45 一脉); 自开训第一步即如此, 动作语义不中途变 (T2-9 红线)。
- 观测 +1 维 "冻臂旗标" (OBS_DIM 507→508 仅 stage C 运行): 同状态臂冻/臂活
  动作语义不同, 不给旗标 = 观测混叠。
- 新针: diag/cap_any_c、diag/screw_deg_c (分母 = C env 步)。
**冒烟**: 出生表 [t0,g1,g2,g3,ret,c] / C 出生行 221=IA0+k_sep / obs 508 /
40 步满力臂动作臂残差恒 0 / 盖拧紧待拧 => PASS。
**编成**: v52 停 (30M, 使命完成); v54C 上 GPU0 (seed42 HYB c_frac=0.25),
v53 (GPU1) 继续作"无 C"对照 —— 单变量 = stage C 课程。
**预注册针**: ① diag/cap_any_c ≫ diag/cap_any (~1%) = C 出生点确实把手指
泡在盖上; ② diag/screw_deg_c 脱零 = 拧转微技能涌现; ③ sr/gate3 (v54C) 脱零
且持续 = 拧脱可学; ④ 若 8M 后 cap_any_c 高但 screw_deg_c 仍 ≡0 → 手指有
接触却拧不动, 问题在接触几何/摩擦/残差权限, 回母带层面调 (下一单变量);
⑤ 若 cap_any_c 也 ≡0 → 出生点接触即失稳 (对照验收 [1,0,0,1] 的鲁棒性忧虑),
调 pin/焊接热身或指残差起点。

#### T2-30 追记: v53 也在 20M 出现 gate2 衰减 —— "塌陷系旧带专属"被证伪 (2026-09-03 12:15)

**数据**: v53 sr_t0/gate2 峰值 6M:70~79% → 20M 近 10 bin 均值 ~4% (0~13% 跳动),
gate1 仍 100%, cap_any→0, 回合长 ~200, entropy 平台稳定 (无 v50 式膨胀)。
v52 12M 塌到硬 0, v53 18M+ 衰减到个位数 —— 同族病, 只是更慢更浅。
**修正结论**: 塌陷机制**不是旧带专属** (T2-29/30 的判读作废该句), 也不是熵
(coef=0 照衰)。指向**风险规避型局部最优**: 抬升认证要押上"抓握失败挨罚"的
风险, 悬停贴参考吃低 leash 是更稳的期望收益; 探索噪声早期靠运气过门,
策略收敛后退回安全悬停 —— 与 18M 确定性 rollout "左手悬停不敢合拢"互证。
**候选下一单变量** (待判读齐后择一预注册): ① G2 报酬从一次性改持续工资型 /
加码 MS_REWARD[2]; ② 认证尝试窗内 leash 宽限; ③ 失败罚下调。
v54C 因 C 出生点结构性免疫"悬停收租"(卡在 k_sep 只吃罚), 其 gate2 走势是
天然对照针。另: 录像进程僵死占 GPU0 半算力 45min (v54C 首 M 步 FPS 335),
已清; C 针首读 (0.9M, 过早勿判): cap_any_c ~0.001 (⑤号针方向: 出生即握持
不稳, 与验收 [1,0,0,1] 互证), screw_deg_c=0, 但 sr/gate3 首现 1.56%。

#### T2-30 追记2: C 出生崩溃现场 + t0 确定性突破 (2026-09-03 18:10, 12M 录像取证)

**t0 集** (确定性 μ, 700 步上限): G链 [1,1,0,0] —— 左手真实合拢抓瓶、过抬升
认证。v53@18M 同口径只有 [1,0,0,0] (悬停不敢抓)。gate2 固化成确定性行为,
与 TB 95% 互证; stage C 人口结构 (25% 回合无法悬停收租) 疑似连带治好了
风险规避塌陷 —— 待 16-20M 复读确认。
**stage C 集** (c 出生, 冻臂): **17 步即 fail 终止**。逐帧: 焊接热身 (15 步
pin) 一松, **左手握不住横躺的瓶** —— 瓶落桌滚走, 右手的盖随之报废。病灶比
⑤号针预想的更靠上游: 不是右手捏不住盖, 是 c 出生点的**左手冷启动握持**先崩
(t0 回合的左手是在瓶直立时建立抓握再转平的, 直接空降到"已横躺"状态没有这个
建立过程)。cap_any_c≈0 全部由此解释。
**修复方向 (v55 候选单变量, 待用户裁定)**:
A. C 出生点**钉死瓶体** (盖仍自由、真实螺纹副可拧): 把 C 子任务纯化为
   "右手指学接触+拧", 绕开左手冷启动 —— 与 T2-16 "冻腕排除假拧"同哲学,
   技能以手指运动模式迁移;
B. 焊接热身 15 步 -> 60 步 + 热身期手指可预压: 保双手真实物理, 但左握
   平衡态能否建立仍无把握。
倾向 A (更小、更定向); B 可作 A 失败后的叠加。

### T2-31 v55C 发射: C 出生点钉瓶 (2026-09-03 晚, 用户裁定"按你认为最好的方案修")

**选型**: 方案 A (钉瓶) —— C 出生 17 步崩的根因是左手对已横躺瓶无建立抓握
过程 (T2-30 追记2 取证), 钉瓶把 C 子任务纯化为"右手指学接触+拧" (瓶被完美
握持的极限情形), 比延长焊接热身 (方案 B, 左握平衡态无把握) 更小更定向。
**实装**: UNSCREW_STAGE_C_PIN_BOTTLE=1 —— C env 焊接热身后每控制步把瓶根
姿态写回出生行姿态 (盖自由, 真实螺纹副照拧; 时钟在脱盖后因瓶不随护送行而
停滞 => C 回合的报酬载荷 = 真实拧转 K_SCREW×Δθ + G3 里程碑, 由 t0/其他
RSI 车道负责全链缝合)。冒烟: c 出生 4env 零动作 120/120 步存活 (原 17 步)、
瓶位移 0、臂冻结 ✓。
**编成**: v53 停 (42M, gate2 塌陷已确认, 对照使命完成); v55C 上 GPU1
(seed42, c_frac=0.25 + 钉瓶), v54C (GPU0) 继续 —— 唯一变量 = 钉瓶。
**预注册针**: ① diag/cap_any_c 应从 ~0 上台阶 (回合活了, 手指泡在盖上);
② diag/screw_deg_c / ep_rew/screw 脱零 = 拧转涌现 (主判读);
③ sr/gate3 (含 c 车道) 持续非零; ④ v54C 的 gate2 16-20M 复读 (塌陷免疫
确认, 与 v55C 无关的平行针); ⑤ 若 cap_any_c 上台阶而 screw_deg_c 仍 0
=> 接触有了但拧不动, 下一站查捏合深度/摩擦/残差步长 (母带或 env 参数层)。

**T2-31 追记 (2026-09-04 深夜, 31M 步判读): 钉瓶单变量失败, 且 v55C 塌陷。**
- ①号针 **未通过**: diag/cap_any_c ≡ 0 全程 (瞬时峰 0.03)。回合确实活了
  (17 步崩已消失), 但手指从未接触盖 —— 病灶又上移一层: 冻臂 + 参考 k_sep
  手指构型 + 钉瓶下, 手指与盖之间仍无接触, 即参考手指姿势捏合深度不够,
  且指残差在回合内没学出"压向盖"的动作。
- ②号针 **未通过**: screw_deg_c ≡ 0, ep_rew/screw ≡ 0。
- ③号针 **未通过**: gate3 全程 0 (两 run 皆然)。
- **塌陷复发**: v55C sr_t0/gate2 轨迹 0.31→0.96(15M)→0.125(19M)→**0 (23M
  起, 12M 步未恢复)**。stage C 编队没保住 v55C —— T2-30 "C 编队 = 塌陷
  解药"假设被削弱; 嫌疑变量 = 钉瓶后 C 回合活到超时、全程吃停滞/leash 罚
  (发射时实测 Mean Rewards -403), C 车道巨额负回报拖垮 value/优势归一,
  连带 t0 车道信号被淹。
- ④号针 (v54C 平行): sr_t0/gate2 40M=1.00, 48M=0.86, **56M 瞬时塌 0,
  65M 自行回升 0.61-0.72** —— 首次观察到塌陷后自恢复 (v52/v53 从未恢复过),
  C 编队价值的新证据 (v54C 无钉瓶、C 回合 17 步即死 => 没有负回报出血)。
- **判定**: 钉瓶以现配方放弃。下一站单变量候选 (待裁定, 一次一个):
  (a) C 出生手指预压 —— C env 出生时把手指初始化为对盖已捏合状态 (或母带
      k_sep 行手指值加预压量), 直击 cap_any_c=0;
  (b) C 车道罚项减免 —— C env 停滞/leash 罚归零, 只留拧转正报酬, 治出血;
  (a)+(b) 有交互, 但 (b) 是 (a) 的前置安全阀 (预压若成功, 回合更长, 出血
  更多)。倾向先 (b) 后 (a) 或合并为"C 车道报酬重写"一个变量对待。
- v55C 建议停 (12M 步零 gate2, 按 v52/v53 经验不可逆), GPU1 释放待命。

### T2-32 G2 考试计件工资 (2026-09-04 深夜, 用户裁定"左手的瓶身都没转,需要改")

**用户肉眼针**: 68M 确定性录像里左手全程没把瓶转平。代码核实的机制链: 转平
不是独立技能, 是时钟走到转平行后跟母带走出来的; 而时钟推进被 `& g2` 钉死
(progress_batch `can = ok & (k<cap) & g2`) —— G2 举升认证不过, 转平/拧盖/
放置全链冻结在起点。TB 实锤: **sr/cert_pass 74%(18M)→58%(37M)→40%(52M)→
1%(63M)→0%(74M)**, 尝试率反升到 1.5-1.8 次/集 —— 在考, 次次不及格, 单调
烂穿, 无自愈。56M 的 gate2 塌陷就是这条线砸穿地板的时刻。
**病理**: G2 考试是纯稀疏判定 (过=+8, 不过=0), 考试过程零梯度; 而"摸着不举"
有收入 (wage 0.05/步×封顶3 + 贴实力势, 都只要求接触); 滑移罚/反射奖全部
g2 门控=考后才生效。及格线附近的姿态细节无梯度可修, 一漂移就回不来。
**实装** (env 开关 UNSCREW_CERT_WAGE=<K>, 默认 0=关, 不影响旧 run 回放):
认证 ramp/hold 期按 min(瓶z升,盖z升)/CERT_RISE 的**回合新高**发钱
(earn-only 棘轮防提放刷分; pads3 门防戳举; K=满举回合总收入, v56 用 3.0
—— 与 WAGE_CAP 同量级, 低于 MS_REWARD[2]=8)。附带观测 (不算变量):
diag/certfail_{rise,slip,pads} 失因分针, 分母=判定次数。
**验证**: selftest_progress_batch 开/关双跑全绿 (判据未动, 标量镜像无需
同步); CPU 单元测试 6 项 (发钱比例/棘轮零增量/封顶/pads门/复位归零/关断);
Isaac 4env 零动作 60 步集成冒烟 (记账通路)。
**编成**: v55C 停 (T2-31 判定失败+自身塌陷), GPU1 上 **v56 = v54C 配方 +
UNSCREW_CERT_WAGE=3.0** (含 stage C c_frac=0.25 不钉瓶, 与 v54C 唯一差=
计件工资); v54C 留 GPU0 做自然对照 (其 cert_pass=0 已 10M+ 步)。
**预注册针**: ① sr/cert_pass 脱离 0 并稳到 >50% (主判读); ② sr_t0/gate2
≥0.8 且不再有 >2M 步的归零区间; ③ prog/clock_frac 上行 + 录像瓶被转平
(用户肉眼针); ④ ep_rew/cwage 应有正流水但 <MS 量级 (若逼近 K×尝试数上限
而 cert_pass 仍 0 => 在刷坡不毕业, 查 slip/pads 分针对症); ⑤ v54C 对照
继续躺 0 (若它自愈则本变量的必要性存疑, 如实记)。

### T2-33 右手贴盖计件工资 (2026-09-05, 用户裁定"试试，做")

**背景**: T2-32 主针通过 (v56 25M: cert_pass 0.78-0.84, sr_t0/gate2 0.94-1.0,
certfail_slip 0.81→0.18 被学掉, clock_frac 0.47 超 v54C 历史峰值)。确定性
录像 G链 [1,1,0,0]: 抓瓶/举升/转平全通, **链条唯一断点后移到右手拧盖** ——
sr_t0/align ~0.5 (一半 t0 回合走到对准点) 但 cap_any≈0 / gate3≡0: 手到盖旁,
指不碰盖。病理同 G2 旧疾: "伸指捏盖"考试期零梯度 (贴实奖金只付"靠近"
距离势, 不付接触压力; 2026-09-01 的轴向修正也只把 cap_any 从 0.0056 挪到
0.0075, 量级不足)。
**实装** (env 开关 UNSCREW_CAP_WAGE=<K>, 默认 0=关): 对准后 (PB.k>=k_sep)
·释放前·交互行·非热身窗口, 右垫×盖接触垫数 (_n_cap_any, 与真值门同一测量,
力阈 SCREW_CONTACT_FTH) 的**回合新高**按 K/5 每垫发钱 (earn-only 棘轮)。
v57 用 K=3.0 (T2-32 同量级)。C 车道自动受益 (其 k 钉在 k_sep 即在窗内)。
**验证**: py_compile ✓; Isaac 4env c出生+假接触注入冒烟 (monkeypatch
_n_cap_any=3): 预期总账 7.2 = 4env × 3垫 × 0.6, 验窗口/棘轮/费率。
**编成**: v54C 停 (对照使命完成: 100M 全零无自愈, 预注册针⑤兑现), GPU0 上
**v57 = v56 配方 + UNSCREW_CAP_WAGE=3.0** (唯一变量; 含 CERT_WAGE=3.0 已
随 T2-32 转正入基底); v56 (GPU1) 续跑当对照, 兼验 40-60M 塌陷窗口。
**预注册针**: ① diag/cap_any 上台阶 (主判读: 0.003 量级 → 持续 >0.05);
② ep_rew/capw 有流水且**不逼近** 3.0×回合数上限 (逼近而 cap_any 不涨 =
刷坡不接触, 查窗口漏洞); ③ diag/screw_deg / ep_rew/screw 脱零 = 拧转涌现
(链条真正的终点针); ④ sr/gate3 首次非零; ⑤ v56 对照 cap_any continues ≈0
(若同涨则是 v56 自然进步, 本变量必要性存疑, 如实记); ⑥ 副作用监视:
v57 的 cert_pass/gate2 不得因新款项失稳 (报酬蛋糕被稀释的风险)。
**落空条款**: 若 cap_any 涨但 screw_deg 仍 0 => 接触有了拧不动, 下一站
查捏合力/摩擦/腕滚残差权限 (T2-31 ⑤号预案复活)。

**T2-33 追记 (2026-09-05 下午, 13M 中期判读): 剂型错误, v57 且已塌陷。**
- ①号针失败在机制层: ep_rew/capw 全程≈0 —— 接触是**离散稀有事件** (步占
  比 0.3%), 棘轮只在接触后发钱 = 还是稀疏奖励。T2-32 的真正成功要素是
  "举升有连续部分进度可计酬"; 贴盖没有铺出"悬空→压近→接触"的连续坡。
- v57 附带塌陷 (cert_pass 0.52→0.01, sr_t0/gate2 0.93→0.06, 13M): capw 几
  乎未发钱, 归因于 v52/v53 型随机塌陷盆地 (同 seed 但 PhysX 非确定; v56 在
  5M 也深跌过后爬回)。两案并发, v57 停。
- v56 对照 37.5M: 主链稳 (gate2 0.89-0.95, align 0.81, clock 0.43) 但
  cert_pass 自 18.8M 峰 0.83 缓降至 0.43-0.54, 正在自己的塌陷危险窗口里。
  cap_any 恒≈0.001 => 预注册针⑤成立: 不给梯度, 接触不自发涌现。

### T2-34 贴盖接近坡 (2026-09-05, 用户裁定"改，训")

**药理不变换剂型**: 在 T2-33 同窗口 (对准后·释放前·交互行·非热身) 加
**连续接近坡**: 右 5 指垫到盖心的最近距离 dmin, 进度 = clamp((8cm-dmin)/
(8cm-2cm), 0, 1), 对其**回合新高**发钱 (earn-only 棘轮), 满程 K=3.0
(UNSCREW_CAP_APPROACH, 默认 0=关)。接触计件 (UNSCREW_CAP_WAGE) 保留在
坡顶衔接。关键细节: **出生基线免费额** —— 窗口首步只记基线不发钱
(capap_primed), 否则 c/深 RSI 出生按出生位白领横财; 只有比出生态更近才
算收入。t0 车道臂残差是活的 => 坡的梯度同时流向手指屈曲与腕臂下压。
**预注册针**: ① ep_rew/capap 有持续正流水 (坡在被爬); ② diag/cap_any
上台阶 >0.05 (坡顶兑现成接触, 主判读); ③ ep_rew/capw 首次有实质流水;
④ screw_deg/ep_rew/screw 脱零、gate3 非零 (终点针); ⑤ 副作用: cert_pass/
gate2 不失稳; ⑥ 若 capap 拿满而 cap_any 仍 0 => 2cm 壳到接触之间还有沟
(盖几何/力阈), 量 dmin 分布对症。
**编成**: v57 停 (13M); GPU0 上 **v58 = v56 配方 + CAP_WAGE=3.0 +
CAP_APPROACH=3.0** (合并记一个变量 "贴盖坡+计件", 因 T2-33 已证单独计件
不发钱, 二者是一味药的两段); v56 (GPU1) 续跑对照。

**T2-34 追记 (发射前几何勘探, dmin 逐步实测)**: c 出生写入态 dmin=4.5cm,
但**臂控制器稳态坐在 ~9.6cm** (6 步内漂回, 平衡位比写入姿态远 5cm; 焊接一
松瓶落, 15.9cm+, 即已知 C 崩)。原 8cm 起坡会把稳态留在坡外零梯度区 ——
**坡域改 12cm→2cm** (prog=(12cm-dmin)/10cm)。附带发现①: "对准" (align,
v56 达 0.8) 只保证时钟到行, 手的稳态离盖仍有 ~10cm, cap_any≡0 的几何原因
即此。附带发现②: 臂控制器 5cm 稳态误差本身是候选病灶 (若坡给了梯度仍拉
不动腕, 下一站查臂增益/参考行姿态偏置)。冒烟 (机制三项) 已在 8cm 版通过,
12cm 版复跑基线免费额+强制付款后发射。

**T2-34 追记2 (用户指正"我设计的是拇食双指, 不是非要3根")**: 核对代码 ——
① U40 起 screw_drive_gain 仅是观测特征不进物理, 拧开只看真实接触力矩
(双指对捏每垫 ~0.2N 即超 0.04N·m 脱扣, 物理上双指足够); ② 旧分级口径下
拇/食都在 SCREW_TRIAD, 双指=2/3, 单垫也有 1/3 下限, 从无"必须三指";
③ 现役先验就是用户双指姿 (CAP_GRASP=user_thumb_index + PINCH=8), 右抓判
定/护送持盖都是 ≥2 垫口径。唯一轻偏差: T2-33 接触计件按 5 垫分级, 完美
双指只领 1.2/3.0 —— 不中途改 (毁单变量), 挂观察: 若见"为凑垫数破坏捏合"
则下变量把计件 2 垫封顶对齐双指设计。

### T2-35 双指持捏工资 (2026-09-06, 用户裁定"可以，开始做")

**背景**: v58 42M / v56 73.5M —— 两 run 各出现一次孤立 gate3 (16.3M vs
73.5M, 坡+计件 = ~4.5× 加速器而非唯一使能器, ⑤号针两面如实记), 但都没
稳住。共同卡点: 接触是**单垫瞬时擦碰** (cap_any~0.001, 探针实证 1/64 回合
单垫 10 步), 拧开需要拇食**持续对捏** —— 棘轮对"擦一下"和"捏住"发同样的
钱, 缺指向"持续"的梯度。
**实装** (UNSCREW_PINCH_WAGE=<回合封顶>, 默认 0=关): 同 T2-33/34 窗口内,
>=2 右垫同时在盖 (真值门测量) 按 **0.02/步** 发钱, 回合封顶 2.0 即停
(WAGE_CAP 同构防吃租); 单垫不发 —— 对齐用户双指设计 (T2-34 追记2)。
量级: 满捏 100 步 = 2.0, 仍 < G3(10), 利润大头留在终点。
**验证**: py_compile ✓; Isaac 4env 假接触分组冒烟: 3垫 env 付 0.9000
(=45 合规步×0.02 精确), 1垫/0垫 env 付 0, racc 总账吻合 → PASS。
**编成**: v56 停 (79M, 对照使命全部完成: 塌陷窗口穿越 + ⑤号针两面);
GPU1 上 **v59 = v58 配方 + PINCH_WAGE=2.0** (唯一变量); v58 (GPU0) 续跑
当对照。
**预注册针**: ① ep_rew/pinchw 有流水 (捏合在发生); ② diag/cap_any 上台阶
>0.01 且**持续** (主判读, 对照 v58 的 ~0.001); ③ gate3 从孤立事件变成
持续非零 (终点针); ④ v58 对照仍是孤立事件形态; ⑤ 副作用: 持捏工资不得
诱发"捏住不拧"吃租 (若 pinchw 拿满而 screw_deg 仍 0 => 封顶 2.0 太肥或
需改为"捏且转才发", 下一变量)。

**T2-35 追记 (2026-09-06 深夜, 用户揪出重大取证事故): 猎捕"拧开"全部是伪证。**
用户看出猎捕视频最后一帧画面突变。取证: IsaacLab 在 step() 内对终止 env
当场重生 (record_task.py 2026-08-28 注释早有此坑, 猎捕脚本没设防), 终止步
之后读闸门/渲染 = 读到**下一回合的出生态**; 抽到 g3/ret/c 谱系 (生来盖已
拧下, 螺角=30.0°) 即同时伪造 g3=True、"拧角峰值恰 30.0°"、"末帧盖在桌上"。
倒数第2/3帧盖仍在瓶上 = 铁证。**全部 4 支猎捕"拧开"视频已删除, "史上第一
支拧开视频"的说法撤回。** b_g3 守卫只防了本回合出生预置, 没防下一回合的。
**未被污染的证据**: 训练侧 sr/gate3 (PB reset 前结算+pre3 掩码) 与 diag/*
(逐步累计) 仍为真 —— 拧开在训练分布里发生过, 但从未被相机捕获。
**教训 (与 2026-08-28 同款, 第二次犯)**: 终止步后的任何 env 读数一律作废;
录像/探针的闸门与状态读取必须在 done 判定**之前**截断。

**T2-35 追记2 (修正判定后的真率探针)**: v59@1M "热区" ckpt + mu+0.08 噪声,
64env×800 步 = 297 完结回合, **真拧开 0 发** (终止步前判定+出生预置双重排
除)。结论: 训练 TB 里的零星 gate3 是 PPO 自身大 sigma 探索的产物, 离线
ckpt+小噪声不可复现; **当前不存在可拍摄的真拧开, 相机狩猎停止**。第一支
真策略拧开视频的前置条件 = v59 (或后继) 把 sr/gate3 训成持续非零; 完整闭
环视频的前置条件 = 再加 gate4 (放稳+撤退归位) 脱零。

### T2-36 捏且转工资 (2026-09-07, 用户裁定"可以改", 并准停 v58)

**背景 (v59 尸检)**: v59 冲顶后退潮 —— 23M 巅峰 (cap_any 0.014 /
capw 0.0027 / pinchw 首笔流水 / screw 5.1e-4, 给用户录的 23M 视频正是
顶点), 25M 起触盖四通道齐跌, 29.5M 触盖率掉 70 倍至 ~2e-4、pinchw/screw
连续 4M 步归零。**但基本盘同期创新高** (gate2_t0 0.96~1.00, align
0.92~1.00, cert_pass 0.83, capap 仍稳定支付): 手在靠近, 只是不碰了。
判读: ⑤号针的"捏住不拧吃租"没发生, 发生的是它的镜像 —— 盖子三通道全是
**一次性收入** (capap/capw 棘轮 + pinchw 封顶 2.0), 领完即无; cert 链
收益走高后, 碰盖的死线风险 (碰翻瓶=丢掉全部后续收入) 盖过残值, 加上
sigma 退火, 风险调整后"只靠近不碰"成了占优策略。
**实装** (UNSCREW_PINCH_WAGE **语义变更**: 回合封顶→**每步工资**, 默认
0=关): 同窗口内 [>=2 右垫在盖] 且 [螺角当步爬过回合新高 >0.05°
(PINCH_CLIMB_EPS, 滤关节抖动)] 按步发 0.05, **不封顶** (自然上界=回合
步数, 且必须真捏真拧才领得到 —— 把持续收入和任务真进度绑死, 对冲死线
风险, 顺带杜绝吃租)。新高棘轮无条件推进: 没捏住时的爬升不欠账事后不
补票; 出生基线=出生角 (g3/ret 满角出生无横财)。旧 pinchw_paid 退役。
launch_v59.sh 已加"不可直接重放"警示 (2.0/步会天价)。
**验证**: py_compile ✓; Isaac 4env 四相冒烟: 捏且转付 5.0000 (0.05 整除
✓), 捏不拧 0, 单垫 0, g3 出生横财 0, 出生基线对齐 ✓ → PASS。
**编成**: v58 停 (71.9M, 对照使命完成: align 0.26 对 v59 0.96, 四通道
归因坐实; run 目录保留作证据)。GPU0 上 **v60 = v59 配方 + 捏且转工资
0.05** (唯一变量); v59 (GPU1) 续跑当对照 (同代码在内存, 行为不变)。
**预注册针**: ① v60 的 cap_any 在 20M+ 不重演 v59 的 25M 退潮 (主判读,
对照 v59 续跑曲线); ② ep_rew/pinchw 出现持续流水且与 ep_rew/screw 同步
(捏且转在发生); ③ gate3 从孤立事件变成持续非零 (终点针); ④ 若 cap_any
回升而 screw/pinchw 仍 0 => 瓶颈不在激励在物理 (力度/摩擦/位形), 下变量
转物理侧; ⑤ 副作用观察: 慢拧刷步 (螺角以 ~eps 速率蠕升凑步数) —— 若
ep_len 拉长且 screw_deg 爬升速率异常低则封顶或改按角增付费。

### T2-38 预案 (2026-09-07, 用户裁定): 拧盖不行 → 开"拔盖"新环境, 现有环境不删

**用户原话要点**: "如果扭瓶盖不行, 开一个新的环境 (现在的扭瓶盖的当然不要删),
新环境和现在环境唯一的不同就是: 瓶盖不需要扭了, 拔开, grasp pose 和现在
一样, 也是双指, 就是我设计的那样。"
**触发条件 (预注册)**: T2-37 臂前馈上线后的 run 仍不能把 sr/gate3 训成持续
非零 (即几何修好后瓶颈仍在"拧"这个动作本身)。触发前**不动工**, 避免分散
单变量主线。
**实现边界 (触发时照此做, 用模式开关不复制代码, 默认模式=拧盖行为不变)**:
① 螺纹副 → 轴向拉脱: G3 判据改为 盖沿盖轴相对瓶位移 ≥ 阈值 且 ≥2 右垫在盖
(接触真值门沿用); ② r_screw (K×Δθ) → r_pull (K×Δ轴向位移, 同量级总额);
③ 捏且转工资 → 捏且拔 (位移当步爬新高); ④ 观测螺旋块 4 维语义换成拉脱进度
(维数不变, 指纹 obs_dim 不动); ⑤ 母带 IA 段右手动作由"扭"改"拉" —— 用
build_reference.py 重铸 v2 (抓姿先验仍 user_thumb_index + PINCH=8 不动);
⑥ 世界指纹 assembly.* 记录新模式, 与拧盖 ckpt 互斥。
**不变量**: 左手抓瓶/举升认证/转平/放稳/撤退全部沿用; 抓姿双指不变。

### T2-37 臂前馈标定 (2026-09-07, 用户选方案 A) —— **前提被证伪, 真因另有其人**

**原假设**: 右臂 PD 控制器在拧盖位形有 ~5cm 重力/刚度稳态漂移 (T2-34 实测
c 出生 写入 4.5cm → 稳态 9.6cm), 逐行标定关节预偏前馈抵消 (迭代学习控制)。
**标定探针** (calib_arm_ff.py, 78 env=78 IA 行, 物体钉母带行, 保持 25 步):
- 自由空间双臂跟踪母带到 **~2mm** (左腕 78 行全 0.2cm; 右腕 61/78 行 0.2cm)
  —— **不存在重力/刚度漂移**, 假设①证伪。
- 位移只在行 **212~228** (对准/接近盖段, c 行 221 在内): 0.4→3.4→0.1cm 的
  平滑鼓包 = 被接触**顶出**的形状; 前馈迭代零效果 (3.36→3.44cm, 预偏发散到
  0.84 rad) = 平衡点由接触约束而非控制器决定。**方案 A 机制上不可能修此病,
  产物 arm_ff_v1.npz 已删, 消费代码已撤。**
**真因追踪** (三支诊断探针, 逐一排除):
① 物体抬高 1m 位移逐行**完全不变** (3.22→3.22), 右垫力 0.00N → 与瓶/盖无关;
② 关节顶限位 0 个; 世界指纹 robot.self_collision=False → 与自碰无关;
③ 手部连杆离桌 ≥+2.9cm → 不是手碰桌; 但最低连杆是**右前臂 R_arm_l5**, 原点
   离桌仅 3.3~4.0cm, 而 l5/l6 截面半径 4~6.7cm;
④ 用基类同款外壳点云 (arm_shell_points.npz 含 R 侧) 算写入态外壳最低点:
   **右前臂外壳在行 212~228 低于桌面, 最深 -1.8cm (行 216~225)**, 与鼓包
   逐行吻合; 左臂全程 ≥+2.5cm。
**结论**: 母带 v2 的右臂 IA 段 IK 解**把前臂穿进了桌面**; PhysX 把整臂顶起
~3cm, 手被带离盖 (写入 5.2cm → 稳态 8.8cm)。基类的臂外壳撞桌罚/诊断是
**逐侧**的, Unscrew 交互手=left, 右臂从未被检查过 —— 这个洞就是这么漏的。
**对 T2-34/35/36 判读的修正**: "臂控制器 5cm 漂移"叙事作废; capap 坡稳态
悬在 ~9cm、擦碰不成捏的几何根因 = 前臂顶桌。T2-36 捏且转工资 (v60) 的判读
不受影响 (它治的是激励退潮), 但在几何修好前它可能同样"无单可接"。
**修法方向 (待用户裁定, 见下一条)**: 修的是参考的碰撞缺陷, 不是镀金 ——
7 自由度臂有肘部零空间, 可在**手位姿不变**的前提下抬前臂离桌; 或重铸 IA
段右臂 IK 时加桌面外壳约束。母带 md5 会变 → 新 run 新指纹。

### T2-39 母带右臂离桌重铸 (2026-09-07, 用户裁定"重铸右臂 IK 抬肘离桌")

**实装** (build_reference.py): 逐行 IK 后加**臂外壳离桌约束** SHELL_CLEAR=1.2cm
(基类罚余量 0.8cm + 4mm 给残差), 口径 = 基类同款外壳点云 (arm_shell_points.npz
含 R 侧) + URDF FK, 离线可算 (对照 in-sim 深 ~1cm, 保守)。
**零空间抬肘走不通**: 手位姿钉死时, 行 213~226 旋肘圆上的离桌极大只有 -1.3cm
(无关节顶限位, 就是几何无解); "先旋肘到极大再抬腕"会落到关节改变 82° 的另一支
(拧盖前 0.4s 内 80° 甩臂, 否决)。**最终采用只抬腕** (最小范数 GN, 姿态贴原带,
腕目标逐毫米沿 +z 抬到达标, 抬高写回 tgtP 供下游重投影/统计): 关节改变 <=10°,
行间跳变 12.7° (<20° 上限)。**只约束右臂**: 首次全侧约束把左臂 33 行抬了 <=22mm
—— 左臂外壳最深 -0.84cm (in-sim -0.2cm, 腕漂移 0.2cm 无害) 且左手正握瓶, 抬左腕
会破坏手-物体几何, 撤回。
**产物**: reference_v2.npz md5 f526f23f → **42d307fe** (旧带备份
_backup_pre_elbow_20260907/, v59/v60 回放用 POUR_REF_NPZ 指过去)。只有 right_q 变
(行 206~288: 抬腕带 206~230 + 后续行从抬后种子重解 + 缝2 重融), left_q/物体/
手指/人手字段逐字节相同; 新增 wrist_rise_r/l (逐行抬高量)。抬高剖面 (mm, 行
206~230): 5 7 8 10 13 18 26 34 38 41 42 43 44 44 43 42 43 44 45 45 44 39 32 20 5
—— 峰 45mm@行224, 到行 231 归零 (捏合行不动)。物理本来就把手顶高 ~32mm (带接触
力), 现在是同量级的**有意**抬高 + 1.2cm 余量, 无接触力。
**验证**: ① 离线外壳: 全 IA 右臂最小 +1.20cm, 0 行低于要求; ② in-sim 外壳探针:
行 212~229 全部 +1.2~1.3cm (旧 -1.8cm); ③ 逐行稳态漂移探针 (calib_arm_ff
--iters 0): 右腕 78 行全 0.2~0.4cm (旧 212~228 鼓包 3.4cm 消失), c 行 221 dmin
写入 5.4cm → 稳态 **5.3cm** (旧 5.2 → 8.8); 交互 IK pos>1cm 0/156, 关键窗坏行 0。
④ probe_acceptance 凭据重签 + ⑤ 回放视频: 见追记。
**对训练的含义**: capap 坡 (12→2cm) 的稳态起点从 ~9cm 回到 ~5cm; T2-36 捏且转
工资从此"够得着单"。**编成**: v61 = v60 配方逐字不变 + 新母带 (唯一变量=几何),
对照 = v60 (旧母带); v59 (T2-36 对照, 45M 已完成退潮判读) 可停以腾 GPU1。
**预注册针**: ① v61 的 diag/cap_any 在 10M 内超过 v60 同龄段 (0.002~0.004) 一个
量级 (主判读: 几何是触盖瓶颈); ② ep_rew/pinchw 出现持续流水 (捏且转有单可接);
③ gate3 持续非零 (终点针); ④ 若 cap_any 上台阶但 pinchw/screw 仍 0 → 瓶颈转到
指尖力/摩擦 (物理侧); ⑤ 副作用: 抬腕带 (行 206~230) 不得诱发左手掉瓶/cert 退化
(sr/cert_pass、gate2_t0 不低于 v60 同龄段)。
**T2-39 追记 (验收 ④)**: probe_acceptance 零残差全链回放 **训练稳定性 4/4 (要求
≥3) ✅**, 凭据 acceptance_v2.json 重签绑定新母带 md5 42d307fe,
acceptance_receipt_issues(REF_V2) = [] —— train_task 门放行。
**T2-39 追记 (发射预检)**: 首次发射 v61 被 train_task 预检拒绝 —— 我把 meta_v2 的
gen 标成 unscrew_v2_shell, 预检要求 gen=unscrew_v2 (生成器族名, 不是版本号)。
改法: builder 恢复 gen=unscrew_v2 (外壳字段仍在 meta 里记录), 已验收的 npz 只改
meta 字符串重存 (其它键逐字节相同, md5 42d307fe → 04c2beb1), probe_acceptance
重签凭据后再发射。编成: v59 已停 (26.5h, T2-36 对照使命完成, run 目录保留);
v61 上 GPU1, v60 (GPU0) 留作几何变量的对照。

### T2-40 拔盖新环境 (2026-09-07, 用户裁定"现在就建 (编码+冒烟)", 随后"放在 v60 那张卡上一起训")

**用户约束**: 与现有拧盖环境**唯一**的不同 = 盖不用拧, 拔开; grasp pose 不变
(user_thumb_index 双指); 现有拧盖环境不删。
**母带发现 (建之前查的)**: 人手示范里盖的相对转角只有 ~2° (对称盖, 重建的自转不可
信), 盖在行 222~228 随左手扶正瓶身**沿轴脱离**; "拧"这个动作完全来自 env 的螺纹
物理 (30° 脱扣), 不来自母带。⇒ 拔盖模式**母带/双手路径/物体路径一律不改**, 只换
装配物理 + 让 G3 由"拔满"触发。
**实装** (模式开关 UNSCREW_CAP_MODE=pull, 默认 screw = 逐位不变):
- screw_assembly._pull_step: 与 _thread_friction_step **同构**的轴向塞盖副 —— 力矩→
  轴向力 (f = m_cap·dv/dt, dv = 盖 v − 上子步实写 v 矢量·轴, 瓶身加速自动抵消), 真值
  门 (指尖零接触 ⇒ 0) / EMA / 持续解锁 / 静-动摩擦滞环 / 准静态过阻尼 (v = (|f| −
  F_k)⁺/b) 逐条照搬; _pull_finish: 实写 v 矢量 / 惯量切换 (抽成 _cap_inertia_switch
  共用) / 反作用轴向力回瓶身 (左手得抗住拔力)。
- screw_angle 承载拔出进度 (拔满 15mm = 满角 30°), 轴向滑速按 pull_full/max_angle
  折算 —— 观测块 / G3 (detach_at_full) / r_screw / 捏且转 / RSI g3 出生 / TB 全部零改动
  (TB 里 screw_tau 通道在 pull 世界的语义 = 轴向力 N, 台账记一笔)。
- 参数 (env var): PULL_FULL_MM=15, BREAKAWAY_N=2.0, KINETIC_N=0.8, VISCOUS=40 N·s/m,
  VMAX=0.15 m/s。量级依据: 摩擦配合盖数 N 级; 双指对捏 1N × 垫摩擦 6 ⇒ 可传 ~6N。
- 世界指纹 assembly.cap_mode + 5 个 pull 参数入 CRITICAL (旧记录补默认 screw/0.0):
  v60 记录 vs 当前 screw 匹配 / vs pull 关键不符 ✓ (两种 ckpt 互斥)。
**编成 (用户裁定)**: Unscrew17PULL_v1 = v61 配方逐字不变 + CAP_MODE=pull, 与 v60
同卡 GPU0 (launch_pull_v1.sh)。
**预注册针**: ① sr/gate3 在拔盖世界应远早于拧盖世界脱零 (拔只要力, 不要持续对捏
+跟转); ② 若拔盖也 gate3≡0 ⇒ 瓶颈在"够到并捏住", 与拧无关 (反推 T2-39 之后的
下一刀在抓取/力度侧); ③ 副作用: 拔力反作用回瓶身不得把 cert_pass/gate2 打下来;
④ 伪证防线同 T2-35: 任何"拔开"视频必须终止步前判定 + 拔出进度逐步爬升自证。
**T2-40 追记 (冒烟)**: 4 env c 出生 (g2 预置, 盖 seated): 相1 零动作 30 步拔出进度
全 0、无 released (无幻影); 相2 关真值门 + 对盖施轴向外力 60 步: 4N 的两个 env 拔满
(进度 1.00, 首次脱扣第 11 步 ≈0.55s) 且 G3=True (终止步前锁存), 1N env 进度 0
(力 EMA 0.83N < breakaway 2N), 0N env 0 → **PASS**。指纹: v60 记录 vs 当前 screw
匹配 / vs pull 关键不符 ✓。
**T2-40 追记 (首次发射被指纹拦下, 属正确行为)**: train_task 启动时把验收凭据里记录
的世界与当前 env 比对, 新增的 assembly.cap_mode/pull_* 关键项把拔盖世界判为"与
拧盖世界不符" —— 凭据绑定的是"母带 × 世界", 拔盖世界物理不同, 本就该各签各的。
处理: ACCEPTANCE_JSON 按盖模式分文件 (screw: acceptance_v2.json 不动; pull:
acceptance_v2_pull.json), probe_acceptance 在拔盖世界重跑零残差全链稳定性门后
重签, 再发射 (探针/预检/训练三处都走 TC.ACCEPTANCE_JSON, 一处改动全覆盖)。
