# KeyFrame:从 Prior 编译训练逻辑(拧瓶盖任务通用设计,草案)

**日期** 2026-08-13 　**状态** 草案待审 　**适用** screw_unscrew_bottle_cap 全类,**基于 18 号开发**
**上游依据** `NOISY_WORLD_FRAMEWORK.md` P2~P5;`DESIGN_LOOP.md` §2.14(顺序门控)/§2.17(先抓后飞);
screw27 三训(§2.20~2.22)只继承**机制结论**(配方迁移性/门控/affordance 机制/扰动经济学教训)。
**用户裁定(2026-08-13)** ①**27 号数据整条弃用**(重建不准),不做训练数据、不做真值对拍;
②CAD 替身沿用 27 号那套瓶身+盖 CAD **资产**(资产本身干净,与 27 的重建数据无关);
③18 号 retarget 补跑;④GraspPose 池与盖 affordance 由另一 Agent 基于 18 号交付。

---

## 状态与续做指引(2026-08-13 存档,**先做更简单的任务,本设计暂缓**)

**当前状态**:设计与数据准备完成到"可以开始写抽取器"的断点;**未写任何抽取器代码,
未改任何 env 代码,未起任何训练**。用户决定先从更简单的任务做起,本方案原样封存待续。

**续做时的第一个动作**:按 §11.3 写 `rl_rebuild/correction/keyframes.py`,先只做
`load → roles → events → confidence`,拿 `data/screw18_gt.json` 对拍(§11.6 第 1 关)。

| 类别 | 位置 | 状态 |
|---|---|---|
| 本设计全文 | `docs/KEYFRAME_PRIOR_DESIGN.md` | ✅ 本文件 |
| 网页版(自包含,含证据图) | `docs/keyframe_design.html` | ✅ 浏览器直接打开 |
| 人工事件真值(18 号) | `data/screw18_gt.json` | ✅ 只供测试/校准,训练禁读 |
| 逐帧图(104 张) | `data/screw18_frames/` | ⚠ 64MB **不入库**,重生成:<br>`ffmpeg -i <18.mp4> -start_number 0 data/screw18_frames/frame_%03d.png` |
| 手轨迹审计工具 | `steps/step2_reconstruction/hand_audit.py` | ✅ 入库;**判据待按 §10.3 修**(改掌心代理 + 只在物体运动时判) |
| 18 号 retarget 产物 | R&R 仓 `Output/RetargetOutput/egodex_auto/screw_unscrew_bottle_cap/18/` | ✅ replay_world + 双手 ref_qpos |
| 18 号逐帧 conf | R&R 仓 `Data/VideoPrior/poseqa/pose_audit.json` | ✅ 已增量写入 |
| GraspPose 池 + 盖 affordance | — | ⏸ 另一 Agent,未交付 |

**已验证、可直接复用的结论**(证据都在正文):
1. 嵌套接触区间 → holder/operator 角色分派,5/5(含手性相反的 27);
2. 物体运动起始 = holder 抓稳帧,18 号 **14 vs 真值 13**;
3. 刚体耦合判据**只在物体运动时可观测**,静止段无信息;
4. **物体朝向不可用** ⟹ 锚点坐标系 = 质心 + 重力轴,方位角声明为不可观测(§11.2);
5. 手/物数据**准**(投影核验),唯腕平移字段偏静 ⟹ 空间参考改用掌心代理(§10);
6. 拧的真实幅度 ~180°+轴向拔出、无重捏 ⟹ CAD 装配态初始角改"剩余半圈"(§4)。

**方法论教训**(比结论更值钱):判"数据坏没坏"先做**投影核验**,再谈统计判据 ——
本轮曾据 3D 相关性误判"手轨迹不可用/坐标差 115°",两条结论均被投影推翻(§10.1)。

---

## 0. 一句话

从三类逐帧 Prior(接触区间、手/物轨迹、可信度)**自动编译**出一条
「里程碑链(稀疏奖励,高可信锚帧)+ 模仿走廊(稠密奖励,可信度加权)」,
训练顺序、奖励结构、成功判据全部由数据推理而得;人工只做校准审批(P5),不做设计。

原则映射:锚帧=高可信帧(P4 两端锚定);操作段不模仿位姿(P2 交互帧最不可信 +
P3 盖旋转不可观测,rotation_usable=false 实测);所有目标量用**相对位姿**表达
(接触胶水原则:手↔物相对关系是视频最可信量,绝对坐标不可信)。

---

## 1. 输入清单(per clip,全自动)

| 输入 | 文件 | 形态 | 18 号实测 |
|---|---|---|---|
| 接触区间+逐帧接触分 | `contact_auto.json` | per-hand [t_on,t_off] + 逐帧邻接分 | left [3,103] / right [29,61] |
| 手轨迹 | `world_fused.npz` | MANO trans/rot/pose ×2手×T帧 | ✅ 手指/朝向准(投影核验);⚠ 腕**平移**字段偏静,空间参考改用掌心代理,见 §10 |
| 物体轨迹 | `world_fused.npz` | `object_ob_in_world_all` (1,T,4,4) | ✅ 104/104 pose_valid |
| 逐帧可信度 | pose_audit 产物 | conf_pos/conf_rot 0~100 逐帧 | ✅ 已重跑:conf_pos 中位 81、**conf_rot 全程 0**、坏帧 8.7%、lifted 41 帧 |
| 遮挡率 | pose_audit `per_frame.occl` | 逐帧 0~1 | ✅ 已产出(交互段 0.2 量级) |
| 参考 qpos | RetargetOutput | ref_qpos_left/right + replay_world | ✅ 已补跑(104/104,跳变 ≤0.10 rad);手指真实(指尖相对腕运动 12.7cm),`wrist_pos` 继承腕字段 ⟹ 见 §10.2 |

⚠ **占位陷阱**:`world_fused.npz` 里的 `hand_confidence`/`object_confidence` 是**全 1 占位**
(18 号实测),不能当真实可信度用;真实逐帧值以 pose_audit 产物为准。
⚠ **手轨迹审计**(`steps/step2_reconstruction/hand_audit.py`,2026-08-13 新建)首版判据
**自身被证伪**(把"小幅度"误判成"数据坏"),结论与修法见 §10 —— 上线前判据必须先过一次投影核验。

---

## 2. 事件抽取与角色分派(任务文法)

### 2.1 嵌套区间规则

对每只手取**最长接触区间**(碎段按 `min_len=3, max_gap=4` 合并/丢弃;27 号 auto 版
左手有 [116,118] 三帧碎段,正是要被过滤的对象):

> **一只手的接触区间包含另一只手的 ⟹ 外层手 = holder(稳瓶手),内层手 = operator(拧盖手)。**

物理解释:稳瓶手必须先于拧盖手接触、后于拧盖手离开——嵌套不是巧合,是任务语义的时间投影。

**实证(全部 5 条有 contact_auto 的 clip,5/5 通过)**:

| clip | left | right | 分派 | 对拍 |
|---|---|---|---|---|
| **18(主)** | [3,103] | [29,61] | 左 holder / 右 operator | ✓ 与视频抽帧一致 |
| 0 | [3,132] | [10,95] | 左 holder / 右 operator | 待抽帧核验 |
| 1 | [2,103] | [35,54] | 左 holder / 右 operator | 待抽帧核验 |
| 4 | [13,99] | [39,58] | 左 holder / 右 operator | 待抽帧核验 |
| 27(仅当规则证据) | [16,110] | [12,154] | 右 holder / 左 operator | ✓ 手性相反也判对;**数据已弃用,不入训练** |

回退:不嵌套(部分重叠/无接触)⟹ 该 clip 不属于本文法,交 VLM 语义仲裁或人工,不硬套。

### 2.3 自动事件 vs 人工真值:实测偏差(18 号,2026-08-13)

| 事件 | 自动 | 真值 | 偏差 | 根因 |
|---|---|---|---|---|
| hold_on | 3 | 8(碰)/13(稳) | 早 5~10 帧 | 2D 邻接在真接触前就触发 |
| op_on | 29 | 48(三指捏稳) | **早 19 帧** | present 段(15-45)holder 转瓶把盖送近右手,邻接≠接触 |
| op_off | 61 | 85(放盖) | **早 24 帧** | 重建单物体=瓶;盖脱离后无跟踪,右手携盖期间与瓶无邻接 |

推论(写进抽取器设计):
1. **邻接区间起点不能直接当接触帧**——一律做 onset 精化:邻接分持续过阈 +
   手↔物相对速度归零(抓稳的运动学定义),精化规则在 18 真值上标定、0/1/4 上盲测;
2. **盖脱离之后的事件(放盖/瓶回位)对 mask 邻接链路不可见**——自动化欠账
   记账(两部件跟踪 or VLM 事件标注);sim 侧不受影响(仿真里盖位姿精确已知,
   里程碑谓词全部可判),只是**新 clip 的自动抽取**暂缺这两个事件的视频证据;
3. 锚点搜索窗一律挂在**精化后**的事件上;±15 帧窗吸收不了 19/24 帧级的系统偏差。

**免手版 onset 判据(2026-08-13 实测,与手轨迹判据并存;更省事且不受腕字段影响)**:
**物体开始运动 = 被抓稳的运动学证据**。18 号物体 3 帧位移的静止基线 0.15cm,
阈值取 max(0.8cm, 3×基线) ⟹ 运动起始 **帧 14,真值 13,差 1 帧**
(对比 mask 邻接给的 3,差 10 帧)。该判据只用物体轨迹,不依赖手,且自带
"抓稳"语义(碰到但没抓稳时物体不动)。
局限:①**只对 holder 有效**(operator 抓的是盖,重建里没有独立跟踪 ⟹ op_on 仍缺
自动来源,记欠账:两部件跟踪 or VLM);②**纯旋转不可见** —— 18 尾段 return 回转
(86-100)在位移轨迹上完全没有信号(最后运动帧 73 vs 真值 100),因为绕轴转动
质心不动且 conf_rot=0,与 P3 一致。

### 2.2 事件序列 → 任务文法(2026-08-13 按 18 号人工真值修订)

18 号逐帧真值(用户标注,存 `data/screw18_gt.json`,**只供测试/校准读,训练代码禁读**)
把文法从四段扩成了完整闭环:

```
站姿 K0 ──▶ holder接近 ──▶ [8]碰瓶 [13]抓稳 ──▶ (present: holder 转瓶~90°送盖,15-45)
 ──▶ [48]op 三指捏稳盖 ──▶ 拧~180°(49-53) ──▶ 轴向拔出(54-58) [58]盖脱离
 ──▶ (并行: op 携盖放桌 [85] ∥ holder 回转~90° 86-100) ──▶ [100]瓶竖直回原位 ──▶ 终态
```

**里程碑只挂任务语义事件**(抓稳/捏稳/盖脱离/盖落桌/瓶回位);
present/return(转瓶送盖、回转)是**人的解法**(手腕工效学),不设里程碑、
不强模仿——只以低权重走廊形式存在,策略可以保持瓶竖直让 op 从上方接近
(锚点都参照盖系,相对参数化天然兼容两种解)。

**拧的真实幅度 = ~180° + 轴向拔出,无重捏**(用户实标)。CAD 装配态默认两圈
(720°)才脱离,与视频语义不符 ⟹ **世界构造参数修订:装配态初始 screw_angle
设在"剩余约半圈"处**,拧 ~180° 走完行程即脱离,随后盖变自由体、轴向拔离瓶口
(新增 K_pull 谓词:盖沿瓶轴离口 ≥3cm)。此为第一层世界构造决定(必须准),
依据是人工真值,不是自动先验。

---

## 3. KeyFrame 选择(稀疏锚,高可信帧)

### 3.1 选帧规则

逐帧综合可信度:`c(h,t) = conf_pos(t)/100 × (1 − occ(h,t))`(手审计落地后再乘手部档位)。

| 锚 | 搜索窗 | 选帧 | 存什么(全部相对量) |
|---|---|---|---|
| K0 站姿 | — | 约定(非视频) | 对称待命站姿(stance_prefix 沿用) |
| KA_hold 接近锚 | [hold_on−15, hold_on] | argmax c(holder,t) | holder 腕/掌相对**物体系**位姿 + 指尖质心 |
| KG_hold 抓握锚 | [hold_on, hold_on+10] 内接触分达该区间 P75 的首帧 | — | 同上 + 参与指集合(接触蒸馏交付后细化) |
| KA_op 接近锚 | [op_on−15, op_on] | argmax c(op,t) | operator 腕相对**盖系**位姿(盖轴+盖顶面参数化,见 §3.2) |
| KG_op 捏合锚 | [op_on, op_on+10] 同上规则 | — | 同上 |
| KR_op 脱离锚 | [cap_sep, cap_sep+15] | argmax c(op,t) | 盖脱离语义(sim=行程到顶+**轴向离口≥3cm**)+ 拔出方向 |
| K_place 放盖锚 | [cap_place−8, cap_place+5] | argmax c(op,t) | 盖静置桌面(sim 谓词);18 真值=85,视频侧暂靠 gt(§2.3 欠账) |
| K_end 终态锚 | [hold_replace−5, 末帧] | argmax c | 瓶竖直回原位 XY±容差;18 真值=100 |

18 号各窗逐帧 conf_pos 预览(pose_audit 实测):KA_hold 窗 85~91(零遮挡)、
KG_hold 窗 80~85、KA_op 窗 74~82、拧+拔段 74~80、放盖窗 85~89、终态窗 85~86
——**每个锚窗里都有 ≥74 的帧可选**,稀疏锚方案的原料充足;
最深的谷(52,帧 65~67)落在 KR 窗内部,argmax 自动绕开。

容差随锚点可信度缩放:`tol = tol0 × (1 + k·(1−c*))`(c* 为锚帧综合可信度;
低可信锚给松容差,而不是给错目标——P5 的工程化)。

**KA_op 就是用户点名的那个关键帧**:holder 抓稳后,鼓励 operator 走向
"视频里 operator 开始接触时它相对盖的位姿"。

### 3.2 相对量的参数化(CAD 替身兼容)

⚠ **本节已被 §11.2 约束一收紧**:物体**朝向不可用**(18 号 conf_rot 全程 0,实测
指尖到"盖端"距离恒 ≥9cm 就是这么来的)。锚点参照系因此定义为
**原点=物体质心(可信)/ z 轴=重力(可信)/ 方位角=不可观测,不进锚点**;
盖相对瓶口的精确几何由 **CAD 替身在 sim 侧**提供,视频只负责判"哪一端是盖"。
替身入场仍走 recon→CAD 对位(`recon_pose` 静态重建先例)。
operator 锚按 **(轴向高度, 径向距离, 相对重力轴倾角)** 参数化,盖径差异被径向坐标吸收。

---

## 4. 稠密信号分配(可信度门控)

按 18 号真值分段(帧号为人工标注值):

| 段 | 帧 | 稠密信号 | 依据 |
|---|---|---|---|
| 接近走廊(K0→8、13→48) | 0-8 / 13-48 | holder/op 手轨迹模仿,逐帧权 `w(t) ∝ c(h,t)`。实测该段 conf_pos 74~91、遮挡 0~0.2 = 稠密奖励的主场 | P4 |
| present 转瓶(holder) | 15-45 | **低权走廊,无里程碑**。人的解法(转 90° 把盖送到右手边)是工效学产物,不是任务要求 | 参考角色转移(§2.18) |
| 捏合+拧+拔(操作段) | 48-58 | **零位姿模仿**。sim 内生:垫↔affordance 环带距离、三指接触保持、screw_angle 进度、轴向拔出距离 | P2 遮挡 + P3 转角视频不可观测 |
| return 回转 + 放盖 | 58-100 | 盖:落桌谓词驱动(参照 §2.19 place 段机制);瓶:回原位 XY 势函数。均为相对量目标 | P1/P4 |
| 物体轨迹项 | 全程 | 仅 conf_pos 高的帧参与;18 号 conf_rot **全程 0** ⟹ 物体姿态项完全不进奖励,只用位置 | P3 |
| 时间轴 | 全程 | 视频只贡献回合长度/斜坡节奏先验(拧仅 5 帧≈0.33s,拔 5 帧),不监督过程 | §2.18 |

**盖转角:视频不可观测 ≠ sim 里没有。** 重建对"盖转了多少度"贡献为零
(对称盖,18 号 conf_rot 全程 0)——但 CAD 替身场景里这个量**精确已知**:
解析螺旋关节 角度↔上升 = 3.18mm/圈,env 已暴露
`screw_angle`/`screw_engaged`/`screw_has_depth`(带死区+去回注防漂)。
所以操作段的稠密监督**不缺**,只是换来源:视频给两端锚点与节奏,螺旋关节给过程真值。
⚠ **行程按真值改**:默认两圈(720°/6.36mm)是场景实现的出厂设定,18 号人实拧
**~180° 即脱离** ⟹ 装配态初始角设在"剩余半圈",否则 RL 要学的动作量是人的 4 倍,
时间轴先验也对不上。

**悬空拧的处理**:人把瓶拎离桌面(audit 自动检出 41 帧 lifted)横着拧;该段是低可信
操作段 ⟹ 不模仿姿态,策略可选立桌解或空中解。成功判据全部定义在**相对量**
(盖↔瓶相对转角、盖↔瓶口轴向距离),与瓶在桌上/手中无关。不强模仿,也不禁止。

---

## 5. 奖励编译与门控链

- **稀疏**:每个锚一个里程碑奖,回合内一次性,谓词=相对位姿进容差(KG 类另加接触谓词)。
- **顺序门控** = `grasp_first` 的泛化:里程碑按事件序排链,`gate_i = slowEMA(milestone_i) ≥ 0.8`
  才放行 i+1 的探索学分。§2.14 的教训(共享网络起步竞速必死,顺序门控 6/6 存活)直接继承,
  只是"抓→飞"两级链变成由事件图生成的 N 级链。
- **成功判据(自动派生,按 18 真值三级课程)**:
  C1 微拧 = holder 持稳 ∧ screw_angle 跟进 ≥25°(判据机制沿用旧 §2.21:跟进/失败线);
  C2 拧脱 = 行程到顶(初始留半圈,~180° 即到)∧ 盖轴向离口 ≥3cm(K_pull);
  C3 全流程 = C2 ∧ 盖静置桌面(K_place)∧ 瓶竖直回原位 XY±3cm(K_end)。
  逐级毕业逐级解锁,均为 sim 内谓词(仿真里盖/瓶位姿精确已知)。
- **operator 手指集**:18 真值=拇+食+中三指 ⟹ min_pads 起点 2/3(双垫必要、三垫
  满分),交另一 Agent 的 affordance 蒸馏对拍(15_2 三指模板经验先例)。
- **谁在训练**:18 号 holder=左手,**无任何可迁移权重**(旧训①是右手且基于弃用的 27 数据)
  ⟹ 一切从零。文法直接给出三阶段(每阶段单独批准起训):

| 阶段 | 谁动 | 里程碑 | 达标 | 机制来源 |
|---|---|---|---|---|
| **S1 稳瓶** | 左臂(右臂锁站姿) | KA_hold → KG_hold(+持稳/竖直) | 确定性 ≥0.9,tilt ≤5° | 旧训①同构(接近+抓+持稳),配方逐字 |
| **S2 拧脱** | 右臂(左臂冻结/低 lr) | KA_op → KG_op(三指)→ C1 微拧 → C2 拧脱+拔出 | 微拧 ≥0.7 → 拧脱 ≥0.5 | 旧训②机制,但**死因已消除**:瓶在 holder 手里,不是无人扶的自由立瓶 |
| **S3 收尾** | 两臂 | K_place(盖落桌)+ K_end(瓶回原位) | 全流程 ≥0.5 | §2.19 place 段机制复用 |

  依据:§2.14(共享网络起步竞速必死,顺序化 6/6 存活)。S2 起训时 S1 的抓握已巩固,
  正是"先巩固后开荒"的形态。单网双臂 vs 两张网在 S2 开工前按 S1 形态单独裁定。
  **present/return 转瓶不单列阶段**——若 S2 因可达性需要转瓶,策略会自己在 S1 权重上
  长出来(残差可达即可);若不需要,就不学,这正是"人的解法不强加"的检验点。

---

## 6. 与现有机器的接线

新模块 `rl_rebuild/correction/keyframes.py`:
`extract_keyframes(clip) → keyframes.json`(事件、角色、锚帧、相对位姿、c*、容差)+ 校准页。

env 侧三个改动点(一次一个上线,遵守台账纪律):
1. 里程碑奖励项 + 门控链(泛化 `grasp_first`,读 keyframes.json);
2. 模仿奖励接逐帧权重向量 w(t)(现在是均权);
3. phase 通道由 keyframes 派生(现在由 contact_auto 手工烘入——同源,改为统一出口)。

**抽取器怎么验收(27 真值已弃用,换三道验证)**:
1. **校准页人眼批准**(§7,主验证):18 号每个锚出一页叠画,用户逐锚批;
2. **跨 clip 文法一致性**:同一套代码跑 0/1/4,角色/事件序/锚帧必须产出结构一致的文法
   (它们不入训练,只当抽取器的一致性测试集);
3. **FK/IK 可达性冒烟**:每个锚的相对位姿在机器人工作空间内可达(KF2)。

---

## 7. 校准页(P5:可信度标错比没有更糟)

`keyframes.py --calib`:每个锚输出 视频帧叠画(锚帧+手/物投影+c* 数值)一页,
**用户人眼批准后才接线训练**。沿用 pose_calib_sheet 流程。

---

## 8. 预注册草案(批准后誊入 DESIGN_LOOP)

| # | 假设 | 证伪信号 | 若被推翻 |
|---|---|---|---|
| KF1 | 嵌套区间规则的角色分派在全类 clip 上正确(已 5/5) | 任一 clip 分派与视频不符 | 该 clip 交 VLM 语义仲裁;规则不硬套 |
| KF2 | 高可信锚的相对位姿在机器人工作空间内可达 | 冒烟 FK/IK 审计锚点不可达(参照训①的 IK 误差口径) | 调搜索窗重选锚;仍不可达则该 clip 淘汰并记录 |
| KF3 | 里程碑链+可信度加权走廊足以自组织"先抓后拧",无需手写课程 | 某 milestone 长期 0 导致链死锁 | 查该锚容差/可达性;不回退手写课程(那是被证伪对象) |
| KF4 | 操作段零模仿不缺信号(sim 内生信号够) | op 接触后 screw_angle 长期 0 且 pad_touch>0 | 加密 affordance 稠密项(另一 Agent 交付物接入点) |
| KF5 | CAD 替身资产承载 18(盖径/瓶径差被相对参数化吸收) | 18 冒烟:锚点谓词零学习不可达,或抓握几何与 KA/KG 锚系统性冲突 | 替身按 18 重建包围盒(6.8×24.9cm)缩放重试;再不行回资产库检索 |
| **KF6** | onset 精化(邻接持续+相对速度归零)能把 op_on 从自动的 29 拉到真值 48 附近(±5 帧) | 在 18 上标定后,0/1/4 盲测偏差 >10 帧 | 邻接链路不足以定接触帧 ⟹ 升级为 VLM 事件标注,记欠账 |
| **KF7** | present/return 转瓶是人的工效学解法,机器人不必学(锚点全在盖系,相对参数化免疫瓶朝向) | S2 中 op 到位率长期 0 且探针显示"盖被瓶身遮挡/不可达" | 把 present 升格为里程碑(holder 转瓶 90° 作为 S2 前置),即"人的解法其实是任务必需" |

**判据**:
- **S1**(左手抓稳瓶身+竖直):毕业 gate≥0.8;主判据 = 确定性 ≥0.9 且 tilt ≤5°;
  判死 = 15M 未毕业 或 8M 回避塌缩形态。
- **S2**(捏盖→微拧→拧脱拔出):主判据 = 微拧口径 ≥0.7、拧脱口径 ≥0.5(新技能低期待);
  回归 = S1 持稳不破;判死 = 8M 回避塌缩(§7.2 形态)。
- **S3**(放盖+瓶回位):全流程 ≥0.5;回归 = S2 不破。
- 通用性总判据:配方=冠军四组件逐字,训练逻辑全部出自 keyframes.json,
  **零 per-clip 人工裁定**;每一处被迫人工介入 ⟹ 记入自动化欠账清单
  (当前已挂账两笔:§2.3 的 op_on/op_off 事件精化、盖脱离后的两部件跟踪)。

---

## 9. 工序与分工

| 序 | 事项 | 谁 | 状态 |
|---|---|---|---|
| 1 | 18 号 retarget 补跑(replay_world + 双手 ref_qpos) | 我方 | ✅ 2026-08-13 完成,104/104 有效,帧间跳变 ≤0.10 rad |
| 2 | 18 号逐帧 conf(pose_audit) | 我方 | ✅ 完成:conf_pos 中位 81、**conf_rot 全程 0**、坏帧 8.7%、lifted 41 帧 |
| 3 | 18 号人工事件真值 | **用户** | ✅ 完成,存 `data/screw18_gt.json`(帧图 `data/screw18_frames/`) |
| 4 | `keyframes.py` 抽取器(含 onset 精化)+ 校准页 | 我方 | 待写 ← **下一步** |
| 5 | 跨 clip 一致性盲测(0/1/4)+ FK/IK 可达性冒烟 | 我方 | 待跑 |
| 6 | 18 号 GraspPose 候选池(瓶身)+ 盖 affordance(三指口径) | **另一 Agent** | 等交付 |
| 7 | CAD 替身对位到 18 场景 + **装配态初始角改为剩余半圈** | 我方 | 待跑 |
| 8 | 校准页人眼批准 | **用户** | — |
| 9 | env 改动(里程碑链/加权模仿/phase 出口,逐个上线)+ 冒烟 | 我方 | 待批 |
| 10 | S1 → S2 → S3 逐阶段起训 | **用户逐段单独批准** | — |

---

## 10. 手/物数据核查(2026-08-13,**投影法定案;本节推翻本节此前的"阻塞"结论**)

### 10.1 过程与更正

为写抽取器首次审计手轨迹,我先用"手↔物刚体耦合"判据得出"18 号手轨迹不可用"的结论,
**该结论已被用户质疑并经投影法推翻**。用户论点(成立):18 号瓶子就在手边、小臂搭在
桌沿,靠腕部转动即可交互,运动幅度本就小;27 号瓶在正前方需要伸手,幅度自然大 ——
**幅度小 ≠ 数据坏**。

**定案方法**:把 EgoDex 的 ARKit 关节与重建物体位姿,用**数据集自带的相机内外参**
投影回原视频画面(完全绕开我们的重建/融合链路)。结果(`scratchpad/arkit_overlay.png`、
`obj_overlay.png`):

| 通道 | 结论 | 证据 |
|---|---|---|
| ARKit 手指关节 | ✅ **准** | 6 帧全部贴合双手(f45/f58 红点在瓶身、绿点在盖上) |
| 物体位姿 | ✅ **准** | 黄圈全程贴瓶,含横躺(f45/58)与立回(f75/100) |
| 手指 MANO 拟合 | ✅ **已生效** | `hand_pose` 非零(max 0.94 rad),replay 指尖相对腕运动 12.7cm |
| 手腕**平移**字段 | ⚠ 偏静 | ARKit `leftHand/leftArm/leftForearm` 整条臂链跨度 0.5/0.9/1.2cm;而指节 4~10cm、指尖 17.5cm。图上大红点全程钉在袖子同一点,真实腕位约动 4~5cm |
| 手↔物坐标一致性 | ✅ **一致**(此前"差 115° yaw"的判断**作废**) | 两者用同一相机都投影得准 ⟹ 同一世界系;原判据是拿"不动的腕"与"会动的瓶"做相关性,必然得 cos≈0 |

### 10.2 唯一遗留的小问题与修法

`world_fused.hand_trans`(以及下游 replay/ref_qpos 的 `wrist_pos`)取的是 ARKit
`leftHand/rightHand` 平移 —— 这条臂链偏静,与指节实测 7~10cm 的运动不符。
**修法(我方本地、有界)**:手的空间参考改用**指节推出的掌心代理**
(四指 knuckle 均值,已验证准),而不是那个腕字段;`arkit_to_mano` 的拟合与
手指通道**不动**。不重跑 HaWoR、不换 clip(用户裁定)。

影响面:KA/KG 锚点的"手相对物体位姿"精度(4~5cm 量级的差别,对抓握锚是要紧的);
接近走廊模仿受影响很小(本就只提供时间轴/形状,§2.18)。

### 10.3 留下的真收获

1. `steps/step2_reconstruction/hand_audit.py` 保留,但**判据须改**:①用掌心代理而非
   腕字段;②"小臂支撑+腕部转动"型抓握下,腕的刚体耦合假设不成立,应改测
   **掌心↔物体**距离恒定性。**本次教训入库:判据本身也要先被证伪一次再上线(§2.20 姿态
   仪表事件的重演)。**
2. **投影法应成为所有"数据准不准"问题的第一手段** —— 3D 空间里的相关性/距离判据
   依赖多个坐标系同时正确,任何一环错都会伪造出结论;投影只依赖相机内外参。
3. 无手版 onset 判据(物体开始运动=抓稳,18 号实测差 1 帧)照样有效且更省事,保留。

### 10.4 预注册(修订)

| # | 假设 | 证伪信号 | 若被推翻 |
|---|---|---|---|
| KF8 | 掌心代理(四指 knuckle 均值)是可用的手部空间参考 | 掌心↔物体距离在抓握段波动 >3cm,或投影不贴手 | 改用指尖质心/MANO 前向的腕位;仍不行才考虑 HaWoR |
| KF9 | 手腕平移偏静不影响 S1 学习(参考只供时间轴/前缀/目标,§2.18) | S1 在 15M 内未毕业而旧任务 ~8~20M 毕业 | 说明接近走廊形状确有价值,再议手轨迹升级 |

---

## 11. `keyframes.py` 抽取器实施方案(2026-08-13 制定)

### 11.0 KeyFrame 的定义(用户口径)

> **KeyFrame = 由 Video Prior 推理出的、且本身足够可信的关键帧;
> 关键帧之间可以再插入若干高可信的过渡帧。**

于是参考不再是"逐帧模仿一整条轨迹",而是**一串被挑选过的可信路点**:
语义锚(任务事件)+ 过渡锚(高可信、且形状上必要)。没被选中的帧不产生任何监督 ——
**不可信的帧不是降权,是不参与**(P4 的严格版)。

### 11.1 已验证可用的信号(全部在 18 号上实测)

| 信号 | 用途 | 实测 |
|---|---|---|
| 嵌套接触区间 | holder/operator 角色分派 | 5/5 正确(含 27 手性相反) |
| **物体运动起始** | holder 抓稳帧 | **帧 14 vs 真值 13(差 1)**;mask 邻接给 3(差 10) |
| 掌心在物体系中的静默 | 刚体耦合验证 | 可用,**但只在物体运动时可观测**(静止时手物皆不动,判据无信息)⟹ 必须与"物体在动"联合 |
| conf_pos / occl 逐帧 | 挑锚帧 | 各锚窗内均有 ≥74 分的帧 |

### 11.2 两条硬约束(实测得出,直接决定数据结构)

**约束一:物体朝向不可用 ⟹ 锚点只能挂在"重力对齐"的物体系上。**
实测:指尖到"瓶盖那一端"的距离全程 ≥9cm(捏稳时应 ~2cm)。根因不是手也不是质心
(两者投影都准),而是**物体旋转**(18 号 conf_rot 全程 0)—— 任何依赖朝向的几何量
(盖端位置、瓶轴方向)都被污染。
⟹ 锚点参照系定义为:**原点=物体质心(可信),z 轴=重力(可信),方位角(绕 z)声明为
不可观测、不进锚点**。盖在瓶上的精确位置由 **CAD 替身在 sim 侧给出**,视频只负责
"哪一端是盖"(第 0 帧静置时世界 z 更高的那端)。这是 P3"不可观测自由度事先声明"
的第二次实战。

**约束二:operator 的捏合帧目前无可靠自动来源。**
三个候选信号在 18 上都不干净:mask 邻接早 19 帧;手指开口(拇↔食/中)在捏合处
5.9cm、真正收拢到 4.0cm 要等到帧 54;指尖↔盖端距离被约束一污染。
⟹ **本轮 operator 事件用人工真值**(`data/screw18_gt.json`),并**显式记为自动化欠账**;
下一轮按优先级试:①**2D 口径**(指尖投影 ↔ 盖在图像中的位置,绕开 3D 朝向问题);
②盖单独跟踪(两部件);③VLM 事件标注。

### 11.3 处理链(每一步都可单独证伪)

```
load ─▶ roles ─▶ events(onset 精化) ─▶ confidence ─▶ keyframes ─▶ transitions ─▶ express ─▶ emit
                                                                        └─▶ calib(HTML 校准页)
```

1. **load**:clip → 帧号、contact_auto、物体位姿(相机系+世界系)、ARKit 关节、
   逐帧 conf_pos/occl、相机内外参。**一切相对量在相机系里算**(投影已验证准,
   且与世界系对齐无关)。
2. **roles**:最长接触区间的嵌套判定 → holder / operator;不嵌套则标 `unsupported`,不硬套。
3. **events**:每个事件带 `{frame, method, confidence}` 三元组,方法优先级:
   `object_motion_onset` > `rigid_coupling_quiet`(需物体在动)> `adjacency_raw` > `manual_gt`。
   **来源必须写进产物**,人工来源在校准页上高亮 —— 通用性判据要能一眼查出哪几处还靠人。
4. **confidence**:`c(t) = conf_pos(t)/100 × (1 − occl(t)) × hand_ok(t)`。
   锚点是"手相对物体"的量,所以两边都要可信才算数。
5. **keyframes**:每个事件在其搜索窗内取 `argmax c(t)`;并要求 `c* ≥ c_min`(默认 0.5),
   达不到就**不发这个锚**(宁缺毋假),在产物里标 `dropped`。
6. **transitions**(用户点名的"高可信过渡帧"):在相邻两个锚之间做
   **可信度门控的 Douglas–Peucker**:只有 `c(t) ≥ c_min` 的帧有资格当断点;
   在有资格的帧里取"离两端连线偏差最大"的那个,递归到偏差 < `tol_path`(默认 2cm)。
   直线段自动一个点都不出,拐弯处自动加密 —— **路点数量由轨迹形状决定,不是手调的**。
7. **express**:每个锚/过渡帧存
   `{t, kind, c, 掌心与指尖在重力对齐物体系中的位置, 手腕朝向(相对重力+接近方向), tol}`;
   `tol = tol0 × (1 + k(1−c))` —— 低可信锚给松容差,不给错目标。
8. **emit**:`keyframes.json`(schema 见下)+ `--calib` 出校准页。

### 11.4 产物 schema(草案)

```json
{ "clip":"egodex_auto/screw_unscrew_bottle_cap/18", "fps":15, "n_frames":104,
  "roles":{"holder":"left","operator":"right","rule":"nested_contact","ok":true},
  "frame_ref":{"origin":"object_centroid","z":"gravity","azimuth":"unobservable"},
  "events":[{"name":"hold_grasp","frame":14,"method":"object_motion_onset","gt":13}],
  "anchors":[{"id":"KG_hold","t":14,"kind":"key","c":0.81,
              "palm_in_obj":[...],"tips_in_obj":[[...]],"tol_m":0.03}],
  "transitions":[{"id":"T_hold_1","t":9,"c":0.86,"palm_in_obj":[...],"tol_m":0.05}],
  "debts":["op_grasp 来自人工 GT","cap_place/bottle_return 视频侧不可见"] }
```

### 11.5 校准页(`--calib`)

复用本次做投影核验的那套代码,输出一张自包含 HTML:每个锚/过渡帧一行 ——
原视频帧 + 手部关节与物体质心的投影叠加 + `c` 数值 + 事件来源(自动/人工要显眼)。
**用户逐条批;未批准的锚不进训练。** 这是 P5 的执行环节。

### 11.6 验收(按顺序,前一关不过不做下一关)

1. **事件对拍**:与 `data/screw18_gt.json` 逐事件比,自动来源的偏差 ≤3 帧;
2. **跨 clip 一致性**:同一套代码跑 0/1/4,角色/事件序结构一致,不崩;
3. **可达性冒烟**:每个锚的相对位姿在 DexMate 工作空间内可解(FK/IK);
4. **人眼校准页**:用户逐锚批准;
5. 以上全过才接 env(§6 的三个改动点,一次一个)。

### 11.7 新增预注册

| # | 假设 | 证伪信号 | 若被推翻 |
|---|---|---|---|
| KF10 | 可信度门控的 Douglas–Peucker 能产出"够用且都可信"的过渡帧(数量 ~3~8/段) | 某段一个合格点都没有(全段低可信),或退化到几十个点 | 该段只留两端锚(退回纯稀疏);若普遍如此则 `c_min` 标定有问题 |
| KF11 | 重力对齐物体系 + 方位角放开,足以表达抓握锚(瓶是旋转对称的) | 同一 clip 内 KG 锚在方位上自相矛盾,或 sim 冒烟时抓不到 | 方位角改由 CAD 替身+GraspPose 定,视频只给高度/半径 |
| KF12 | operator 事件的人工来源是**本轮唯一**的人工输入 | 校准页上出现第二处人工标记 | 记欠账并评估是否影响"通用性零人工"判据 |
