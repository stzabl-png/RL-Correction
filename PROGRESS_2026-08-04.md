# 进度存档 2026-08-04

从「接触点提取」开始，一路查到重建源头的相机内参问题。本文记录**做了什么、发现了什么、还剩什么**，以及每个改动的依据，便于接手或回顾。

---

## 一、接触点提取（stage 1~4，已完成）

**目标**：把 retarget 出来的 SharpaWave 手对齐到物体上，输出接触热力图。

### 核心洞察

重建的手位置不可信（EgoDex 上偏 15~40 cm），但**画面里"手挡住了物体哪一块"是直接观测**，没有深度歧义。用看得见的修正猜出来的。

关键区分：手压在物体上的那圈像素是**遮挡边界**，不是接触点——真正的接触点在手背后，单视角永远看不见。所以它是**定位证据**，不是接触标签。

### 每个自由度由能真正约束它的证据驱动

| 自由度 | 证据 | 原因 |
|---|---|---|
| 垂直视线的 2 个 | 手对物体的遮挡（"咬痕"） | 横向挪 1 cm 遮挡形状立刻变 |
| 沿视线的 1 个 | 物体几何（贴合 + 不穿模） | 沿视线滑动遮挡几乎不变，轮廓管不了深度 |

外加负证据：相机看得见且没被手挡的表面，**确定没有接触**。

### 代码

```
ego_pipeline/contact/
├── frames.py        坐标系统一(Umeyama 反解 ref_qpos 的桌面系) + 体检
├── urdf_fk.py       SharpaWave URDF 正运动学(numpy+trimesh, 不引入 pinocchio)
├── observe2d.py     2D 证据: bite2d(咬痕) / free(确定没接触) / occluded
├── align.py         3-DoF 平移求解 + 多起点搜索
├── heatmap.py       逐顶点接触权重(物体局部系)
└── viz.py           诊断图

tools/
├── contact_align_heatmap.py   主入口(stage check/align/heatmap)
├── contact_batch_summary.py   批量指标表 + CSV
├── contact_gallery.py         自包含 HTML 索引页(红黄绿分级)
├── object_recon_check.py      物体重建质量诊断
├── label_frame_audit.py       审计 SAM3D 标注帧
├── take_quality_gate.py       数据质量闸(写 data_quality.json)
└── propose_label_frames.py    推荐更好的标注帧
```

### 最终参数（经三轮对照实验确定）

```
多起点搜索  ON   沿视线 ±3/±6 cm 播 5 个种子，粗搜后精修最优 2 个
--pen-metric  pmean    只对真正穿模的点求均值
--w-pen  1.5    --w-touch 0.7    --w-neg 3.0
```

**被实验否掉的想法**（都写进了参数 help，避免重走）：
- `pen=max`：把手推出物体、失去接触，IoU 0.599→0.450
- `w_touch=1.5`：强贴合+强不穿模把手按在朝相机那面，负证据反弹到 100%
- `E_ray`（误差沿视线的先验）：实测修正 15.6 cm 里只有 0.8 cm 沿视线，先验是错的

### 结果（19 条 usable take）

| | 起点 | 最终 |
|---|---|---|
| 绿/黄/红 | 12/5/2 | **15/4/0** |
| 咬痕 IoU 中位 | 0.638 | 0.601 |
| 穿模中位/最大 | 0.52/2.50 cm | **0.41/1.35 cm** |
| 负证据违规 | 1 条 | **0 条** |

索引页：`Output/RetargetOutput/egodex/part2/basic_pick_place/contact_gallery.html`

### 已知边界

- **22 个手指关节角从未被修改**，只做刚体平移。所以热力图描述"当前位形接触在哪"，不是"一个好抓取接触在哪"（实测 80% 热点归食指）
- 热区必然在背对相机那面（可见面被负证据否决）
- 只做了单帧，区间聚合未做

---

## 二、v17A 自动交互分割接入（已打通）

**位置**：`experimental/hoi_detr_v17a/`（HOI-DETR + SAM2，来自 `HumanVideo2RobotData` 分支 `STEP_3_V17`）

### 接入方式（关键设计）

**不直接用 v17A 的 mask**，而是用它回答"哪一帧、点哪里"，再让流水线自己的 SAM2 传播全片。

原因：v17A 只覆盖交互窗口（take 11 是 31~62/78 帧），而 `fp_pose` 需要全片物体轨迹。

```
tools/v17a_to_label_prompt.py       单物体
tools/v17a_multi_object_prompt.py   多物体(瓶+盖等配对场景)
```

点击点取「离边界最远的内部点」（距离变换最大值），不是质心——C 形或被遮挡的 mask 质心可能落在物体外。

### 实测成功率

**12/19（63%）**。失败集中在 `run_persistent_mask_sequence.py:41-43` 的
`raise ValueError("conditioning masks overlap")`——多实例种子重叠时**整条放弃**而不是仲裁。
修法：重叠时按面积/置信度归属。**未做**。

### 多物体必须一次重建

ViPE 焦距估计**不确定**：同一视频跑两次，内参差 58~98 px、手轨迹差 3~13 cm。分开跑的两个物体在**不同世界系**，拼起来会静默错位。

`label_prompt.objects` 是列表、`sam3d` 遍历它、`world_fused` 存 `object_ob_in_world_all` —— 流水线本来就支持，之前只是每次放一个物体。

---

## 三、相机内参共识（本次核心，已落地）

### 问题

vipe 在**静止相机**视频上失效：

| | vipe fx | GT fx | 相机行程(vipe) | GT 行程 |
|---|---|---|---|---|
| basic_pick_place/11 | 727.6 | 736.6 | 0.198 m | 0.135 m |
| **screw_unscrew/2** | **1934.1** | 736.6 | **0.869 m** | **0.022 m** |

拧瓶盖时人两手在身前、头不动，单目 SLAM 没有视差，只能把手和物体的运动误读成相机运动，并把焦距一起拧歪 2.63 倍。

basic_pick_place 能用是因为人伸手够物体时头会跟着动，有真实视差。

### 方案：一个数据集一个常数

内参是**设备属性**不是视频属性。EgoDex 全用同一台 Vision Pro，真值是常数，我们却独立估了 28 次。

```
tools/dataset_camera_consensus.py    收割 → MAD 剔除 → 中位数 → 冻结
Output/ReconstructOutput/egodex/dataset_camera.json
   fx = fy = 741.44,  cx=960, cy=540,  image_size=[1920,1080]
   23 条样本，剔除 5 条(全是 screw_unscrew, 2.55~2.68×)
```

**28 条取中位 = 745.0，距 GT 736.63 只差 1.1%**——不用 GT 也能得到准确共识。

### 判据（不是固定的样本数）

需要多少条取决于该数据集的离散度和离群比例，工具**只输出簇结构让人判断**，不内置魔法数字。

本批的决定性证据：**最近的离群点距共识 155%**，主簇和离群簇之间隔着 1.5 倍空白，切分毫无歧义。

样本量与精度（自助法，有放回）：

| N | 中位数 95% 区间半宽 |
|---|---|
| 8 | ±1.2% |
| 20 | ±0.4% |
| 100 | ±0.3% |

**存在 +0.65% 的系统偏差，不随样本量减小**——那是 GeoCalib 在这台相机上的估计偏差。所以共识保证的是**一致性**不是准确性。用户已确认只需要一致性。

⚠️ 绝对尺度的漏口：SharpaWave 手是真实尺寸。共识偏 10% 会让 10 cm 物体的抓取错 1 cm。目前 0.65% 安全。

### 代码改动

ViPE 的 `init.intrinsics="gt"` **是个空壳**——配置项、文档、`optimize_intrinsics` 联动都在，但 `DefaultAnnotationPipeline` 从没读过这个值，`GeoCalibIntrinsicsProcessor` 无条件添加。配了 `gt` 会静默地继续用估计值。

```
third_party/vipe/vipe/config/streams.py        注册 intrinsics 字段 + 校验(pydantic 严格模式会拒绝未注册字段)
third_party/vipe/vipe/pipeline/default.py      实现 gt 分支; stream 没提供内参时明确报错
third_party/vipe/vipe/streams/raw_mp4_stream.py 可选 intrinsics + attributes() + camera_type
third_party/vipe/configs/streams/raw_mp4_stream.yaml
recon_pipeline/vipe/run_sequence.py            自动读冻结常数、校验分辨率、传 Hydra override
recon_pipeline/fuse/run_sequence.py            写 intrinsics_source (闭环: 防止共识自我验证)
```
全部有 `.bak`。

四个坑（都不在文档里）：
1. pydantic 严格校验拒绝新字段
2. `GeoCalibIntrinsicsProcessor` 无条件添加
3. `attributes()` 基类返回空集，pipeline 在迭代前查不到 stream 的内参
4. 跳过估计器后 `camera_type` 也没了，SLAM 崩在 `unpack_optional`

### 效果（screw_unscrew/2）

| | 修复前 | 修复后 |
|---|---|---|
| fx | 1934.11 | **741.44** |
| 左手 3D 误差 | 61.4 cm | **15.1 cm** |
| 右手 3D 误差 | 46.7 cm | **10.0 cm** |
| 物体投影面积比 | 3.66 | **1.69** |
| 比例漂移 | 4.1× | **1.5×** |

**收敛到 basic_pick_place 同一水平**（那批一直是 8.6~10.7 cm）。

**未解决**：相机行程仍虚构（1.105 m vs GT 0.022 m），瓶子偏大约 30%。锁内参后 SLAM 把误差转嫁到位姿/尺度上。

---

## 四、SAM3 手部过滤结论断链（已修复）

流水线**有** SAM3 左右手一致性校验（`_legacy/hawor/sam3_mano_filter.py`，投影 MANO 算 containment，阈值 0.5），它**正确地**拒绝了 screw_unscrew/2 左手第 0~29 帧（containment 全 0.0，手被放到画面对侧）。

但恢复过滤结论的 `pred_valid = filter_keep_valid` **只写在 `if config.use_infiller:` 分支里**，而本流程 `no infiller`——结论算出来了从没被用上。

实证：过滤前 169/170 有效，过滤后 **170/170**（不减反增）。

修复：`_legacy/hawor/common.py` 加 `elif` 分支。验证后 `hand_valid` 左手前 30 帧正确置 0。

---

## 五、我引入的一个回归（已修复，需注意）

`fuse/run_sequence.py` 有两份：
- **权威版**：`/home/lyh/Project/HumanVideo2RobotData/recon_pipeline/fuse/`，962 行，**带 `trajectory_cleaning`**（插值/夹住缺口 + 写 `hand_confidence`/`object_confidence`）
- **本仓 vendored**：`ego_pipeline/Reconstruction/recon_pipeline/fuse/`，955 行，**没有**

我改本地那份再 scp 覆盖远端，把轨迹清理删掉了。已从上游重打补丁恢复。

**教训**：本仓 `ego_pipeline/Reconstruction/recon_pipeline/` 是旧快照，改 recon_pipeline 要以 `HumanVideo2RobotData/recon_pipeline` 为准。

---

## 六、远端环境（msc / yanghong）

```
ssh yanghong@169.229.192.185     现有密钥免密可登
8× RTX A6000 48GB, 128 核, 1TB 内存, 13TB 磁盘
~/Reconstruct_and_Retarget       代码(非 git 仓, 纯拷贝) + recon_pipeline 内置
conda: cu128 HV2RD hawor biv2ap codetr env_isaacsim isaac sam3
```

⚠️ **共享机器**：`msc` 用户常占 GPU 0~5（约 143 GB），我们用 GPU 7（和 6）。
`reconstruct.sh` 的 `--gpu-ids` 已改为可用 `RECON_GPU_IDS` 环境变量覆盖。

装 codetr 踩的坑：
1. setuptools 混装（`_distutils/` 新旧文件混合）→ 清空后重装 60.2.0
2. **系统 nvcc 13.0 vs torch cu128** → 指向 `~/miniconda3/envs/cuda128`
3. **conda CUDA 头文件在 `$CUDA_HOME/targets/x86_64-linux/include`**，不是 `$CUDA_HOME/include` → 要设 `CPATH`
4. 本地 `nohup ssh ... &` 会随父 shell 死掉 → 用 `ssh host 'setsid nohup ... &'`
5. **`pkill -f` 会匹配到执行它的 ssh 会话本身**（我踩了 3 次）→ 用 `kill -9 $(pgrep -f ...)`

常用脚本（远端 `~/`）：`run_recon_all.sh`、`run_v17a.sh`、`run_retarget.sh`、`run_vid3.sh`

---

## 七、当前状态

### 已完成
- basic_pick_place 21 条全量重跑（**19/21 成功**，68 分钟双卡），带统一内参 + `hand_valid` 修复
- screw_unscrew/2 双物体场景（瓶身+瓶盖），已拉回本地
- 接触点提取全套工具 + 19 条产物 + 索引页

### 进行中
- **视频 3 全流程**（`~/run_vid3.sh`，v17A→prompt→重建→retarget）。这条人手移动幅度更大，用来检验"相机有运动时是否表现更好"
- screw_unscrew/2 的 fuse 需重跑以恢复 `hand_confidence`（单独调 fuse 时缺 `RECON_INTERIM_ROOT` 等环境变量，要走 `reconstruct.sh`）

### 待办
1. **take 0（hawor）、take 10（sam3d_scale）失败**——大概率 msc 占卡导致显存不足，需重跑
2. **v17A 的 `conditioning masks overlap`**——6 条 take 因此整条失败，改成仲裁而非 raise
3. **相机位姿虚构**——静止相机时强制静态位姿，能再吃掉剩余的 6~10 cm 深度误差和物体 30% 偏大
4. **HaWoR 9~10 cm 横向偏差**——所有 EgoDex take 都有，与 vipe 无关，是接触点提取要靠 2D 遮挡重定位的原因
5. 区间聚合（热力图目前单帧）
6. **代码全部未 commit**

### 未解决且已知
- 瓶盖 mesh 碎几何（7397 px 小 mask → 80.7 万顶点 → `sam3d_scale` 显存溢出，视频 4 双物体因此失败）
- take 13 接触对齐脆弱（种子散布 0.597 全场最大，`w_pen` 阈值无余量）
- take 55 咬痕 IoU 0.068

---

## 八、Isaac 查看指令

```bash
cd /home/lyh/Project/Reconstruct_and_Retarget/third_party/MagicDexMate
./run_retarget.sh ../../Output/RetargetOutput/egodex/screw_unscrew_bottle_cap/2_scene
```
ENTER 播放 / 再 ENTER 重播 / q+ENTER 退出。

只看某一个物体（**必须加 `--object-index`**，否则会用错轨迹）：
```bash
OMNI_KIT_ACCEPT_EULA=YES .venv-isaac/bin/python sim/retarget_isaacsim.py \
  --traj <take>/replay_world.npz --object-usd <take>/object_1.usd --object-index 1 \
  --mode render --table --recenter table --scene-rot obj0 --smooth 5 --cam-view iso --cam-dist 1.3
```

多物体支持是本次新加的（`retarget_isaacsim.py` 有两份独立副本，**`run_retarget.sh` 执行的是 `third_party/MagicDexMate/sim/` 那份**）。所有物体共用同一个 `Rs`/`scene_shift`/落桌抬升量——用各自的变换会重现相对位置错乱。physics 模式明确拒绝多物体。

---

## 9. 视频 3 瓶子被放倒 —— 查明经过（2026-08-04 晚）

### 结论：不是标注帧的问题，是**单视角下瓶轴的深度倾角不可观测**

按顺序排除的三个假设：

**假设 A：mesh 自己的规范朝向不对** —— 否。AABB extents 用的是 SAM3D 任意选的局部坐标系，
不可靠；改用顶点 PCA，两条视频的长轴都稳定落在 local axis 1（奇异值比 2.50 / 3.29）。
长轴判断成立。

**假设 B：世界系重力方向被编出来了** —— 否，实测否定。视频 3 相机路径只有 0.081 m
（近乎静止），本来最可疑，但对比 EgoDex GT 相机外参：

```
             重建相机倾角   GT 相机倾角   差
视频 3          18.2 deg      18.0 deg    0.3 deg
视频 2          28.6 deg      32.1 deg    3.6 deg
```

世界系是对的，相机也是对的。**重力假设作废。**

**假设 C（成立）：投影约束不住绕相机横轴的倾角**

用 mask 像素 PCA 量图像内主轴朝向（比包围盒 h/w 可靠）：

```
                   观测主轴      投影主轴     中位失配
视频 3 (帧20标注)  2.6~12.1 deg  1~46 deg     7.9 deg
视频 3 (帧0标注)   同上          —            5.4 deg
视频 2 (好的)      0.7~88.8 deg  0.1~88.2 deg 1.2 deg
```

视频 3 的瓶子**全片在屏幕上都是竖的**（观测主轴 2.6~12.1 deg），相机只倾斜 18 deg。
fp_pose 的投影和观测吻合到 5~8 deg —— **二维上它是对的**。可世界系 |z| 只有 0.02~0.51。

原因：一个竖直投影的长条，其三维轴可以在**包含相机光轴的平面内任意倾斜**，投影不变。
这个自由度单视角看不见。视频 2 之所以对，是因为瓶子在屏幕上真的转过 0.7 -> 88 deg，
运动本身提供了多视角约束；视频 3 瓶子屏幕朝向不变 + 相机不动（0.081 m 无视差）
= 这个自由度**没有任何观测约束**，fp_pose 只能瞎填。

### 换标注帧 20 -> 0 的实验结果（已跑，GPU 7，sam3d 65.8s + fp_pose 27.3s）

选帧 0 的理由：v17A 只在**检测到交互**的帧上产实例，手还没靠近时它看不到帧 0；
而帧 0 其实更好（38644 px vs 帧20 的 33593 px，手重叠都是 0%）。

```
                mesh 尺寸          2D 主轴失配   世界系 |z| (帧0/20/50)
帧20 (原)   [11.3, 27.2, 11.3] cm    7.9 deg     0.183 / 0.413 / 0.410
帧0  (新)   [15.8, 38.2, 15.8] cm    5.4 deg     0.016 / 0.115 / 0.509
```

mesh 尺寸变好（38.2 cm，接近视频 2 的 35.6 cm）、二维拟合变好（5.4 vs 7.9 deg），
但**三维朝向更糟**（|z| 0.016 = 几乎完全躺平）。这正是假设 C 的签名：
二维越拟合越好，三维越自由乱转。**换帧不解决这个问题。**

备份：远端 `3_scene_frame20_backup/`，本地 `3_scene_frame20/`；
上游 interim（vipe/sam3_hands/sam2_object/hawor）存在远端 `Output/_interim_keep/`
（那次跑带 cleanup 会删 interim，留一份省下 30 分钟重跑）。

### 真正该做的修法（未实现）

给 fp_pose 加**先验**，把不可观测的自由度用物理常识约束住：
1. 静止在支撑面上的物体，长轴应与重力对齐（瓶子立在桌上）——最直接
2. 或加非穿透 + 支撑面接触约束，让瓶底落在桌面上
3. 判据可复用本节的量：二维主轴失配小但世界系 |z| 与"静止"矛盾 = 触发先验

同样的根因（静止相机 -> 无视差）之前已经在焦距上出现过一次（已用数据集级常量冻结解决），
这是它在**物体旋转**上的第二次出现。

### 附带答案：为什么视频 2 的手比视频 3 差

其实不差。中位误差几乎一样（左 15.1 vs 14.6 cm，右 10.0 vs 12.2 cm）。差别只在最大值：
视频 2 左手最大 50.9 cm vs 视频 3 的 21.9 cm —— 是开头 30 帧一段左右手混淆（已被
SAM3 一致性校验拒掉），不是整体更差。视频 3 的 SAM3 一帧没拒（106/106 全有效）。
根因是视频 2 开头两手都在瓶子附近、距离近，HaWoR 的左右手判别在这种构型下容易翻。

### 可视化

- `Output/v17a_vis/3_fp_pose_overlay.mp4` —— 帧20 标注版逐帧位姿叠加
- `Output/v17a_vis/3_fp_pose_overlay_frame0.mp4` —— 帧0 标注版
- `Output/v17a_vis/3_labelframe_compare.png` —— 候选标注帧对比
  （橙色 = 按 fp_pose 位姿投影的 mesh，绿箭头 = mesh 长轴，左上角 |z|）
