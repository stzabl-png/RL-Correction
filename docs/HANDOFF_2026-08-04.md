# 交接 2026-08-04 01:30 PDT — Approach+Pick 任务收官总结

> 取代 `HANDOFF_2026-08-03.md`。先读 `CLAUDE.md` → 本文 → `DESIGN_LOOP.md` §2.12~§2.18。
> 成功经验详录: `logs/overnight_20260803/无混合期中_8p5M/README.md`。

---

## 0. 一句话 (2026-08-04 07:00 终版)

**Approach+Pick 已解决且复现盖章**。三条独立 seed 全部收官(40M, 确定性端到端):
- 冠军(seed42): **99.99%**, 成功中位 61 步
- 复现 seed21: **100.00%**(12343/12343 零失败), 中位 70 步
- 复现 seed7: **93.93%**(过 ≥0.9 判据线; 路线偏长 140 步, 行为风格有 seed 方差)

配方 3/3 存活(混训时代 2/6+), "毕业→起飞→收敛"在 1.4~22.1M 三个不同毕业点上
全部复现。终版评测+录像已全部拉回本机(§4 产物地图)。

## 1. 冠军配方(待复现盖章后固化为默认)

```bash
--clip Grasp3 --num_envs 1024 --prior_npz .../Grasp3_candidates/8_5.npz --prior_yaw 215 \
--approach --stance_prefix 60 --kl_threshold 0.02 --grasp_first --gf_target 0.8
```

四组件缺一不可: 站姿前缀 / 先抓后飞门控 / 抖动起点池(门控自带) / kl 0.02。
**不需要**: 姿态混合(orient_blend)、锥形信任管(cone)、kl 0.01、课程提速、组3超参。
⚠ 评测/录像必须带 `--approach --stance_prefix 60`(端到端口径)。

## 2. 三大机制结论(证据见台账 §2.14~§2.17)

1. **起步竞速**: 混训两分支抢一张网,抓取先巩固则活(2/6+ 幸存率靠运气);
   门控顺序化后,毕业→起飞 6M 步(B 的 ~6 倍速),无一崩塌。
2. **交接断裂**: 标准起点练的抓取不识到达偏差(O1: 到位 76%/端到端 0);
   抖动池(±3cm/15° = 课程包络)让抓取自带容错。
3. **参考轨迹的角色转移**: 冠军接近段顶着模仿罚直飞(参考路径形状被弃用),
   但参考仍提供时间轴/站姿前缀骨架/**搬运放置段的唯一目标**;泛化场景的作用未验证。
   附: 姿态混合三臂(O1/姿态混合/O1续训)全部"到位 0.9+/端到端 0" ⟹ 改参考姿态
   重解 IK 落在别的手臂构型分支,抓取不认识 —— 姿态混合正式证伪。

## 3. 待办(2026-08-04 晨更新)

- [x] 复现终局: **成功**(seed21 100.00% / seed7 93.93%, 详见 §0 与台账 §2.18 判读)
- [x] 终版评测/录像已全部拉回本机
- [ ] **等用户拍板**: 固化方式 —— 把 grasp_first/stance_prefix 烘进 cfg 默认值,
      还是保持显式 flag(配方已记录于本文 §1 与记忆);MANUAL 补章节
- [ ] **等用户审批**: 清理名单(§5)执行
- [ ] **等用户批准开工**: Place 段(设计已分析: TRANSPORT=带货的接近模式复用
      q_ref[gs..re] 前馈; PLACE=脚本化反向合拢+落稳判定; 成功=物体落在人手放置位
      ±3cm; 目标点由 `interaction_seg[1]`+搬运参考给出; 热启动自冠军 ckpt; obs 不变)
- 附注: `先抓后飞_姿态混合` 的终版评测 0% 判为无效数据(kill 时疑似存档写坏),
      该臂权威记录以训练期 TB 为准(证伪结论不受影响)

## 4. 产物地图(本机)

```
logs/overnight_20260803/
  无混合期中_8p5M/         冠军期中: 99.95% 评测日志 + 单次接近录像 + 成功经验总结 README
  AP_B_kl02_midtrain_20p5M/ 旧半程冠军 B: 两口径评测 + 新旧口径录像对比 + README
  S1_final_40M/            S1: 98.14% 评测 + 站姿录像 + 分数图 png + 轨迹分析图 + 探针npz
  B_final_40M/             B 终版: 99.96% 评测 + 录像 + N1/N3 摘要
  AP_B_kl02.score_map.png  B 分数图
  remote_archive/          远端全部摘要/评测日志/TB快照的镜像 (今晨已同步)
  o1_trace.npz / c1_trace.npz / s1_trace.npz   逐步探针原始数据
```
远端(UCB): 各 run 的 ckpt 在 `logs/<name>/<ts>/stage1_nn/`,冠军+复现臂终版
评测/录像明晨落在 `logs/overnight_20260803/`。

## 5. 清理名单(已判死、判读价值全部记入台账,**等用户批准后删**)

**远端 UCB `~/RL_Correction/logs/`**(共约 220M,量小,清理为了目录清爽):
- 上轮消融败者: `AP_A_base` `AP_C_ppo3` `AP_D_ppo3_kl02` `AP_E_tighteps` `AP_F_noimit`
- 复现失败者: `AP_N1_seed7` `AP_N3_arema98`
- 本轮证伪臂: `AP_O1_oblend` `AP_C1_cone` `AP_OC1_combo` `先抓后飞_姿态混合`
  `O1续训_起点抖动`
- 早期杂项: `UCB_Grasp3` `UCB_approach_reflook5`
- `先抓后飞_无混合/` 内的两个**中间** run 子目录(15:22 带bug版 / 16:10 旧毕业参数版,
  只留最终 20:19 版)
- **保留**: `AP_B_kl02`(旧冠军) `AP_S1_stance`(站姿首通) `先抓后飞_无混合`(冠军)
  `先抓后飞_复现seed7/21` `overnight_20260803/`

**本机 `logs/`**:
- 确定可删(本轮败者): `AP_N2_kl01`(27M, kl0.01 败) `AP_G_seed7`(8.7M, 早停)
- 待确认(旧里程碑,可能被文档/回归引用,删前问一句): `GraspPose_3`(43M)
  `PreGrasp_0`(13M) `PreGrasp_0_j6`(30M) `G3_8_5`(21M) `G3_approach_8_5`
  `G3_8_5_obsv2` `AB_Grasp5_base/prior`(旧AB实验)
- **保留**: `Grasp0`(pp0 冠军, CLAUDE.md 钦定) `overnight_20260803/`(全部产物)

## 6. 在跑(今晨自然收官, 勿动)

| GPU | run | 预计收官 |
|---|---|---|
| 7 | 先抓后飞_无混合(冠军) | ~04:00 + 自动终版评测/录像 |
| 3 | 先抓后飞_复现seed7(已毕业@14.1M) | ~06:00 |
| 2 | 先抓后飞_复现seed21(阶段A 0.52 爬坡中) | ~06:00 |
| 5 | (姿态混合的终版评测,评完自动释放) | ~01:40 |

监控: 每小时状态报 + 收官事件报,全部自动。
